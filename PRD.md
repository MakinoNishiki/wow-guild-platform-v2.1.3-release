# WoW 团本考勤管理系统 - 产品需求文档 (PRD)

## 1. 产品概述

### 1.1 产品名称
WoW 团本考勤管理系统

### 1.2 产品定位
面向《魔兽世界》团队副本（Raid）玩家的团队协作管理工具，提供公会成员管理、考勤记录、装备分配、心愿单等核心功能，支持多用户云端协作编辑。

### 1.3 目标用户
- 魔兽世界团队副本团长/会长
- 团队核心管理人员（指挥、装备分配官）
- 普通团员（查看自己的出勤、装备、心愿单）

### 1.4 核心价值
- 替代 Excel/腾讯文档，提供专业的 Raid 管理体验
- 支持团队成员共同编辑，类似飞书文档权限系统
- 数据云端同步，多设备访问

## 2. 技术架构

### 2.1 技术栈
- **前端**: HTML5 + CSS3 + Vanilla JavaScript (ES6+)
- **后端**: Node.js 静态文件服务器（server.js）
- **数据库**: Supabase PostgreSQL
- **认证**: Supabase Auth（邮箱注册/登录，自动确认）
- **数据缓存**: localStorage（本地模式/云端缓存）
- **部署**: Coze 沙箱 + 公网域名

### 2.2 项目结构
```
├── index.html          # 入口页面
├── css/main.css        # 全局样式（WoW 暗色主题）
├── js/cloud.js         # Supabase 集成层
├── js/app.js           # 应用逻辑
├── server.js           # Node.js 静态服务器 + API 代理
├── PRD.md              # 产品需求文档
├── AGENTS.md           # 项目说明
└── DESIGN.md           # 设计规范
```

### 2.3 数据同步机制
- **云端模式**: 数据存储在 Supabase，支持团队协作
- **本地模式**: 数据存储在 localStorage，单用户使用
- **写入策略**: 前端 → server.js 代理 → Supabase（使用 service_role key 绕过 RLS INSERT 限制）
- **读取策略**: 前端 → Supabase REST API（RLS SELECT 策略正常工作）

## 3. 数据库设计

### 3.1 核心表

#### `guilds` - 公会表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键，公会唯一 ID |
| name | varchar(100) | 公会名称 |
| owner_id | uuid | 创建者用户 ID |
| invite_code | varchar(50) | 8 位邀请码 |
| server_region | varchar(50) | 服务器区域（一区/三区/五区/十区/推荐服务器） |
| server_name | varchar(100) | 服务器名称 |
| created_at | timestamptz | 创建时间 |

#### `guild_members` - 公会成员表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| guild_id | uuid | 公会 ID |
| user_id | uuid | 用户 ID |
| role | varchar(20) | 角色：owner/editor/viewer |
| display_name | varchar(100) | 显示名称 |
| joined_at | timestamptz | 加入时间 |

#### `raid_members` - 团本成员表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| guild_id | uuid | 公会 ID |
| name | varchar(100) | 角色名 |
| class | varchar(50) | 职业 |
| spec | varchar(50) | 专精 |
| role | varchar(20) | 职责：坦克/治疗/输出 |

#### `activities` - 考勤活动表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| guild_id | uuid | 公会 ID |
| name | varchar(200) | 活动名称 |
| activity_date | date | 活动日期 |
| raid | varchar(100) | 团本名称 |
| boss | varchar(100) | BOSS 名称 |
| created_by | uuid | 创建者 |

#### `activity_attendance` - 出勤记录表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| activity_id | uuid | 活动 ID |
| member_id | uuid | 成员 ID |
| status | varchar(20) | 状态：出勤/缺勤/请假/迟到 |

#### `loots` - 装备分配表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| guild_id | uuid | 公会 ID |
| item_name | varchar(200) | 装备名称 |
| member_id | uuid | 获得成员 ID |
| boss | varchar(100) | 掉落 BOSS |
| raid | varchar(100) | 团本 |
| distributed_at | timestamptz | 分配时间 |

#### `wishlists` - 心愿单表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| guild_id | uuid | 公会 ID |
| member_id | uuid | 成员 ID |
| items | JSONB | 心愿装备列表 |

### 3.2 用户中心表

#### `user_profiles` - 用户资料表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| user_id | uuid | 用户 ID（唯一） |
| display_name | varchar(100) | 显示名称 |
| avatar_url | text | 头像 URL |

#### `user_characters` - 用户角色表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| user_id | uuid | 用户 ID |
| character_name | varchar(100) | 角色名 |
| server_name | varchar(100) | 服务器名 |
| server_region | varchar(50) | 区域（CN/Asia/US-EU） |
| armory_url | text | 英雄榜链接 |
| level | int | 等级 |
| race | varchar(50) | 种族 |
| class | varchar(50) | 职业 |
| spec | varchar(50) | 专精 |
| faction | varchar(20) | 阵营 |
| item_level | int | 装等 |
| guild_name | varchar(100) | 所属公会 |

#### `notifications` - 通知表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | uuid | 主键 |
| user_id | uuid | 接收用户 ID |
| type | varchar(50) | 类型：join/leave/role_change |
| title | varchar(200) | 标题 |
| message | text | 内容 |
| guild_id | uuid | 相关公会 ID |
| related_user_id | uuid | 相关用户 ID |
| is_read | boolean | 是否已读 |
| created_at | timestamptz | 创建时间 |

## 4. 权限模型

### 4.1 角色定义
- **Owner（所有者）**: 公会创建者，拥有所有权限
- **Editor（编辑者）**: 可编辑公会数据、成员信息、考勤、装备分配
- **Viewer（浏览者）**: 只读权限，可查看所有数据但不能修改

### 4.2 权限矩阵
| 功能 | Owner | Editor | Viewer |
|------|-------|--------|--------|
| 查看公会数据 | ✅ | ✅ | ✅ |
| 编辑成员/考勤/装备 | ✅ | ✅ | ❌ |
| 管理成员权限 | ✅ | ❌ | ❌ |
| 修改公会设置 | ✅ | ❌ | ❌ |
| 删除公会 | ✅ | ❌ | ❌ |
| 生成/查看邀请码 | ✅ | ❌ | ❌ |

## 5. 功能模块

### 5.1 认证与账号
- 邮箱注册/登录
- 自动确认邮箱（无需验证）
- 本地模式入口（无需登录）
- 用户中心（个人资料、我的角色、通知）

### 5.2 公会系统
- 创建公会（选择区域和服务器）
- 生成 8 位邀请码
- 通过邀请码加入公会
- 公会切换
- 公会设置（成员权限管理、邀请码）
- 公会 ID 展示

### 5.3 成员管理
- 添加/编辑/删除 Raid 成员
- 13 个职业选择
- 专精/职责联动
- 职业颜色标识
- 出勤详情查看

### 5.4 考勤记录
- 日历视图
- 列表视图
- 活动 CRUD
- BOSS 选择
- 出勤状态标记（出勤/缺勤/请假/迟到）

### 5.5 装备分配
- 103 件史诗装备库
- 按团本/BOSS/槽位筛选
- 分配记录
- 掉落统计

### 5.6 心愿单
- 按成员管理心愿装备
- 全员竞争概览
- 飞书同步

### 5.7 统计报表
- 出勤率图表
- 角色分布统计
- 活跃度排行

### 5.8 数据管理
- 飞书多维表格同步
- 本地 JSON 导出/导入（仅本地模式可用）
- 数据重置（云端模式下隐藏）

## 6. 用户流程

### 6.1 新用户注册流程
1. 打开网站
2. 注册账号（邮箱 + 密码）
3. 登录成功
4. 创建公会 或 输入邀请码加入公会
5. 进入主应用

### 6.2 邀请成员流程
1. Owner 打开公会设置
2. 复制邀请码
3. 发送给新成员
4. 新成员注册登录后输入邀请码
5. Owner/Editor 收到成员加入通知
6. Owner 可提升成员权限为 Editor

### 6.3 创建活动流程
1. 进入考勤记录页面
2. 点击添加活动
3. 选择团本、BOSS、日期
4. 保存活动
5. 在出勤记录中标记成员状态

### 6.4 装备分配流程
1. 进入装备分配页面
2. 选择团本/BOSS
3. 选择装备
4. 选择分配成员
5. 保存分配记录

## 7. 关键数据

### 7.1 团本数据
- 12.0 版本 4 个团本：
  - 虚影尖塔
  - 梦境裂隙
  - 进军奎尔丹纳斯
  - 孢陨幽境

### 7.2 职业数据
- 13 个职业：战士、法师、牧师、盗贼、猎人、圣骑士、萨满、德鲁伊、术士、武僧、恶魔猎手、死亡骑士、唤魔师
- 每个职业 2-4 个专精
- 3 个职责：坦克、治疗、输出

### 7.3 装备数据
- 103 件史诗装备
- 按槽位和 BOSS 掉落分配

## 8. 当前问题与待优化

### 8.1 已知问题
1. **部署延迟**: 生产环境更新后需要重新部署才能生效
2. **英雄榜解析**: 只能自动解析区域/服务器/角色名，无法获取职业/装等（需要暴雪 OAuth API）
3. **本地模式局限**: 本地模式数据无法跨设备同步
4. **移动端体验**: 部分页面在移动端的交互可以进一步优化

### 8.2 技术债务
1. app.js 文件过大（约 8000 行），需要模块化拆分
2. 部分 CSS 使用固定像素值，可以改为更灵活的响应式单位
3. 错误处理可以更加统一和友好

## 9. 后续功能规划建议

### 9.1 短期（1-2 周）
1. **UI 优化**: 修复移动端显示问题、统一按钮样式
2. **通知增强**: 支持角色变更通知、活动提醒通知
3. **导入导出优化**: 支持从 Excel/CSV 导入成员列表
4. **活动模板**: 支持保存常用活动模板

### 9.2 中期（1-2 月）
1. **英雄榜深度集成**: 接入暴雪 OAuth API 自动获取角色完整信息
2. **装备优先级系统**: 支持自定义装备分配规则（DKP/EPGP/CL）
3. **团队日程**: 支持定期活动安排、日历订阅
4. **数据统计增强**: 成员活跃度趋势、装备获取统计

### 9.3 长期（3-6 月）
1. **多公会支持**: 一个用户可以加入多个公会
2. **移动端 App**: 开发微信小程序或 PWA
3. **语音/截图识别**: 自动从游戏截图识别装备掉落
4. **社区功能**: 公会公告、讨论区、文件共享
5. **副本战术板**: 支持上传战术图、标记站位

## 10. 非功能需求

### 10.1 性能
- 页面加载时间 < 3 秒
- 数据操作响应时间 < 1 秒
- 支持 20 人团队同时使用

### 10.2 安全
- 用户密码通过 Supabase Auth 安全存储
- 所有写入操作通过 server.js 代理验证 JWT
- RLS 策略限制数据访问范围

### 10.3 可用性
- 暗色主题，护眼设计
- 响应式布局，支持桌面和移动端
- 关键操作二次确认

### 10.4 可扩展性
- 数据库设计支持未来扩展
- API 代理模式便于添加新接口
- 模块化代码结构便于维护

## 11. 附录

### 11.1 测试账号
- 邮箱：564245086@qq.com
- 密码：keleimengsi90

### 11.2 相关文档
- `AGENTS.md` - 项目工程说明
- `DESIGN.md` - 设计规范
- `skills/supabase-integration-pitfalls/README.md` - Supabase 集成踩坑记录

### 11.3 版本历史
- v3.0.0 - 新增用户中心、角色管理、通知系统
- v2.13.0 - 创建公会支持选择服务器区域和名称
- v2.12.2 - 通过 server.js 代理修复 RLS INSERT 策略失效问题
- v2.12.0 - 集成 Supabase 云端协作、公会系统、权限管理
- v2.0.0 - 单文件 HTML 重构为模块化项目
- v1.0.0 - 初始版本（单文件 HTML）
