# 任务书 #23-补丁6 修改报告：筛选条体验修正

> 日期：2026-08-07 ｜ 执行：Kimi Code ｜ 需求编号：BUG-054 / BUG-055 / REQ-084 / REQ-085（台账已登记并销号待验收）
> 状态：**已实现待运营验收**（未 commit、未 push，git 纪律候验收）

---

## 一、实际改动点（只动公示页三文件）

### 1. BUG-054 chip 光学居中 — `css/data-public.css`
`.dp-chip` 增加 `line-height: 1`（flex 居中体系、24px 高 / 12px 圆角 / 12px 字 / padding 全不动）。规则挂在基础类上，「全部」chip 与各子类组 chip 全覆盖；文件内无其他 24px chip 变体（副检查已做，`.dp-toggle` 为 26px 按钮非 chip）。

### 2. BUG-055 排除杂项行对齐 — `css/data-public.css`
`.dp-misc-row { padding-left: 64px; box-sizing: border-box; }`（64 = 组头列宽 56 + 组间 8），内容左缘与 chip 列起点（1366 档 = 217 基线）对齐。开关默认关、问号逐字文案、交互一律未动；label 结构未动（BUG-056 红线）。

### 3. REQ-084 搜索框清除钮 — `data.html` + `css/data-public.css` + `js/dataPublic.js`
- data.html：`#dpSearch` 外套 `.uc-input-wrap`，内嵌 `#dpSearchClear.uc-clear-btn`（`aria-label="清除搜索"`）——**原类复用** REQ-077 英雄榜清空钮（main.css 已随页面加载，零新样式类）；
- data-public.css：`.dp-search-row .search-input` 补 `padding-right: 36px` 让位，无布局抖动；
- dataPublic.js：有内容才显示；点击/Esc 清空 → `state.search=''` → render 还原全集；清除后 `focus()` 回输入框；无 toast（无感清除）；`resetFilters()`（赛季切换）同步隐藏清除钮。

### 4. REQ-085 套装一览区块折叠 — `js/dataPublic.js`
- 套装区块头由纯 `.dp-section-title` 升级为 `secHead('sec:tiers', '套装一览', tierFiltersHtml)`——与团本/大秘境**同构**（`.dp-section-head.dp-section-collapser` + CARET_BTN 24×24 热区/2px 描边）；
- 折叠态与另两区块同机制：`collapsed.has('sec:tiers')` 时不渲染 `.dp-tiers` 网格，sessionStorage `dp23:collapsed` 记忆（补丁4 口径），默认全展开；
- 三维筛选下拉常驻头内；折叠事件委托加 `.dp-tier-filters` 豁免（点下拉不触发折叠，与既有 `.dp-toggle` 豁免同法）；
- 只做区块级：职业级分组折叠需结构改造，按任务书取舍不做。

### 5. 版本串双头递增
`20260807.19` → **`20260807.20`**：index.html 8 处、data.html 6 处（replace_all 机械替换）。

## 二、验证（留证 `backup/2026-08-07-task23-patch6/`）

- **三 verify 全量重跑全绿**：patch **33/33**、patch3 **31/0**、patch4 **36/0**（patch4 三级折叠/热区/新本徽标断言零回归）；
- **补丁6 专项实测 13/13**（shot-patch6.js after 档，一次性留证脚本）：
  - 清除钮三态：无内容隐藏+aria-label ✓ → 有内容显示 ✓ → 点击清空还原全集（0→409）+钮隐藏+焦点在输入框 ✓，Esc 同效 ✓；
  - 对数回归：清空 409 / 爆击 166 / 力量 101 / 组合 45，**页面=库内逐项精确相等**（与运营参照 409/166/101/45 一致，无漂移）；
  - 套装折叠：热区 24×24+描边 2px ✓；折叠 40 张隐藏+另两区块不受影响+筛选保留 ✓；折叠期间改职业筛选→展开=库内该职业 3 套精确匹配 ✓；切赛季往返记忆 ✓；团本收/放全程套装保持收起（互不影响）✓；
- **截图**：`after-chip-row-2x.png`（1366 档 2x 同顾问 A/B 手法）、`after-misc-row-217.png`（217 红基线标注，勾选框左缘贴线）、`after-search-with-clear.png` / `after-search-cleared.png`、`after-tiers-collapsed.png` / `after-tiers-expanded.png`，before- 同位对照同目录；
- **控制台零报错零 404**（逐页 pageerror + 404 响应监听）；`node --check js/dataPublic.js` 通过；
- **npm test 5/5、SEC-001 34/34**；**零测试数据创建**（全只读实测）。

## 三、必须向运营坦白的一点（BUG-054 验证强度）

施工机（headless Chromium + 本机字体栈）上，BUG-054 的「偏上」**未能复现**：几何实测（Range.getBoundingClientRect 文本中点 vs chip 中点）在 line-height:normal 与 1 两种模式下 **delta 均 = 0**（字形本就 dead-center），before/after 截图为**像素级一致**（md5 相同）。即本机环境下该 CSS 改动无可观测差异——`line-height:1` 已按顾问生产 A/B 定稿如实应用（计算样式 12px 已验证），对生产 Chrome/雅黑字体栈的修正效果以顾问 A/B 与运营线上验收为准，本机截图对照只能证明「无回归、无劣化」。

## 四、遗留与说明

1. `.tag` 徽标族观感（副检查附注）：main.css `.tag` 与补丁5 并轨后的 dp-badge/dp-tag/dp-count **本就带 `line-height:1`**（main.css:3971、补丁5 并轨时同步加入），无同类偏上风险，本补丁未动（红线遵守）。
2. BUG-056（label 无障碍 ×78）按任务书排队未动；A1 分诊（CSP/eval 报错=浏览器扩展噪音）已在案不立项。
3. 更新日志未补录：沿用 REQ-081/083 先例（红线不碰主应用）。
4. commit 未做（git 纪律）。验收通过后建议标题：「任务书#23-补丁6：筛选条体验修正（chip光学居中/排除杂项行对齐/搜索清除钮）」——注意任务书给定标题未含套装折叠项，提交时可按实际四维补。
5. 留证脚本 `backup/2026-08-07-task23-patch6/shot-patch6.js` 为一次性工具，非交付物。
