# BUG-070 修改报告：统计报表职业列 1366 档两字折行

> 日期：2026-08-10 ｜ 执行：Kimi Code ｜ 版本串：20260810.40 → **20260810.41**（index.html 10 处 + data.html 6 处全量同步）
> 依据：台账 BUG-070 行 + #27-补丁完工报告遗留申报（docs/TASK-027-补丁-修改报告.md 第八节 2）
> 红线自查：只动显示层（css/main.css 一条规则 + app.js changelog + 双 html 版本串）；未动数据/权限/RLS/server.js/.env/密钥；未 commit 未 push

---

## 一、折行根因

**列宽分配 + white-space 双重成因**：排名表为 auto 布局，1366 档双列网格下单卡内容宽 468px，9 列分完后「职业」列（第 3 列）分得宽度不足「4 字 + 左右 8px 内距」；单元格 white-space 默认 normal，CJK 允许任意断行，遂两字一折（战/士）。非字体、非徽标挤压——「已删除」徽标在角色名列（第 2 列），与职业列无直接关系。

## 二、修改文件清单

| 文件 | 位置 | 改动 |
|---|---|---|
| css/main.css | BUG-058 块后（约 3797-3807 行） | 新增 `#page-reports .stats-rank-table th/td:nth-child(3) { white-space: nowrap; }`——只对本表职业列禁折行，注释含根因与零回退依据（§5：注释无半角 `＊/`） |
| js/app.js | changelogData 顶部 | 补录「修复BUG」条目（BUG-070） |
| index.html / data.html | 版本注释 + 全部 ?v= | 20260810.40 → 20260810.41 |
| docs/问题与需求清单.md | BUG-069 行后 | 补登记 BUG-070 行（此前台账缺该行，编号纪律补齐） |
| scripts/verify-bug070.js | 新建 | 双档生效值断言 + 截图 + T70A 前缀清零复核 |
| scripts/verify-task27-patch.js | 零报错过滤 | 409 纳入过滤（sql/25 执行后 ensureTagNum 碰撞重试的 console 噪音，与 task29 脚本同口径；业务零改动） |

同表其他列排查：出勤/替补/缺席/迟到/请假/出勤率各列内容最长 2-3 字或「100%」，双档实测均无折行无裁切，**无同类问题，不扩面**。

## 三、验证（真浏览器，scripts/verify-bug070.js 10/10）

样本（§4）：T70A 公会 = 「死亡骑士」（4 字职业）成员 T70A死骑甲 + 法师对照 + 硬删成员 T70A将删丙（「已删除」伪行，BUG-058 场景），考勤入出勤率排名表。**样本数：3 成员 / 1 伪行**。

**1366×768（§2 生效值断言）**：
- 职业列 th/td computed `white-space` 全为 **nowrap**（非类名断言）
- 「死亡骑士」单行不折行：td clientHeight=44px（与法师对照行同为单行基准）
- **scrollWidth=468 = clientWidth=468**（与 BUG-058 历史实测逐值一致，零回退）
- 「已删除」徽标 [341,390] 完整落在容器 [282,752] 内，无裁切

**1920×1080**：nowrap 全过；三行职业格均 44px；**scrollWidth=761 = clientWidth=761**（与历史实测一致）；徽标 [387,436] ⊂ 容器 [266,1029]。

截图（已抽看）：`backup/2026-08-10-bug070/reports-1366.png`、`reports-1920.png`。全程零 JS 报错。

附带说明：伪行整行偏高（58px）系角色名列「已删除」徽标换行所致，为 BUG-058 期既有行为、信息完整可见，非本次修复引入，不扩面。

## 四、§1 副作用审计单（唯一约束族 = 宽度/排版，对照 BUG-058 实测值逐项声明）

| 触及约束 | 可能影响的既有行为 | 验证方式 | 结论 |
|---|---|---|---|
| 职业列 nowrap（列内容最小宽从 1 字升至 4 字 ≈ +40px min-content） | **BUG-058 零回退**：本表各列 min-content 总和（≈395px）仍小于 1366 档 468px，auto 布局只重新分配盈余、不撑出横滚 | 1366 档 scrollWidth/clientWidth 实测 **468=468**（与 BUG-058 历史值逐值一致） | 零回退 |
| 同上 | 1920 档不回归 | 1920 档实测 **761=761**（与历史值逐值一致） | 零回退 |
| 同上 | 「已删除」徽标完整可见 | 双档 boundingBox ⊂ 容器断言 | 完整可见 |
| 同上 | 同表其他列受牵连（盈余被职业列多分，名字列/数字列被压） | 双档全表逐格行高实测：数字列/出勤率列单行 44px 无折行；verify-task27-patch 内含 BUG-058 断言 24/24 | 无牵连 |
| 作用域 `#page-reports .stats-rank-table` | 其他页面/其他表不受影响（选择器锁死本表第 3 列） | wp1 8/8 + wp2 27/27 全绿 | 无 |

## 五、回归五组全绿 + 清零复核

| 项 | 结果 |
|---|---|
| verify-bug070（新建） | **10/10** |
| verify-task27-patch（内含 BUG-058 断言） | **24/24**（468=468 / 761=761 复测一致） |
| verify-task27-wp1 | **8/8** |
| verify-task27-wp2 | **27/27**（同名一删一留预期维持补丁2 裁定口径） |
| npm test | **5/5** |
| SEC-001（verify-authz.js） | **34/34** |

**测试数据清零复核**：T70A 前缀 7 项全 0（raid_members/activities/deleted_raid_members/guilds/guild_members/user_profiles/auth 404）；patch 11 项、wp1/wp2、authz 各前缀复核全 0。

## 六、遗留

- 台账 BUG-070 行状态待运营验收后销号。
- 伪行徽标换行使整行偏高（58px）为 BUG-058 期既有观感，报备不擅改，候运营示意。
