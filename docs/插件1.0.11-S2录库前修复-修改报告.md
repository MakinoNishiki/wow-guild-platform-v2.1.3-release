# 插件 1.0.11 · S2 录库前修复包 — 修改报告

> 任务来源：修复清单（终案版，2026-08-13，附两轮 probe 原文）。
> 关联编号：**BUG-082**（新立，冷缓存占位污染）、**REQ-088**（特效/毒咒终案）、**REQ-110③**（毒咒采集）、**REQ-089**（送审件基数订正）、任务书 #47 WP4（A7 写点门禁）。
> git 纪律：运营验收通过前不提交。

## 一、两轮 probe 定论（修复依据）

- 报障样本 271876（觉醒恐牙胸甲）：A 通道（SetItemByID）与 B 通道（GetItemInfo 裸链接 SetHyperlink）全行原样 dump 各 13 行，**两通道均无特效行、无毒咒行**——1.0.10 双通道回退路线证伪，EJ 预览态平面 tooltip 天生缺绿字段落。
- `GetItemStats` 全局函数 12.1 不存在（probe 实锤「函数不存在」），数值 API 通道唯一正主 = `C_Item.GetItemStats`。

## 二、修改文件清单与改动点

### 插件（addon/WoWButlerExporter/）

| 文件 | 改动 |
|---|---|
| `WoWButlerExporter.lua`（1.0.10→**1.0.11**） | ①**冷缓存 P1（BUG-082）**：新增就绪门 `isItemDataReady`（`C_Item.IsItemDataCachedByID`，缺位回退 GetItemInfo 判存）+ `whenItemReady` 延迟重试队列（`Item:ContinueOnItemLoad` 回调驱动，Item mixin 缺位回退 0.5s 延时重查）；`exportInstances` 重构为两阶段——同步纯 EJ 枚举（实例/BOSS/itemID）→ 异步串行解析（逐实例→逐 BOSS→逐件过就绪门），未就绪件绝不解析；`doExport` 段间串行化 + 导出并发锁（`exporting`）。②**特效/毒咒挂难度上下文通道（REQ-088 终案）**：`collectTiers` 内同扫特效/毒咒（与 primary_tiers 同源按档链接），特效按档读、毒咒读史诗档判有，导出 `effect` 存史诗档文本（史诗档非空才覆盖基础通道值）。③`statValuesFromApi` 改 `C_Item.GetItemStats` 优先、旧全局回退。导出字段格式零改动、向后兼容 |
| `WoWButlerExporter_Probe.lua` | ④新增 `/wjdc probe hover` 悬停链接直读模式（`GameTooltip:GetItem()` 取实物链接，H 通道全行 dump + 特效/毒咒判定 + `C_Item.GetItemStats(链接)` 对照）——以后同类取证的标准件；物品级诊断 API 段同步 C_Item.GetItemStats 优先 |
| `WoWButlerExporter.toc` | Version 1.0.11 |
| `README.md` | probe 命令行加 hover；新增「1.0.11 说明」段（冷缓存自动等待/史诗档口径/API 换名，运营向） |
| `运营测试步骤卡.md` | 附（取证专用）改版为 1.0.11：Q1 版本核对、Q2 悬停直读（新标准件）、Q3 物品级诊断保留、Q4 重导判据四条写死（见第四节） |

### Web 侧（A7 门禁核对结论 + 收口）

- **核对结论**：`wclAttendanceWrite`（app.js:4858）**已在 A7 白名单内**——verify-task47 A7 断言点名「fetch(/api/db)=1（wclAttendanceWrite 登记例外）」，零动作。`syncActivity` 内考勤批量插入（cloud.js 原 986 行）为 REQ-033 时代直 fetch /api/db 既存写点，**不在 A7 计数面**（A7 只 grep app.js）——按清单纳入收口。
- `js/cloud.js`：syncActivity 批量插入改走 `dbInsert` wrapper（数组 body 由 dbWrite 原样透传），保留「出勤记录同步失败：」中文错误前缀；行为零变化。
- `scripts/verify-task47.js`：A7b 扩锁——cloud.js 裸 `fetch('/api/db` 字面量锁 0，超限即红。
- `index.html` / `data.html`：版本串 20260811.59→**.60**（两壳 11+7 处全量替换，含顶部注释）。
- `js/app.js`：changelogData 顶部新增 1.0.11 条目（fix 类，含 REQ-089 订正与 A7b 收口明细）。
- `docs/开发规范.md` §1.2.5、`AGENTS.md` 数据同步机制段：A7 锁数描述同步 A7b 口径。

### REQ-089 送审件

- `docs/REQ-089-兑换物展开规则表-送审.md` 第七节：兑换物基数 **5 件→20 件**（毒咒/毒织/毒铸/毒锻 4 族 × 5 部位；早前「5 件」备案仅覆盖毒咒一族，已注明取代关系）；type 不可依赖条补记 BUG-082 毒锻污染案；影响评估同步按 20 件重算。**未触碰** 2.1/2.2 规则格与任何基线数字（送审状态不变）。

### 台账

- `docs/问题与需求清单.md`：新立 **BUG-082**（P1，已修复待真机回验）；REQ-088 行追记 1.0.10 双通道证伪与 1.0.11 终案；REQ-089 行追记基数订正。

## 三、验证方式（已完成）

- Lua 两文件：自研块配对检查器（剥离注释/字符串后校验 function/if/for/while/do/repeat…end/until 配对）双双通过（594/346 行）；改动段逐行复读复核（闭包捕获、EJ 全局状态单线推进、guard 语义延续）。
- `node --check`：js/cloud.js、js/app.js、scripts/verify-task47.js 全过。
- A7 口径复核（node 实跑 grep 计数）：app.js saveCloudData=16 / fetch('/api/db')=1 / dbOps=0 ✅；cloud.js 裸 fetch=0 ✅（收口中途曾被我自己的注释字面量误触发一次，已改写注释措辞规避门禁模式——门禁字面量敏感度记录在案）。
- 版本串：两壳单一串 20260811.60，verify-task47 A1 断言口径（/20260811\.\d+/ 且 ≥.59）兼容。

## 四、遗留问题（待运营/真机）

1. **真机重导回验（S2 录库前硬门禁）**，判据四条：
   - 271876 `effect=「装备：你的法术和技能有几率使你的爆击提高377，但会使你的其他次要属性降低62，持续12秒。」`、`venomcurse=毒咒`；
   - 后二 BOSS 同类 3 件同判；
   - 此前 31 件缺损件主/副属性补齐；
   - 3 件毒锻兑换物 `type` 归位（不再错标武器）。
   步骤卡 Q1-Q4 已按 1.0.11 更新。
2. **大秘境段无四难度档通道**（钥石层数缩放，通道不存在）：M+ 池装备的特效/毒咒仍走平面双通道，预览态缺行病害对 M+ 无难度档链接可解——若 S2 实采发现 M+ 侧同类缺损，需另案（候选：悬停实物补采/OCR 管道）。
3. `C_Item.GetItemStats` 真机返回形态未经实证（probe hover 已内置对照输出，首轮重导顺手取证）。
4. 版本串 .60 起 web 侧仅 cloud.js 内部收口 + changelog，无界面变化；验收可走轻量 B 表（WCL 同步考勤主链路一次 + verify-task47 全量）。
