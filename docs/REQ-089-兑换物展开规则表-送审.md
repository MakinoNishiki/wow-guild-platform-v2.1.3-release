# REQ-089 套装兑换物展开规则表（送审草案）

> 状态：**送审稿，待运营逐格确认**。本文件只出规则表，不含任何施工内容；
> 未经运营确认新基线前，**禁止施工展开、禁止改动任何断言/基线数字**。
> 关联：任务书 #46 侦察结论；数据源口径见本文第四节。

## 一、现状

- `boss_loot` 中套装兑换物**仅 1 行**：`slot='套装兑换物'`、`item_type='套装兑换物'`（杂项族现存值，见 `js/lootTaxonomy.js:53`）。
- 该兑换物行在副本掉落公示页以「可兑换本赛季套装」独立分类展示（`js/dataPublic.js:602`），
  心愿单/装备分配 picker 恒定排除（`js/lootTaxonomy.js:30`）。
- 问题：兑换物本体不是装备，用户无法从公示页直接看到「它能换成什么」——
  需要把 1 行兑换物**展开**为 13 职业 × 专精 × 部位的真实套装装备行。

## 二、展开规则草案表（待运营逐格确认）

### 2.1 展开主规则（兑换物 → 套装装备行）

| 规则格 | 草案口径 | 待确认点 |
|---|---|---|
| 展开维度 | 1 行兑换物 → 13 职业 × 各职业专精（`game_specs`，2~4 个/职业）× 套装部位 | ① 是否按专精分行（同名套装件多专精共用时是否合并为一行） |
| 部位集合 | 套装五件：头部 / 肩部 / 胸部 / 手部 / 腿部（部位词表以 `js/lootTaxonomy.js` 部位词表为准） | ② S1 套装是否恰为五件、部位名称逐字确认 |
| 归位 | 展开行 `boss_id` = 兑换物原行所在 BOSS（归位原 BOSS，不新建 BOSS/实例） | ③ 确认归位原 BOSS 而非「兑换商」类虚拟条目 |
| 甲型 | 按职业定甲型：板甲=战士/圣骑士/死亡骑士；锁甲=猎人/萨满祭司/唤魔师；皮甲=潜行者/武僧/德鲁伊/恶魔猎手；布甲=牧师/法师/术士 | ④ 13 职业逐格核对甲型 |

### 2.2 展开行字段形态（对齐 `boss_loot` 列）

| 字段 | 草案口径 | 来源 | 待确认点 |
|---|---|---|---|
| `item_name` | 套装件名（`tier_sets.set_name` + 部位词拼接，如「星界织法者法衣 · 头冠」形态） | `tier_sets.set_name` + 部位词表 | ⑤ 命名模板逐字确认（连接符/是否带部位后缀） |
| `slot` | 部位（头部/肩部/胸部/手部/腿部） | 部位词表 | 同 ② |
| `item_type` | 甲型（板甲/锁甲/皮甲/布甲，按 2.1④） | 职业→甲型映射 | 同 ④ |
| `primary_stats` / `secondary_stats` | 按专精推导：主属性=职业主属性（力量/敏捷/智力）；副属性=该专精套装件实际副属性 | `game_classes` / `game_specs` + 顾问侧套装数据 | ⑥ 副属性数据源待指定（游戏内实测 / 顾问侧 OCR 管道），未指定前不得编造 |
| `primary_values` / `secondary_values` / `primary_tiers` / `secondary_tiers` | 数值待采集端补采后回填；首版可留 NULL | 插件 1.0.9 采集链 | ⑦ 首版是否接受数值留空 |
| `effect` | 留 NULL（套装效果走 `tier_sets.bonus_2/bonus_4`，不复制进 effect） | `tier_sets` | ⑧ 确认不复制套装效果文本 |
| `official_item_id` | 套装件真实 itemID 待采集补登；首版留 NULL | 插件采集 | ⑨ 首版是否接受留空 |
| `venomcurse` / `icon_id` | 留 NULL（sql/26 / sql/29 口径，待采集回填） | — | ⑩ 确认 |
| `note` | 可注「由套装兑换物展开（REQ-089）」溯源 | 本规则 | ⑪ 溯源文案逐字确认 |
| 原兑换物行 | 展开后**保留**原 1 行兑换物（公示页分类逻辑依赖 slot='套装兑换物'） | — | ⑫ 确认保留而非替换 |

## 三、13 职业 × 专精展开基数（草案）

- 职业清单与专精数以 `game_classes` / `game_specs` 字典表现为准（13 职业，每职业 2~4 专精）。
- 理论展开行数上界 ≈ Σ（各职业专精数） × 5 部位；若 2.1① 裁定为同名件合并，则上界 ≈ 13 × 5 = 65 行。
- **具体逐职业展开清单待 ①⑤ 确认后由施工方生成附表，不在本送审件展开。**

## 四、数据源

| 数据 | 来源表/文件 |
|---|---|
| 职业清单 / 甲型映射 | `game_classes`（13 职业，sql/10） |
| 专精清单 | `game_specs`（class_id + spec_key，sql/10） |
| 套装名 / 2件/4件效果 | `tier_sets.set_name` / `bonus_2` / `bonus_4`（season_id 限定当前赛季，sql/10） |
| 部位词表 | `js/lootTaxonomy.js` 部位词表（DC_SLOT_OPTIONS 19 项，任务书 #40） |
| 兑换物原行 | `boss_loot` slot='套装兑换物' 1 行（现状唯一） |

## 五、基线影响红线（必须运营确认后才施工）

- 展开将**增量 `boss_loot` 行**（上界约 65 行，见第三节），现公开复核基线
  **全部 308 / 团本 104 / 大秘境 204** 与库内 **boss_loot 190 / dungeon_loot 221** 必然改变。
- 纪律：运营逐格确认本规则表并**拍板新基线数字**后，方可施工展开并同步更新
  各 SQL 头部复核口径与断言；**禁止静默改基线**。
- 公示页过滤口径（`slot<>'杂项'`、`item_type not in('装饰品','幻化')`、`gr.type is distinct from 'world'`）
  对展开行天然放行（正常装备行），无需改 RPC；展开行进场后公示页行数随基线同步变化属预期。

## 六、确认签核栏

| 待确认点 | 运营裁定 | 签字/日期 |
|---|---|---|
| ①~⑫（见 2.1/2.2 各格） | 待填 | 待填 |
| 新基线数字（全部/团本/大秘境 + 库内两表行数） | 待填 | 待填 |

## 七、附：备案情报（2026-08-13 运营供，仅备案不动手；同日二次订正）

- **兑换物正体（二次订正）**：S2 实为 **20 件**——毒咒/毒织/毒铸/毒锻 **4 族 × 5 部位**
  （护手/头/肩/胸/腿）；早前备案「5 件（毒咒神像/残骸/圣像/遗物/雕像）」仅覆盖毒咒一族，
  本条取代之。与规则表「1 行兑换物」的现状记录不同（S1 旧档），展开施工时以本情报的
  20 件为准重新盘点 BOSS 归位。
- **插件导出该 20 件 `type` 不可依赖**：EJ 子类型口径现标「垃圾」，且 1.0.9 冷缓存污染曾致
  3 件毒锻兑换物 type 错标「武器」（BUG-082，插件 1.0.11 已修）；展开映射表需按名称
  白名单识别兑换物，不能依赖 type。
- 影响评估：规则表 2.1 部位集合/归位格（②③）与展开基数（第三节）届时按 20 件兑换物
  重算；本备案不改变送审状态。
