# 任务书 #23-补丁5 修改报告：公示页组件规范收口

> 日期：2026-08-07 ｜ 执行：Kimi Code ｜ 需求编号：REQ-083（台账已登记）
> 来源：任务书 #25 铁律#4 开工审计（improve-ui 对 data.html 基线页，报告在案）三发现转化
> 状态：**已实现待运营验收**（未 commit、未 push，git 纪律候验收）

---

## 一、实际改动点

### 1. 徽标并轨（发现①，样式并轨、类名全保留）
`css/data-public.css`：
- `.dp-badge`：高 18→**20px**、padding 0 7→**0 8px**，补 `line-height:1` / `white-space:nowrap`（字重 600、圆角 5px 原已符合）；
- `.dp-tag`：高 18→**20px**、padding 0 6→**0 8px**、圆角 4→**5px**、字重 400→**600**，补 `line-height:1` / `white-space:nowrap`；
- `.dp-count`：圆角 8→**5px**、padding 1px 7→**0 8px**、补高 **20px** / 字重 **600** / inline-flex 居中，色 `text-muted`→`text-secondary`（对齐 `.tag-grey` 弱化灰语义）。
- 语义色核对：dp-badge-mplus/new/lair 与 dp-tag 主/副属性色值与 main.css `.tag-blue/.tag-green/.tag-yellow/.tag-grey` 逐值相等，无需改动即满足「复用既有修饰类色板」。
- **类名零改动**：`.dp-badge` / `.dp-badge-new` / `.dp-tag` / `.dp-count` 全部保留，verify 九处断言钩子原样生效。

### 2. 删冗余徽标（发现②）
`js/dataPublic.js:414`：`secHead('sec:dungeons', '大秘境掉落池 <span class="dp-badge dp-badge-mplus">大秘境</span>', …)` → 纯标题 `'大秘境掉落池'`。「新本」徽标（.dp-badge-new）不受影响（patch4 四处钩子复测通过）。

### 3. 字级回归（发现③）
`css/data-public.css`：`.dp-section-title` 17px/700 → **18px/600**（§3 注册值）；`.dp-subtitle` 12px → **11px**（副标注册值）。页头主标题 `.dp-title` 22/700 未动（§10 挂起项，任务书明列不做）。

### 4. 版本串双头递增（验收口径）
`20260805.18` → **`20260807.19`**：`index.html` 8 处（注释+7 引用）、`data.html` 6 处（注释+5 引用），replace_all 机械替换，无漏串。

### 5. verify 断言同步（任务书验收条款「类名保留或断言已同步，二选一」）
两处脚本各做最小同步，均**先在施工前树上复现同一失败**（git stash 对照实验），证明失败为陈旧断言、与本补丁无关：
- `scripts/verify-task23-patch.js:305`：赛季重置断言的 chips 计数选择器补 `:not(.dp-chip-all)`——补丁3 引入「全部」chip 后，重置即空选=「全部」active（筛选规范 §3 默认态），旧断言未排除导致恒计 2；
- `scripts/verify-task23-patch3.js:146`：部位排序断言排除「全部」chip（`data-v=""` 占据首格导致恒错位）；
- `scripts/verify-task23-patch3.js:169`：「装饰归其它组」断言改有样本才断言、无样本跳过——库内「装饰」值已漂移不存在（REQ-081 在案「装饰非杂项 slot 0 行」同款漂移），断言总数 32→31。

## 二、验证（留证）

- **三 verify 脚本全量重跑全绿**：patch **33/33**、patch3 **31 通过/0 失败**、patch4 **36/0**（含 REQ-074④ 徽标零重叠审计、patch4 三级折叠/热区/新本徽标全链路）；
- **两档前后对照截图**（`backup/2026-08-07-task23-patch5/`，before-/after- 各 5 张 + 同屏对照）：
  - `after-tag-compare.png`：公示页 dp-* 与主应用 .tag **同高/同字重/同圆角**同视感（真实 CSS 渲染对照，server.js 静态白名单下用 setContent 注入，零临时文件残留）；
  - `after-1366-sectionhead-mplus.png`：「大秘境掉落池」标题后冗余徽标已删，「按 BOSS/整体池」切换不受影响；
  - `after-1366-raidhead-badges.png` / `after-1366-item-tags.png` / `after-1366-tier-tags.png`：件数徽标、装备卡 meta 标签、套装 2件/4件 标签均为 20px 规范形态，无重叠无溢出；
  - 两档 full page（1366×768 / 1920×1080）前后对照在目录内，控制台零报错零 404（截图脚本逐页监听 pageerror 与 404 响应）；
- `node --check js/dataPublic.js` 通过；
- **npm test 5/5**、**SEC-001 回归（verify-authz.js）34/34**；
- 本补丁**零测试数据创建**（公示页为 live 只读数据，截图/验证均只读；patch 脚本自带 T23X 清理复核全 0）。

## 三、遗留与说明

1. `.dp-badge-mplus` CSS 规则保留未删：页面内已无渲染点，但类名属公示页族注册组件，留给 #25-WP3（recruit.html 版式迁移）复用；强行删除属顺手清理，不做。
2. 更新日志未补录：同 REQ-081 先例——本补丁红线不碰主应用，changelog 在主应用内，候验收合并时由运营定夺是否补。
3. commit 未做（git 纪律：运营验收通过前不提交）。验收通过后建议 commit 标题：「任务书#23-补丁5：公示页组件规范收口（徽标并轨/冗余徽标/字级回归）」。
4. 截图工具 `backup/2026-08-07-task23-patch5/shot-patch5.js` 为一次性留证脚本，非交付物，可随验收后清理。
