# 插件 1.0.15 · D6 收尾哨兵红字 — 修改报告

> 任务来源：顾问终审裁定——D 组剩余代码活唯一 = D6 收尾哨兵红字。
> 关联编号：**BUG-089**（D1/D2 栈溢合案 + D6 谎报案，本包落地其收尾半）。
> git 纪律：不单独提，工作树叠 1.0.12+13+14+15 全量未提交改动，待顾问出合成 commit 物料一次入仓。

## 一、施工约束三条执行回执

| 约束 | 执行 |
|---|---|
| ①只加看门狗计时器+红字提示 | 全部新增逻辑 = `watchdog`/`beat`/`lastBeat`/`watchdogArmed` 四名目 + 红字一条（文案同 1.0.14 诚实化口径：「导出链已超过 300 秒无进展，判定链中断——已扫数据已按段落表不丢，请 /reload 后重跑」） |
| ②零触数据路径 | **grep 实证**：watchdog/beat/lastBeat/watchdogArmed 全部命中行 = 主文件 822-886 行，**完全落在 doExport 函数体内**；exportInstances（~702 行止）/collectTiers/whenItemReady/1.0.9 解析路径（getLootItemID/getItemBasics/parseItemDetail/scanLink）**零行改动**。心跳打点只落在 doExport 自有回调闭包：段完成 done ×2、1.0.13 onProgress 落表 ×2、段中断 pcall 分支 ×1 |
| ③不做超时自解锁 | 哨兵只报红字，不碰 `exporting` 锁；/reload 兜底，步骤卡 Q1 已写明（1.0.14 批已落） |

## 二、哨兵语义（设计一句话）

每 30s 自检一次：距上次心跳 ≥300s 判链死 → 红字提示一次并重武装（链可能只是慢，复活后再死能再报，但不刷屏）；正常跑完终局分支解除（`watchdogArmed=false`），挂出的末次定时器空转一次即终。心跳源 = 实例收尾落表（onProgress）与段完成/段中断——正常跑每个实例必有心跳，300s 阈值对病态冷缓存单实例（~150s 量级）留足一倍余量，误报面近乎零。

## 三、同步件

- `WoWButlerExporter.lua`：doExport 内哨兵 + 文首 changelog 1.0.15 条目 + `ADDON_VERSION="1.0.15"`。
- `WoWButlerExporter.toc`：Version 1.0.15。
- `README.md`：「1.0.15 说明（断链哨兵）」段（运营向）。
- `运营测试步骤卡.md`：Q1 版本 1.0.15 + 哨兵红字预期说明（顺带更正一处事实：段提交不丢数据为「1.0.13 起」，原文误写 1.0.14）。
- 台账 `docs/问题与需求清单.md`：BUG-089 行追记 D6 收尾半落地 + D3（SecureTypes）残差仍待取证。

## 四、验证方式（已完成）

- Lua 块配对自检（重建一次性检查器，用后已删）：主文件 opens=181 closes=181 **PASS**、Probe 73=73 **PASS**（Probe 零改动）。
- 零触数据路径 grep 实证（见上表②行号清单）。
- 语义走查：`beat()` 先于 segs 定义闭包捕获成立；`/wjdc me`/`tier`/ejAvailable 失败三条早退路径均在哨兵武装之前返回，不会挂空狗；段中断分支 beat() 防「跳段续跑期」误报。
- 如实声明：无 Lua 真机环境，未本机运行；哨兵触发/误报面只能真机验证（跑 C 强制中断场景可顺带观察：中断后 300s 内应见哨兵红字一次）。

## 五、遗留

无新增。D 组代码活清零，等合成 commit 物料 → 真机一次验收会话（跑 A→B→可选 C，V1-V13 全判）。
