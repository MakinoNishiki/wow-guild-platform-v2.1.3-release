# 任务书 #23-补丁7 修改报告：筛选条左缘对齐收口（组头左对齐 + misc 行回左线）

> 日期：2026-08-07 ｜ 执行：Kimi Code ｜ 需求编号：**BUG-055（复开，修法变更）**
> 状态：**已实现待运营验收**（未 commit、未 push，git 纪律候验收；规范两份已于 commit 51b5ce5 同修入库，施工以新条款为准）

---

## 一、实际改动点（红线内：只动 data-public.css + verify 断言 + 版本串）

### 1. 组头左对齐 — `css/data-public.css`
`.dp-chip-label` `text-align: right` → **`left`**。56px 固定盒宽、12px 次级色、24px 行高与首行 chip 同基线等其余规格不动——四标签（部位/类型/主属性/副属性）文本左缘同线。注释同步改为左对齐 + 左缘同线条款。

### 2. misc 行回左线 — `css/data-public.css`
删除补丁6 的 `.dp-misc-row { padding-left: 64px; box-sizing: border-box; }` 整条规则（64 = 组头 56 + 组间 8 的缩进修法撤销），注释改写为补丁7 定稿口径（misc 行零缩进、勾选框左缘与组头盒/搜索框行同一条竖线）。开关默认关、问号逐字文案、label 结构、交互一律未动（BUG-056 红线遵守）。

### 3. 不动项核验
搜索框行本就在左线（153）未动；chip 列 217 基线未动；行内子类组标（护甲/武器/首饰等）样式与对齐未动；chip 几何规格（24px 高/12px 圆角/12px/500/line-height:1）未动。

### 4. verify 断言同步 — `scripts/verify-task23-patch4.js`
- 组头断言 `textAlign==='right'` → `'left'`，断言文案同步「56px 左对齐」；
- 补丁6 的 misc 217 基线断言同步改为**左缘同线断言**，新增 3 条：
  - 组头四标签左缘同一条竖线（left 两两差 ≤1px）；
  - misc 行 `padding-left === '0px'` 且勾选框/搜索框左缘与组头盒同线（≤1px）；
  - chip 列 217 基线不动（1366 档回归，≤1px）；
- 其余断言零改动（patch4 总数 36 → 39）。

### 5. 版本串双头递增
`20260807.20` → **`20260807.21`**：index.html 8 处、data.html 6 处（replace_all 机械替换）。

## 二、验证（留证 `backup/2026-08-07-task23-patch7/`）

- **几何实测**（shot-patch7.js，headless Chromium 2x）：
  - 1366×768：组头四标签 left = **153/153/153/153**，misc 勾选框 = **153**，搜索框 = **153**，misc 行 paddingLeft = 0px，chip 列 = **217 不动**；
  - 1920×1080：四标签/勾选框/搜索框 left = **430** 同线，chip 列 = 494；
- **两档红线标注截图**：`1366-filterbar-leftline.png` / `1920-filterbar-leftline.png`（红线①=左缘竖线 153/430，红线②=chip 列基线 217/494），已目检：组头四标签左缘一条竖线、misc 勾选框在同线、chip 列不动；
- **三 verify 全量重跑全绿**：patch **33/33**、patch3 **31/0**、patch4 **39/0**（含新增 3 条左缘断言全过；三级折叠/热区/赛季重置等零回归）；
- **对数回归**：爆击 **166** / 力量 **101** / 组合 **45** / 清空 **409**，页面=库内按行精确相等（patch4 脚本附注：库内较运营参照 411/167/101/45 已有漂移，以库内实测为准）；
- **控制台零报错零 404**（三脚本逐页 pageerror + 404 监听）；
- `node --check`：verify-task23-patch4.js、shot-patch7.js、js/dataPublic.js（未改动）全过；
- **npm test 5/5、SEC-001 34/34**；**零测试数据创建**（全只读实测，T23X 清零复核 = 0）。

## 三、遗留与说明

1. 规范两份（魔兽管家UI设计规范v2.md §4.3、公示页筛选系统设计规范 §2/§3）已先行同修入库（commit 51b5ce5），本次施工与之逐字一致，无文档欠账。
2. 补丁6 的 misc 217 断言原在其一次性留证脚本中，未入常驻 verify；本次按任务书口径在 verify-task23-patch4.js 落为 3 条常驻断言（左缘同线 ×2 + 217 回归 ×1）。
3. 更新日志未补录：沿用 REQ-081/083/补丁6 先例（红线不碰主应用）。
4. 留证脚本 `backup/2026-08-07-task23-patch7/shot-patch7.js` 为一次性工具，非交付物。
5. commit 未做（git 纪律）。验收通过后建议标题：「任务书#23-补丁7：筛选条左缘对齐收口（组头左对齐+misc回左线+规范同修）」。
