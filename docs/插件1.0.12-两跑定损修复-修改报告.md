# 插件 1.0.12 · 两跑实测定损修复包 — 修改报告

> 任务来源：顾问一轮整合回复（run2 人证+算术实证定案 BUG-086）+ 顾问终审回执（修法送审件签字放行，docs/插件1.0.12-修法送审件.md）。
> 关联编号：**BUG-083**（熔断误伤）、**BUG-084**（混报）、**BUG-085**（部分带档）、**BUG-086**（跨跑态污染）、**BUG-087**（伪实例过滤）、**REQ-118**（大米段档通道）、**REQ-119**（导出可观测性三件套）。
> git 纪律：运营验收通过前不提交。**本包未 commit 未 push**；当前仓库 HEAD=b00aa40（任务书 #47），1.0.11 提交 1a894dc 悬置本地（HEAD 之子、无分支指向），工作树 = 1.0.11 + 1.0.12 全量未提交改动，待运营合成 commit 一起 push。

## 一、定案依据（顾问一轮整合，在案）

- run2 团本 0 件 = 假设二坐实：run1 以 mythic(16) 难度残留收场（origDiff=nil 不还原之洞），同会话 run2 枚举跑在余态难度上——团本无该档掉落表全灭、大米密谋小径 16→31 / 红玉新生法池 7→21 定向 +29（余六本一致）。假设一（同步前缀异常中断）由人证排除（run2 无红字全黄字）。
- 编号更正已落实：REQ-116/117 撞号改 **REQ-118/REQ-119**（送审件、台账、步骤卡、本报告全部按新号）；BUG-083~087 口径以台账为准（086=跨跑态污染）。

## 二、修改文件清单与改动点

### 插件（addon/WoWButlerExporter/）

| 文件 | 改动 |
|---|---|
| `WoWButlerExporter.lua`（1.0.11→**1.0.12**，593→786 行） | 见下逐函数明细 |
| `WoWButlerExporter.toc` | Version 1.0.12；`## SavedVariables: WJDCDump, WJDCLastCounts`（REQ-119② 计数对比存储） |
| `WoWButlerExporter_Probe.lua` | **零改动**（1.0.12 不触碰诊断模块） |
| `README.md` | 新增「1.0.12 说明」段（连跑一致/逐件隔离/大米档通道/伪实例过滤/观测日志，运营向）；FAQ 红字文案同步新措辞 |
| `运营测试步骤卡.md` | 附节改版为 1.0.12：Q1 版本核对、Q2/Q3 保留、**Q4 = 同会话连跑两次验收跑 + V1-V9 签字判据表逐条 ✅/❌**；主链路第 4/6 步口径同步（大米段档通道提示、大米产 tiers、meta 新字段） |

### `WoWButlerExporter.lua` 逐函数明细

- **文首 changelog 块**：加 1.0.12 条目（七项全述）；`ADDON_VERSION = "1.0.12"`。
- **`buildMeta`**：加 `run_id = date("%Y%m%d-%H%M%S")`（REQ-119③）。
- **新常量**：`DUNGEON_TIERS`（normal=1/heroic=2/mythic=23/timewalking=24，REQ-118）、`PSEUDO_INSTANCE_NAMES = { ["史诗钥石地下城"] = true }`（BUG-087）。
- **`collectTiers(loot, tiers, opts)` 重写**：返回逐件逐路径计数 stats（tiered/scanned/noLink/idMismatch/scanFail/noValue/switchFail/tierSkip）；**整 BOSS tiered=0 判死删除**（BUG-083），软失败不再 stripTiers 销毁已采部分档值；档位表参数化（团本 RAID_TIERS/大米 DUNGEON_TIERS）；`opts.skipBadTier`（大米单档读回失败跳过续扫）/团本切档失败记 switchFail 终止余档但保留已采；特效读 `opts.effectTier` 档、`opts.venomcurse=true` 才扫毒咒（M+ 免）。
- **`whenItemReady`**：回调体 pcall 包裹（guarded()，抛错红字具名+链继续，BUG-086 防链死锁死）。
- **新增 `whenAllReady(loot, fn(notReady))`**（BUG-085）：档扫前全件 RequestLoad 预热+逐件 whenItemReady 确认，仍不就绪计 notReady 单列（不混入 noValue）。
- **`exportInstances(isRaid, label, tierOn, done, abortFlag)` 重构**：
  - ①BUG-086 主洞：`origDiff` **段首无条件捕获**（含不切档的大米段）、`finalize()` 必经还原——`origDiff=nil` 归位普通档 14 并黄字明示；finalized 幂等。
  - ②REQ-119①：阶段一每实例 pcall 包裹 + 枚举前显式钉档（团本锚 14/大米锚 23）+ 回读校验 + 难度档快照黄字（读到什么印什么，钉档失败带注记）。
  - ③BUG-087 双判：伪实例名表过滤 + 全 BOSS 零掉落空实例跳过（黄字）。
  - ④阶段二 `EJ_SelectInstance/SelectEncounter` 补 pcall（失败红字跳 BOSS）；BOSS 收尾 failed 红字文案更新。
  - ⑤档扫整段包 whenAllReady；**熔断器整删**（softFail→bossNoTier 纯计数，段尾汇总黄字，不再影响任何 BOSS 的切档尝试）；分案报错三文案（idMismatch 守卫拦截/noLink 稀疏表缺字段/0 档值含四计数，BUG-084）；硬失败（pcall 炸）保留 stripTiers 但先报已带档件数留证。
  - ⑥`aborted()` 检查在 nextInstance/nextBoss/stepItem/whenAllReady 回调入口（段 abort 后旧链醒来即退出，防双推进）。
- **`doExport`**：run 开始/结束黄字（run_id+时间戳，REQ-119③）；大米段 dungeonTierOn 探测 + `meta.dungeon_tier_channel`（REQ-118）；segs 各挂 abortFlag；段级 pcall 失败置 `abortFlag.aborted=true` + 红字（段名+原因+恢复建议），其余段不受影响；终局 REQ-119② 计数对比（`WJDCLastCounts` 全局 SavedVariable，不一致黄字 diff 后覆写）。

### 1.0.9 解析路径核查（放行基石确认）

`getLootItemID` / `getItemBasics` / `parseItemDetail` / `scanLink` **零触及**（grep 比对在案），1.0.9 平面单档路径作为大米档通道不可用时的回退原样保留。施工中未发现非动不可之处，无停工申报事项。

### 台账

- `docs/问题与需求清单.md`：BUG-083/084/085/086/087 + REQ-118/119 共 7 行状态 🔶→**🔧 已实现待真机回验（插件 1.0.12）**，前态与定案补注全文保留。

## 三、与送审件出入申报（纪律：沉默=零偏差，以下为全部出入）

1. **BOSS 档扫完成黄字文案参数化**：「四档重扫完成（N/M 件带档）」→「档重扫完成（N/M 件带档，跳过档=…）（缺档：noLink=… 错位=… 未就绪=… 无静态值=…）」——大米段非四档，文案去「四」字通用化，BUG-085 逐件计数并入同一行（送审件③的落点即此行，文案字面为实施微调）。步骤卡第 4 步已同步。
2. **failed 红字文案更新**：「N 件物品未缓存记 failed」→「N 件物品解析失败记 failed（1.0.12 就绪门重试后仍失败，导出链继续）」——就绪门时代「未缓存」措辞已不准确。README FAQ 与步骤卡第 4 步已同步。
3. **「实例名+中断原因+恢复建议」红字要素的落点**：实例枚举中断红字（实例级 pcall）含实例名+原因+恢复建议；段级中断红字含段名+原因+恢复建议（段级拿不到具体实例名，两级组合覆盖顾问对账②要求的三要素）。
4. **空实例跳过用黄字不用红字**（BUG-087 第二判）：空实例多为口径性（赛季未开/伪内容），红字会制造恐慌；伪实例名表过滤同样黄字。若顾问要求升红字，一行改动。
5. **大米段同样做段首难度捕获/finalize 还原**：送审件①原文「含不切档的大米段」已明示，照做无偏差，重申在案。
6. 除以上 5 条外，与送审件零偏差：动过的函数仅 exportInstances/collectTiers/doExport/whenItemReady（+新增 whenAllReady/两常量），与放行范围一致。

## 四、验证方式（已完成）

- **Lua 块配对自检**：自研检查器（剥离注释/字符串后 function/if/for/while/do/repeat…end/until 配对计数）——主文件 opens=160 closes=160 **PASS**、Probe opens=73 closes=73 **PASS**（检查器用后已删，backup/_pytmp/ 零残留）。
- **语义走查复核**（完工后全文复读 236-786 行）：`notReady`（whenAllReady 回调参）/`tierSet`/`tierOpts`/`anchorDiff`/`finalize`/`aborted` 作用域全部闭环；RAID_TIERS mythic key 与 effectTier 对齐；abortFlag 段级挂接与回调入口检查逐点核过。
- **残留 grep**：tierAlive/softFail/「不再切档」代码面零残留（仅 changelog 注释与文档提及）。
- **如实声明**：Code 侧无 Lua 解释器/无游戏客户端，**无法本机运行预验**——交付前验证=块配对自检+语义走查，**首次真机跑即验收跑**（步骤卡 Q4 已要求现场全留：两跑聊天框完整截图+导出文件）。

## 五、遗留问题（待运营/真机，V1-V9 全走真机）

1. **V1-V9 验收跑**（步骤卡 Q4 已写死，逐条 ✅/❌）：V1 同会话双跑计数一致（团本 135±/大米 225±）；V2 大米 28+4 件逐一销账（ID 清单在送审件附录，去向表顾问侧出）；V3 271876 effect 全文+venomcurse=毒咒；V4 后二必扫到档无跨 BOSS 截断；V5 31 件补齐+毒锻 3 件 type 归位；V6 M+ 饰品 effect 通过率目标 24/24；V7 伪实例不出现；V8 三条观测日志可见；V9 converter 转换行数=导出行数零回归。
2. **converter 未动**：大米段新增 tiers 字段为纯增量（原字段格式零改动），converter 若按固定字段读取则天然兼容——V9 实证，若不兼容另案。
3. **web 侧零改动**：本包纯插件，无版本串递增、无 changelogData 条目（1.0.11 有 web 条目是因含 A7b/REQ-089 web 改动）；若顾问要求平台更新日志补一条插件 1.0.12 说明，一句话可补。
4. `WJDCLastCounts` 为新增 SavedVariable——旧 toc 残留的 SavedVariables 写盘文件不受影响，首次 1.0.12 导出后才开始有对比基线（首跑无对比黄字属预期）。
