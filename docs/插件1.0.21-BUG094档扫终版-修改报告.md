# 插件 1.0.21 修改报告 —— BUG-094 二次翻转终案：按实例自适应有效档探测 + 无难度维单趟

- 日期：2026-08-14（会话日期）
- 基线：插件 1.0.20（工作区，未 commit）
- 范围：`addon/WoWButlerExporter/`（主文件+toc+README+步骤卡）+ `scripts/validate-export.js` + 台账
- 性质：回执批复三点落地——**②D3（SecureTypes）销案**（台账记功，代码零触）；**③094 二次翻转终案**（旧「LFR 未开」定性作废，档扫改按实例自适应有效档探测）；**⑤冒烟目标调整**（团本目标改烈毒之渊·盘魂者内克扎莉，至暗之夜移出冒烟网留全量验）。①REQ-121 二批验收通过、④工具件已入仓（ff79ed8+85467a6）为确认项，无动作。

---

## 一、094 终案定性回执（运营截图实证 + FrameXML 注释定档）

| 终案要点 | 对插件的直接后果 |
|---|---|
| 至暗之夜=资料片名借用的**世界首领整合类目**，无难度维（难度下拉不存在） | 四档轮扫对它无对象——旧设计必然 0 带档/判空；1.0.21 识别「零有效档」单趟基础采集，tiers 记 normal 单档（与 1.0.15 数据形态连续，converter 零改） |
| 唯三真实例有下拉：毒牙祭坛=普通/英雄/史诗、潮缚石窟=世界/普通/英雄/史诗、烈毒之渊=随机/普通/英雄/史诗 | 「哪个实例有哪些档」不再能一张定档表打天下——改按实例逐个探测 |
| 「世界」档=FrameXML `"World" -> PrimaryRaidLFR`（行为同 LFR、显示为世界、暴雪下拉实际 `SetDifficulty(17)`）；DifficultyUtil 数值：团本 17/14/15/16=LFR/普通/英雄/史诗、大米 1/2/23/24、RaidWorld=250 | 现有候选 17/14/15/16+1/2/23/24 恰好覆盖全部真实例档位集合，**候选表零改**，只改「逐实例筛有效档」 |
| 1.0.15 旧文件交叉验证：至暗件仅 normal 单档，一致 | 终案形态与历史好数据连续，验证判据=4 首领 8±2 件+无难度维注记+tiers 仅 normal 单档 |

## 二、档扫终版设计落实（替换定档表，吸收「按档跳过」小修）

| 指令 | 落实 | 位置（主文件） |
|---|---|---|
| 钉档前逐候选过 `EJ_IsValidInstanceDifficulty`，只扫有效档 | `enumInstance` 阶段一新增逐实例探测：`tierOn` 时逐候选过闸，`false`（明确无效）入 `skipKeys`、其余入 `validTiers`（**闸读不出 nil 按有效对待**，交钉档读回兜底，API 缺位行为同旧版）；`tierOn=false`（通道不可用）时全量候选维持旧钉档回退路径不动 | :906-921 |
| 钉档只钉有效档 | `pinInstanceDifficulty` 改收有效档 id 表（函数体零触），钉首个钉得上的有效档；`#validTiers==0` 时不钉不切 | :922-926 |
| 无效档跳过注记「跳过档=key」 | 快照 `pinNote` 追加 `（跳过档=a/b）` | :956-958 |
| 零有效档=无难度维单趟+注记 | 快照注记三态改写：无难度维（正常态文案）/有效档全钉失败（非正常，截图反馈）/改钉明示；实例记录带 `validTiers`/`noDiff` 入阶段二；实例收尾 msg 追加「（无难度维·normal 单档）」 | :943-955、:974-976、:1032 |
| 无难度维 tiers 记 normal 单档（converter 零改） | 阶段二 `nextBoss` 加 `noDiff` 分支：loot 逐件 `primary_tiers={normal=primary_values}`/secondary 同（仅当数值表非空——无数值品类天然不产档，与档扫「只记有数值的档」口径一致），然后 `finishBoss`，不进档扫 | :1063-1072 |
| 档扫只扫有效档 | `collectTiers` 调用改传 `inst.validTiers or tierSet`（tierOn=false 旧路径回退全量候选） | :1079 |
| 有效档>0 而整 BOSS 0 带档判异常**黄字** | `tiered==0` 四分案 `err`→`msg`（档已自适应探测只扫有效档，0 带档多为数据侧无静态值而非通道故障）；hardFail/switchFail 维持红字不动 | :1093-1110 |
| 段首启用文案同步自适应口径 | 两条启用 msg 改「按实例自适应探测有效档…无难度维实例单趟采集记 normal 单档」 | :1224 等 |
| 常量退役 | `RAID_ANCHOR_CANDIDATES`/`DUNGEON_ANCHOR_CANDIDATES` 删除（候选集由 tierSet+逐实例闸取代）；grep 确认零残留 | — |

## 三、冒烟目标调整

- 团本目标：至暗之夜·鲁阿夏尔 → **烈毒之渊·盘魂者内克扎莉**（四档全有效，lfr 路径进冒烟网）；毒牙祭坛·拉维不动（:1291-1292）。
- 冒烟门槛：快照 slot=15 + 盘魂者内克扎莉 **16±2 件全带档** + 拉维 7/7 全带档 + 物品名归属人工核对。
- 至暗之夜留全量验：4 首领 8±2 件 + 「无难度维」注记 + tiers 仅 normal 单档。
- 冒烟完成 msg 与 /wjdc 用法行同步改。

## 四、改动文件清单

| 文件 | 改动 |
|---|---|
| `addon/WoWButlerExporter/WoWButlerExporter.lua` | 文首 changelog 加 1.0.21 条目；`ADDON_VERSION="1.0.21"`；自适应探测+三态快照注记+跳过档注记；`validTiers`/`noDiff` 随实例入阶段二；noDiff normal 单档分支；collectTiers 传有效档；0 带档降格黄字；冒烟换目标；锚档候选常量退役 |
| `addon/WoWButlerExporter/WoWButlerExporter.toc` | `## Version: 1.0.21` |
| `addon/WoWButlerExporter/README.md` | 1.0.20 段去「当前版本」；新增「1.0.21 说明（BUG-094 二次翻转终案，当前版本）」运营向四条 |
| `addon/WoWButlerExporter/运营测试步骤卡.md` | 头部加 1.0.21 行；Q1 版本→1.0.21（SecureTypes 案改注「已销案观察关闭」）；Q4 冒烟门槛→盘魂者内克扎莉 16±2 全带档+拉维 7/7+slot=15；V14 行「跳过档=key 注记可见且属正常」；V15 行至暗判据→4 首领 8±2+无难度维注记+normal 单档；V16 行同步；判据头注加「1.0.21 终案口径」 |
| `scripts/validate-export.js` | `SMOKE_GATES` 改烈毒之渊·盘魂者内克扎莉 16±2（判定逻辑加 `tol` 支持）+拉维 7/7；新增 `NO_DIFF_INSTANCE` 检查——V15 块内至暗终案形态（4 首领+全件 tiers 键⊆{normal}，出现其他档键=❌「094 四档污染特征现形」具名列件，无 tiers 件仅提示）；全量分支 V16 提示行旧目标文案同步 |
| `docs/问题与需求清单.md` | BUG-094 行追记二次翻转终案+1.0.21（待真机回验）；BUG-089 行 D3 销案（观察关闭/dormant，复现即重开）+爆栈 4 条物证记功；REQ-121 行 D3「支持非我方代码面定性」改「已销案」 |

## 五、零触确认

- `pinInstanceDifficulty` 函数体、`collectTiers` 扫描体（建索引/陈旧校验/staleSkip/absent 分案）、`isDifficultyValidForInstance` 闸本体、096 显式枚举链、095 gating 五源、过滤器三件套、就绪门/看门狗/段提交、毒咒跨行：零触。
- 导出字段格式：仅新增 tiers 的 normal 单档形态（1.0.15 已有同形态历史数据，converter 零改）；skipped_instances/gating 口径不动。
- 候选档数值表（17/14/15/16+1/2/23/24）：零改——终案数值口径与现有候选恰好全覆盖。
- 本包无 app js/css 改动，不涉版本串 ?v= 递增。

## 六、自检证据

- **Lua 块配对自检（backup/_pytmp/lua-block-check.js，剥注释/字符串含长括号层级）：PASS——function=83+if=168+do=28 → end=279=279，repeat/until=1/1**。较 1.0.20 的 270 增 9，与新增量吻合（探测循环/noDiff 分支/三态注记等）。
- **nil 安全走查**：`validTiers` 空表仅在 `tierOn=true` 且全档无效时出现，该情形被 :947 首分支截获；`tierOn=false` 时 validTiers=全量候选非空——:951 `validTiers[1].id` 不可达空表。
- 残留 grep：`ANCHOR_CANDIDATES`/`anchorCandidates` 零命中；`validTiers`/`noDiff`/`skipKeys` 接线点齐（:906/:925/:947/:951/:956/:975-976/:1032/:1063/:1079）。
- **validator 回归（node --check 通过）**：
  - 合成冒烟件（烈毒之渊·盘魂者内克扎莉 16 件全四档+拉维 7/7）：V16 两门槛 **✅**；
  - 合成全量件（至暗之夜 4 首领 8 件 tiers 仅 normal）：V15 至暗终案形态 **✅**；
  - 合成污染件（至暗件带 lfr/heroic/mythic）：V15 至暗终案形态 **❌「094 四档污染特征现形」**具名列件——❌ 路径生效；
  - 真机 1.0.16 残缺 SavedVariables 重跑：解析无崩坏，且正确判出旧文件至暗之夜四档污染形态 ❌（旧版行为留痕，正是 1.0.21 要消除的形态）；VER 行 ❌「游戏目录 toc=1.0.20 落后仓库 1.0.21」——门禁①按设计喊停，**同步插件后即绿**。
- **如实声明：本机无 WoW 客户端、无 lua 解释器/luaparse**——验证=块配对+nil 安全走查+残留 grep+validator 合成/真机文件回归；判据全走真机。

## 七、遗留申报

1. `EJ_IsValidInstanceDifficulty` 对「世界首领整合类目」是否稳定返回全 false 为真机首验点——若读不出（nil）按有效对待，至暗之夜会走旧全量候选钉档回退（行为同 1.0.20，不更坏）；真机快照注记行一跑定案。
2. 「世界」档（潮缚石窟）行为同 LFR/17 为 FrameXML 注释+下拉代码取证，档值语义（primary_values 按 17 档采）与显示名「世界」的对应关系以真机档值合理性复核为准。
3. 无难度维实例若某件有数值但运营期望「无档」，normal 单档会带档——属设计内（与 1.0.15 形态连续），converter 侧无须区分。

## 八、验收判据与物流链

- **冒烟**：快照 slot=15 + 盘魂者内克扎莉 16±2 件全带档 + 拉维 7/7 全带档；快照行「跳过档=key」注记可见且属正常；出现「有效档全部钉失败」即截图回传。
- **全量 A/B**：照旧（V1 双跑一致/V14 大米 8 本 225±/V15 三团本在场+至暗终案形态/V16/V17 等）；至暗之夜判据=4 首领 8±2 件+「无难度维」注记+tiers 仅 normal 单档。
- **物流链**：运营同步插件（仓库 `addon/WoWButlerExporter` 整目录→游戏目录，VER 门禁要求）→ 公司机冒烟 → 全量 A/B → 先截图聊天全文再 /reload → 直读 SavedVariables 跑 validator 附预判结果。本包未 commit（git 纪律：运营验收通过前不提交）。
- **台账与 validator 随包说明**：`scripts/validate-export.js` 本轮改动建议随插件包同 commit（工具件已入仓延续）；台账三行（BUG-094/BUG-089/REQ-121）按运营上一轮「台账行留下次文档批」批复，可留文档批，亦可随包——听运营裁定。

## 附：commit 物料（顾问侧备妥，运营定夺后提交）

标题：

```
插件1.0.21：按实例自适应有效档探测+无难度维单趟（BUG-094二次翻转终案，真机待验）
```

描述（三段式）：

```
什么：插件 1.0.21——BUG-094 二次翻转终案落地。钉档前逐候选过 EJ_IsValidInstanceDifficulty
按实例自适应探测有效档，只扫有效档、无效档注记「跳过档=key」；零有效档=无难度维
（世界首领类，如至暗之夜）单趟基础采集+tiers 记 normal 单档（converter 零改）；
有效档>0 而整 BOSS 0 带档降格黄字；冒烟目标改烈毒之渊·盘魂者内克扎莉（16±2 全带档）。
锚档候选常量退役。validate-export.js 同步新冒烟门槛+至暗终案形态判据。

为什么：运营截图实证至暗之夜=无难度维世界首领整合类目（难度下拉不存在），
「世界」档=PrimaryRaidLFR 行为（暴雪下拉实际 SetDifficulty(17)）——定档表打不了
「哪个实例有哪些档」，旧「LFR 未开」定性作废。另：D3（SecureTypes）销案记功入台账
（三条栈全在 ArchonTooltip+暴雪 UI 链，与我方零交集，定 dormant 观察关闭）。

验证：本机无客户端——Lua 块配对 PASS（279=279）+nil 安全走查+残留 grep 零残留；
validator 合成三文件回归（冒烟新门槛 ✅/至暗 normal 单档 ✅/四档污染 ❌ 两态生效）+
真机 1.0.16 残缺文件重跑无崩坏。真机判据=步骤卡 V14/V15/V16（1.0.21 终案口径）。
```
