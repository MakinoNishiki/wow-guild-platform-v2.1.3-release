# 任务书 #22-补丁：职责图标修正 + 心愿单/装备分配换装补漏 + 成员管理视觉统一

> 执行方：Kimi Code ｜ 单一工作包，一个 commit：「任务书#22-补丁：职责图标修正与换装补漏」｜ 密钥：不涉及 ｜ 朋友侧：零操作
> 来源：任务书 #22 运营验收反馈（截图在案），全部为定稿修正，照做即可。
> **开工顺序红线：#22 三个 WP 合并部署后开工**（已满足）。

---

## 修正项①：职责图标替换（素材运营已更新入库）

- 旧职责图标（坦克/治疗/输出）为切图失误品（一格内混入相邻图形残片），**废弃**；
- 新素材已由运营替换 `assets/icons/roles/{tank,healer,dps}.png`：盾=蓝（坦克）、十字=绿（治疗）、剑=红（输出），均为完整圆形官方图标，64×64 PNG；
- 全站引用职责图标处（成员管理职责列、考勤名单、装备分配、心愿单、仪表盘等）渲染新图即可，图标后保留文字名（图标+文字，不做纯图标）。

## 修正项②：心愿单换装补漏（#22 WP2 漏项）

验收实测：心愿单专精列（「主专精（野兽控制）」）仍是纯文字，无图标。要求：
- 专精列显示 = 专精图标 + 文字，专精图标取 IconMap（含别名链路）；
- 「需求成员」列：成员名**左侧加职业图标**，整体套**职业颜色 tag**（做法与成员管理页职业列一致）；「未认领」状态文字**不进入 tag 内**，保持 tag 外独立小字（现状版式，仅调整归属）。

## 修正项③：装备分配换装补漏（#22 WP2 漏项）

- 「分配给」列：成员名左侧加职业图标 + 职业颜色 tag（与②同一套渲染组件，禁止各写一份）；「未认领」同样在 tag 外；
- 装备名称/部位/主副属性列现状保留（REQ-054 体系已含），本次不加图标。

## 修正项④：成员管理专精列职业色 tag 化

- 专精列改为：**职业颜色 tag 底 + 专精图标 + 专精文字**（tag 底色/描边取 IconMap 写入的官方职业色，与职业列视觉同源），一行内职业 tag 与专精 tag 风格统一、并排不打架；
- 副专精多个时同行依次排列，不折行撑行（沿用 WP1 的 20px 徽标规范）。

## 修正项⑤：成员管理操作列与出勤率列版式

- 操作列：整行操作元素（编辑/装备/历史图标组 + 认领区）**压缩进同一行**，认领按钮/「待认领」标签/「认领人：XXX」均不得换行；列宽不足时优先压缩其他列，操作列保最小宽度；极端窄屏走既有横滚兜底（BUG-040 机制），不允许换行堆叠；
- 出勤率列：数字与表头文字左对齐微调（现状偏中悬空），与该列其他内容对齐基准一致；
- 全程对照 DESIGN.md 表格规范执行（文本左/数字右/状态居中），不得自造对齐规则。

## 验收

- 1366×768 与 1920×1080 两档截图：成员管理（含操作列单行、专精职业色 tag、新职责图标）、心愿单（专精图标 + 成员职业 tag）、装备分配（成员职业 tag）；
- 长角色名/多副专精/双职责极端行不折行不溢出；
- 控制台零报错零 404；npm test + SEC-001 回归全绿；测试数据自清理复核为零。

### 红线

- 纯前端渲染与样式调整，不碰 server.js、不动数据与权限逻辑；
- 职责图标只用运营入库的新素材，缺图报告，禁止生成/抓图顶替；
- 不动密钥与 .env。
