# 任务书 #33：BUG-071 报表「已删除」伪行行高修正

> 单点小修，P3 观感项。示意来源：运营 2026-08-11「继续」。本包为 §7 第二批第 3/3 个版本。

## 一、问题

统计报表（#page-reports .stats-rank-table）中，已删除角色的伪行（名字 + 「已删除」徽标）徽标换行，整行偏高 **58px**；正常数据行 **44px**（BUG-070 验收实测基准）。BUG-058 期既有观感，BUG-070 完工时按 §1 报备不擅改，现运营示意修复。

## 二、施工要求

1. **先侦察后动手**：定位伪行 DOM 与徽标样式的换行成因（换行发生在哪一列、是列宽不足还是徽标块级/间距挤压），成因写入完工报告。
2. **修法按最小选择器范围**：仅作用于「已删除」伪行；若唯一解必须动共用样式（如全表单元格 white-space），**按 §1 先送审**。
3. **语义零改动**：徽标文案「已删除」保留、判定来源不动；只修排版。
4. **正常数据行一行不动**。

## 三、硬约束（验收时逐值复测，任一不达标=回退）

- **BUG-058 不回退**：1366 档 scrollWidth=468=clientWidth，无横向滚动。
- **BUG-070 不回退**：1366 档职业列两字（死亡骑士）单行不折行、行高 44px。
- 修复目标：伪行徽标与名字同行，伪行行高与正常行一致（44px）。

## 四、规范产物

- §1 副作用审计单（伪行选择器影响面逐项）；
- §2 生效值断言：伪行 offsetHeight、徽标与名字同行（getBoundingClientRect 同 top）、normal 行 44px 逐值复测，全部 computed/getComputedStyle 实测，非类名推断；
- §4 样本声明：伪行样本=测试数据自建自清理（报表中无现成已删除样本则如实声明并构造，清理复核为零）；

## 五、验证与回归

- verify-bug071.js：三硬约束逐值断言 + 修复目标断言；
- 回归：task27 三组、task29-wp1、task32、npm test、SEC-001；
- 测试数据自清理复核为零；
- 版本串 20260811.43 → 递增（index.html 10 处 + data.html 6 处同步）。

## 六、B 表（push 后运营验收，随批统一交付）

| 项 | 内容 |
|---|---|
| B1 | 报表页「已删除」伪行：徽标与名字同行，行高与正常行一致 |
| B2 | 1366 档职业列两字单行不折行（BUG-070 不回退） |
| B3 | 报表页无横向滚动条（BUG-058 不回退） |

## 七、纪律

完工报告送审，**不 commit 不 push**；changelog 补录 BUG-071；台账由 Code 登记、顾问复核。
