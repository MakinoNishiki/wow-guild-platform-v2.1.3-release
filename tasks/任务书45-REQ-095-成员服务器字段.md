# 任务书 #45：成员身份模型补服务器字段+同名口径升级（REQ-095，REQ-002 语义完整落地）

> 执行方：Kimi Code ｜ 六 WP、一个 commit ｜ 密钥：**WP1 迁移需 SSH_PASS（运营私聊直发）** ｜ 朋友侧：零操作
> 需求登记：**REQ-095**（P1，口径 2026-08-10 运营全定；BUG-060 并入本项、B4 补验并入验收）
> 背景：游戏现行设定同公会允许同名、前提不同服务器；raid_members 无 server 列，唯一索引停在 (guild_id,name) partial——同名跨服被误拦=BUG-060 根因。REQ-002「同服唯一、跨服允许重名」落点时假设公会单服，跨服时代假设失效。

---

## 一、WP1｜schema 迁移（sql/28）

- `raid_members` 加 `server text` 可空默认 NULL（**存量不回填公会主服，留空待用户补录/导入写入**——2026-08-10 运营定口径）。
- 唯一索引重建：`(guild_id, name, COALESCE(server,''))` 活跃 partial（status<>'离队'），旧 (guild_id,name) partial 退役；迁移纪律=备份→SSH+docker exec→NOTIFY pgrst ×2→回滚注释→前后行数复核。
- 侦察先行（§1 审计单附）：user_characters 服务器维度现状、导入数据 realm 字段、认领 UI 同名消歧现状。

## 二、WP2｜表单与列表

- 新增/编辑成员表单加「服务器」字段（文本输入，可空=同服/未填口径）；**我的角色**表单同步加。
- 成员列表**常驻服务器列**（2026-08-10 运营定：非仅同名时显示）；空值显示「—」类占位不出空括号。
- 同名消歧：所有成员名展示点若同名并存，展示「名字（服务器）」消歧格式（server 空则裸名）。

## 三、WP3｜唯一性校验升级

- 新建/编辑校验从 (guild_id,name) 升 (guild_id,name,server)：同服同名拦、跨服同名放行、server 空与空比。
- 撞离队同名恢复逻辑对齐新键（恢复优先于新建既有语义不变）。

## 四、WP4｜智能导入/WCL 链路

- 导入/WCL realm 落库 server；恢复判定/匹配对齐 (name,server) 双键（导入预览页已有服务器列，FIXED-012 佐证）。
- 导入遇到同名跨服成员：按 server 精确匹配，匹配不到走既有「添加为成员」三分区流。

## 五、WP5｜全名解析点 id 优先化（顾问挂账项）

- `lootSave` 等现按活跃名反查 id 的点，改 picker 直传 id（同名合法化后按名反查不再安全）；全站 grep「按名查 id」类调用点扫齐列清单，逐点改或申报。
- 与 BUG-059 A 方案同向，本包一并落地。

## 六、WP6｜BUG-060 合并验收

- BUG-060（同名跨服误拦）本包根治，其 B4 补验并入本包 B 表。

## 七、规范与红线

- §1 审计单（同名消歧全展示点/解析点清单先列后改）；§4 样本声明（构造同名跨服双成员实测）；§6 版本串 .56→.57 两壳 16+2。
- 零依赖；DB-first；测试数据自清为零（构造的同名成员删干净含垃圾桶）；完工**不 commit 不 push**。
- 回归：task27×3/29/31/32/bug071/34~44 + npm + SEC-001 全绿清零。

## 八、B 表（运营手工，5 项）
| # | 操作 | 预期 |
|---|---|---|
| B1 | 新增成员「测试同名」不填服务器→再加一个「测试同名」服务器填「白银之手」 | 第一个成功；第二个**不再误拦**（BUG-060 根治），跨服同名共存 |
| B2 | 成员列表看服务器列 | 常驻显示；未填的显示「—」；同名两条各自消歧展示 |
| B3 | 同服同名再试（第三个「测试同名」也填「白银之手」） | 正确拦截提示 |
| B4 | 撞离队同名：把一条离队→重新加同名同服 | 弹「是否恢复」语义不变（BUG-060 B4 补验并入） |
| B5 | 心愿单/装备分配给同名成员之一分配装备→核对归属 | 分配到正确的人（id 优先化，不串同名） |

### commit 物料
标题：「任务书#45：成员服务器字段+同名口径升级（REQ-095，REQ-002 完整落地，BUG-060 根治）」，三段式；不 push。
