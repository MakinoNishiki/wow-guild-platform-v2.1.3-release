# 任务书 #41：问题反馈入口·QQ群（REQ-112）

> 执行方：Kimi Code ｜ 单工作包、一个 commit ｜ 密钥：不涉及 ｜ 朋友侧：零操作
> 需求登记：**REQ-112**（2026-08-12 运营提出并亲定位置与动效要求）
> 运营定稿口径：**左侧栏底部、版本号上方**加「问题反馈」按钮；鼠标悬浮显示群二维码卡；按钮带**渐变高亮 hover 特效动画**。
> 素材：`assets/qq-group-qr.jpg`（运营提供，随开工话术一并转交；群名「魔兽管家用户群」、群号 1104273954）。图片为静态资源，**不占版本串 16+2**（仅 js/css/html 引用计数）。

---

## 一、WP1｜按钮与悬浮卡

- **位置**：侧栏导航容器底部、版本号文案上方（版本号保持原位最底）。仅主应用壳 index.html，公开壳 data.html 无侧栏不加。
- **按钮**：文案「问题反馈」（可加 💬 类字符图标，禁 emoji 之外的依赖）；宽度与导航项协调，视觉权重低于主导航（次级入口，不抢视觉）。
- **悬浮卡内容**（hover 触发，显示于按钮上方/右上方，全程不出视口）：
  - 群二维码图（白底图必须加**浅色卡托**——圆角白底 padding，深色主题下保扫码识别率）；
  - 群名「魔兽管家用户群」+ 群号「1104273954」文本（群号可点按复制更好，非强制）；
  - 引导文案一行（如「扫码或搜群号加入，反馈问题/建议」）。
- **移动端降级**（hover 不可用）：点击按钮切换悬浮卡显隐，点卡片外区域/ESC 关闭。
- 层级：卡片浮于侧栏与内容区之上即可，非 modal 不走 modalStack；不得遮挡 topbar 操作区。

## 二、WP2｜渐变高亮 hover 动画

- 按钮 hover：渐变高亮（如深蓝→金色微光渐变位移/亮度渐变），**纯 CSS transition 双向**（§3：hover 进入与离开两向连帧，不得只写进入向）。
- `prefers-reduced-motion` 降级：动画关闭，hover 直接呈现高亮终态（§3 硬规约）。
- 悬浮卡出现可配轻量淡入（可选项），同样遵守双向+reduced-motion 降级。

## 三、规范与红线

- §1 审计单（侧栏结构/REQ-105 拖拽排序将来同域——实现时不得把按钮写死成不可重排结构中的障碍，报告说明与导航项的 DOM 关系）；§2 computed 生效值断言（按钮渐变/卡托底色/卡片位置）；§3 双向连帧+降级断言；§5 注释安全；§6 版本串 .52→.53（两壳 16+2，js 改动照常计数，图片不计）。
- 零依赖零构建；深色 WoW 主题；只动 index.html + css/main.css + （如需）js/app.js + 新增图片资源。
- 完工**不 commit 不 push**，报告+审计单送审。

## 四、验收

- verify：按钮存在性/卡内容三要素（图/群名/群号 1104273954）/hover 显隐/移动端点击显隐/computed 断言/reduced-motion 断言。
- 回归：task27×3/29/31/32/bug071/34/35/36/37/38/39/40 + npm + SEC-001 全绿。

### B 表（运营手工，4 项）
| # | 操作 | 预期 |
|---|---|---|
| B1 | 看左侧栏底部 | 版本号上方有「问题反馈」按钮，样式协调不突兀 |
| B2 | 鼠标悬浮按钮 | 按钮渐变高亮动画流畅（进出两向）；悬浮卡出现，二维码清晰、白底卡托、群名+群号正确 |
| B3 | 手机（或 DevTools 移动模拟）点按钮 | 点击切换悬浮卡，点外部/ESC 关闭 |
| B4 | 实际用 QQ 扫一次码 | 能正确识别跳转加群 |

### commit 物料
标题：「任务书#41：侧栏问题反馈入口+QQ群悬浮卡与渐变高亮（REQ-112）」，三段式；不 push。
