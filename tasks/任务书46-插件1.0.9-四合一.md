# 任务书 #46：插件 1.0.9 四合一——特效补采+毒咒采集+兑换物展开+图标（REQ-088/REQ-110③/REQ-089/REQ-092）

> 执行方：Kimi Code ｜ 四 WP、一个 commit ｜ 密钥：不涉及 ｜ 朋友侧：零操作
> 需求登记：**REQ-088**（饰品特效补采，BUG-061 根因落采集端）、**REQ-110③**（毒咒采集，运营原话「纳入插件最好能直接导出来」）、**REQ-089**（兑换物→职业套装展开）、**REQ-092**（物品图标）——2026-08-09/08-11 运营定：S2 翻牌后捆绑插件 **1.0.9** 一次升到位。
> 现状基线：插件实为 **1.0.7**（WoWButlerExporter.lua:18；1.0.8 未实施）；毒咒插点 parseStatLines():81-99；converter=scripts/wjdc_convert.py norm_items():251 / build_load_rows():430 / _CMP_FIELDS:469 + 文首冻结声明 v3。

---

## 一、WP1｜插件采集端 1.0.7→1.0.9（REQ-088 + REQ-110③ + REQ-092 采集半）

- **特效补采（REQ-088）**：饰品 tooltip 特效行（「装备：…」绿字段落）采集进导出；S1 池 40 件饰品 39 件空串的根因定位后修复（侦察：1.0.6/1.0.7 parseStatLines 为何漏特效行——换行/颜色码/多段落形态），修复后实测导出样本特效非空。
- **毒咒采集（REQ-110③）**：tooltip 品质行下绿字「毒咒」标签行识别→导出字段 `venomcurse`（有=「毒咒」，无=空）；与 REQ-110 WP1 数据模型口径一致（文本标签型）。
- **iconID 采集（REQ-092 采集半）**：导出加 `iconID`（itemID 查询接口既有，透传即可）。
- 版本号 1.0.7→**1.0.9**（1.0.8 跳号追平登记口径）；toc/lua 版本串同步；导出格式版本说明更新。

## 二、WP2｜converter 映射（wjdc_convert.py）

- norm_items/build_load_rows 映射：effect（新采集源）/venomcurse/iconID 三字段透传进 load rows；_CMP_FIELDS 同步加键（比对零丢失口径延续「converter 36=36」先例）；文首冻结声明升 v4 注明四合一变更。
- **REQ-089 兑换物展开**：兑换物行→按职业/专精展开对应部位套装装备（含主副属性）、归位对应 BOSS 掉落——展开规则表侦察后送审再施工（§1：展开映射数据源=静态规则预制，符合 DEC-006 工具化口径）；展开产物=正常装备行进 boss_loot（item_type 按部位、slot 按部位、非杂项），兑换物原行保留不删。

## 三、WP3｜DB 与公示页（REQ-092 落地半）

- **sql/29**：boss_loot/dungeon_loot 加 `icon_id int` 可空（迁移纪律同前）；`get_public_loot_detail` CREATE OR REPLACE 透出 icon_id（显式白名单铁律）。
- 公示页卡片右上角渲染图标（缺图/无 iconID 回退不显示不占位破版）；主应用壳同源渲染（双壳一套）。
- **素材管道**：iconID→PNG 提取建库（复用 REQ-069 运营源图先例；首库约 300 枚+每赛季增量，assets/icons/ 目录，懒加载约 2-3MB）——提取工具链侦察后施工，产物入仓不占版本串；文件名=iconID.png。
- 图标 <img> 懒加载（loading="lazy"）+ 加载失败隐藏；不阻塞卡片首屏。

## 四、端到端链路与验收

- 链路实测：游戏内插件 1.0.9 导出→converter 转换→智能导入/入库→数据中心可见→公示页卡片（特效行/毒咒徽标/图标）全透出。
- §4 样本声明（导出样本件数、特效非空率前后对比、毒咒样本、图标命中率）；§6 版本串 .57→.58 两壳 16+2。
- 回归：task27×3/29/31/32/bug071/34~45 + npm + SEC-001 全绿；308 基线变化如实申报（REQ-089 展开会增量装备行——基线调整需顾问确认后更新断言，**禁止静默改基线**）。
- 完工**不 commit 不 push**；插件 lua/converter 改动与 web 改动同 commit 或分两个 commit 由顾问审时定。

## 五、B 表（运营手工，5 项）
| # | 操作 | 预期 |
|---|---|---|
| B1 | 游戏内 /reload 后用 1.0.9 插件导出， converter 转换 | 转换零报错；导出文件含特效/毒咒/iconID 字段 |
| B2 | 智能导入该文件→数据中心抽查 3 件饰品 | 特效文本正确显示；毒咒装备带毒咒字段 |
| B3 | 公示页找一件毒咒装备+一件普通饰品 | 毒咒卡有绿徽标；两件卡片右上角有物品图标（清晰不变形） |
| B4 | 找兑换物对应 BOSS（如虚影尖塔掉落兑换物的 BOSS） | 该 BOSS 下落池出现展开的职业套装装备（有主副属性） |
| B5 | 弱网/无图场景：DevTools 禁缓存慢速刷新公示页 | 图标懒加载不卡首屏；个别无图装备不破版 |

### commit 物料
标题：「任务书#46：插件 1.0.9 四合一——特效/毒咒/图标采集+兑换物展开（REQ-088/110③/089/092）」，三段式；不 push。
