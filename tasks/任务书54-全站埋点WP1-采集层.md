# 任务书 #54：全站数据埋点 WP1——采集层（REQ-141）

> 执行方：Kimi Code ｜ 单工作包一次送审 ｜ 密钥：不涉及（server.js 复用 .env 既有 service_role，零新增密钥）｜ 运营侧：零操作（验收仅需真机逛两个公示页）
> 前置状态（已实测确认的事实，直接采信）：
> - 全站零埋点零访问日志（2026-09-22 侦察实锤）：server.js 无 morgan/无写文件日志，仅 3 处 console.log 性能日志（server.js:989/993 WCL 耗时、:1149 /api/db 写代理耗时）；无 nginx/反代/PM2 层；静态分发入口 server.js:1035 起
> - server.js 先例：/api/supabase-config 公开配置端点；/api/db 写代理（service_role 服务端转发 PostgREST）——本任务 /api/track 与之间层同款
> - 版本串现=20260919.69（commit 5d87dcb 线上，index×14/decor×5/data×7）
> - sql 编号：sql/32 已被任务书 #53 钉占（decor_plan，未施工）——**本任务用 sql/33**，手动迁移纪律下档案编号≠执行顺序，#53 后执行 32 零冲突
> - 方案已锁：埋点与数据看板-方案单 v1.1（路线 A 自建、指标口径、身份三层、红线），本任务书=其 WP1 采集层
> - 待解决缺陷：小红书引流后 UV/PV 完全不可考（2026-09-22 运营提出）

开工前先读《开发规范》。本任务书之外不顺手加功能。

---

## 零、侦察闸（开工前半小时，写进修改报告即可，不等顾问回闸）

只读确认三处，与预期不符**停手上报**，不擅自改设计：

1. **server.js 结构钉点**：/api/db 写代理的 service_role 转发写法（复刻其 fetch PostgREST 口径）、静态分发回调内 API 分支挂载位置、监听端口逻辑（DEPLOY_RUN_PORT）；
2. **app.js 登录态钉点**：登录成功（会话建立）与退出（handleSignOut）两处回调位置——本任务授权各加一行 `window.__wbUid` 赋值/清空，先报钉点行号；
3. **PostgREST 写库先例**：确认 service_role INSERT 走 `${SUPABASE_URL}/rest/v1/` 路径与 .env 变量名（SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY）。

## 一、需求

### 1. sql/33：analytics_events 建表（DB-first）

```sql
create table if not exists public.analytics_events (
  id      bigint generated always as identity primary key,
  ts      timestamptz not null default now(),          -- 服务端权威时间
  event   text not null,                               -- page_view / 自定义事件名（白名单由服务端控）
  page    text not null,                               -- decor / data / index:<页签key>
  uid     uuid,                                        -- 登录用户（溯源）；未登录为 null
  vid     text,                                        -- 访客 ID（localStorage 临时牌）
  ip_h    text,                                        -- IP+日盐 hash（明文 IP 永不落库）
  ref_dom text,                                        -- 来源域（仅 hostname，空=直接访问存 null）
  props   jsonb not null default '{}'
);
create index if not exists idx_analytics_events_ts    on public.analytics_events (ts desc);
create index if not exists idx_analytics_events_event on public.analytics_events (event, ts desc);
create index if not exists idx_analytics_events_page  on public.analytics_events (page, ts desc);
alter table public.analytics_events enable row level security;   -- 零策略=anon/authenticated 全拒，仅 service_role 可写可查
comment on table public.analytics_events is 'REQ-141 全站埋点事件表（任务书 #54）：保留期 90 天滚动（WP2 实现清理）';
```

- **回滚语句**：`drop table if exists public.analytics_events;`
- **执行纪律**：先备份（既有备份通道）→ 执行 → `NOTIFY pgrst, 'reload schema';` → REST 复核（service 查表在场、anon 查 0 行/不可见实证 RLS）。

### 2. server.js：POST /api/track（复刻 /api/db 同层先例）

- **白名单**（顶部常量，一处维护）：`TRACK_EVENTS = ['page_view']`（WP1 只开 page_view；WP3 扩名单只改此常量）；page 格式校验 `/^(decor|data|index:[a-z]+)$/`；
- **校验**：event 不在白名单→204 吞掉（不入库不报错）；props 序列化 >2KB 或 body >8KB→204 吞掉；
- **限流**：同 IP ≤60 次/分钟（进程内 Map 滑动窗口，超→204 吞掉）；
- **ip_h**：`sha256(ip + '|' + YYYYMMDD + '|' + (process.env.TRACK_SALT || 'wb-track'))`——日变不可跨日关联；IP 取 `x-forwarded-for` 首段兜底 `req.socket.remoteAddress`（当前无反代，兼容将来）；
- **uid**：取 body.uid（前端显式传；WP1 不验 JWT，统计自用口径，边界写注释）；body 字段缺失→对应列 null；
- **写库**：service_role POST `${SUPABASE_URL}/rest/v1/analytics_events`，只取白名单列组装行（event/page/uid/vid/ip_h/ref_dom/props），body 其余字段一律丢弃；
- **响应恒 204**；自身异常 try/catch 打 console.error 不 500（埋点永不得影响业务）。

### 3. js/track.js（新建，零依赖 IIFE，全局 `WBTrack`）

- **vid**：localStorage `wb_vid`，无则 `crypto.randomUUID()`（降级 `Date.now()+Math.random()` 拼接）写入；
- **page 推导**：`location.pathname` 含 decor.html→`decor`、data.html→`data`、其余→`index:dashboard` 起始；
- **index 壳页签 PV**：DOMContentLoaded 后包装 `window.switchPage`——原函数执行后上报 `index:<key>`（包装前判存在性，不存在的页不炸）；
- **ref_dom**：`document.referrer` 取 hostname，空→null；
- **uid**：读 `window.__wbUid`（无→null）；
- **发送**：`navigator.sendBeacon('/api/track', Blob json)` 优先，不支持/失败→`fetch keepalive` 兜底；全程 try/catch 静默；
- **API**：`WBTrack.event(name, props)`（WP1 仅 page_view 自动；自定义事件 WP3 挂点时调用，服务端白名单外自动吞）；
- 页面加载完成即自动上报一次 page_view。

### 4. app.js 授权两行（钉点经零节侦察确认）

- 登录会话建立处：`window.__wbUid = <当前用户id>;`
- handleSignOut 内：`window.__wbUid = null;`
- 除此两行 app.js 零触碰。

### 5. 三壳挂载 + 版本串

- index.html：`js/app.js` 之后加 `<script src="js/track.js?v=20260919.70"></script>`；
- decor.html / data.html：各自最后一条业务 script 之后同行挂载；
- 版本串三壳 **.69→.70 全量递增**（注释行+全部 ?v=；index×14→.70、decor×5、data×7；本任务新增 track.js 引用行使 index 总计数 14→15，verify 口径同步）。

## 二、目标与约束（红线）

- 埋点静默兜底：任何异常不得影响页面业务行为与既有 API；
- 明文 IP 不落库；referrer 只存域；body 白名单列外字段一律丢弃；
- 零新增依赖（不引 morgan 等任何包）；零构建；
- server.js 只增不改（既有端点/静态分发逻辑零行为变更）；app.js 限授权两行；
- 版本串三壳 .69→.70 义务同步（新增 track.js 计入）；
- commit 规范：「任务书#54-WP1：…」三段式；完工只送审，不 commit 不 push；
- 发现与本文档冲突的现实，先报告运营定夺，不擅自改设计。

## 三、验证（scripts/verify-task54.js 新建，逐项 ✓/✗ 汇总非零退出）

**A 静态**：sql/33 文件在位（表/三索引/RLS/回滚注释）；server.js 端点锚点（白名单常量/page 正则/ip_h sha256/限流/204）；track.js 锚点（vid/sendBeacon/switchPage 包装/静默）；三壳引用行；版本串三壳 .70 计数（index×15/decor×6?——以「旧串零残留+全量 ?v= 一致」为准，计数按实）+旧串零残留；`node --check server.js`、`node --check js/track.js`。

**B 实测**（自起服务器，service_role 仅用于断言与清理）：
1. curl POST 合法 `{event:'page_view',page:'decor',vid:'t54-vid-1',props:{test:1}}` → 204，REST 查行在场且 ip_h 非空、uid 为 null；
2. curl POST 非法 event（'hack'）→ 204 但**零入库**；
3. curl POST 超大 props（>2KB）→ 204 零入库；
4. 浏览器真跑 decor.html → 自动产生 page_view 行（page=decor、vid 持久：二次访问 vid 相同、ref_dom 与 UA 合理）；
5. index 壳登录测试用户 → 切考勤页 → 产生 `index:attendance` 行且 uid=该用户 id；退出后再开公示页 → uid 回 null。

**C 清零**：全部测试事件行（props->>test 标记或 t54- 前缀 vid）service_role 删除，复核零残留。

## 四、运营验收步骤（push 后）

1. 真机随手逛 decor.html、data.html 各几下；
2. 顾问 SQL 代查：`select event,page,vid,ref_dom,ts from analytics_events order by ts desc limit 20;`——能看到你的访问记录即过；
3. 看板（WP2）未上前，UV/PV 由顾问 SQL 代报。

## 五、送审物料

diff 全文（git add -N 新件后 git diff）+ 修改报告 + verify 输出全文 + **sha256 清单**（track.js/verify-task54.js/sql/33 新件三枚必附）。
