# 任务书 #38：修复小包（BUG-077 按钮文案 / BUG-075 422 文案 / BUG-076 死选择器）

> 三个 P3 一行修级合包。§7 第四批第 2/3 个版本。

## WP1 BUG-077 批量离队弹窗按钮文案

- `openBatchDeleteModal` 确认按钮文案**参数化**：批量离队弹窗=「确认离队」，批量删除（真删）弹窗=「确认删除」（核查真删弹窗现文案一并归位）。
- 其余弹窗复用处全扫一遍，文案与各自语义逐一对齐。

## WP2 BUG-075 mapAuthError 422 文案

- 「至少 6 位」→「至少 8 位」（与 gotrue GOTRUE_PASSWORD_MIN_LENGTH=8 对齐）；顺手核查全站密码长度文案（登录/注册/改密/提示语）逐处一致。

## WP3 BUG-076 main.css 死选择器清理

- 删 `.guild-bar` 响应式规则 1 条；`.guild-bar-role` 从组选择器列表移除 2 处——**`.guild-member-role`/`.role-badge` 保留**（徽章族仍被用户中心「我的角色」等使用，删除前先核消费点，§1）。
- 零功能零视觉变化，纯死代码清理。

## 验证与纪律

- verify：BUG-077 两弹窗按钮文案断言；BUG-075 文案断言；BUG-076 选择器 grep 零残留+徽章族渲染回归（computed）。
- 回归八组+task36（task37 若已完工则含）全绿清零；版本串 task#37 后递增两壳 16 处；changelog 三条。
- 完工报告送审，**不 commit 不 push**。

## B 表（push 后运营验收，随批交付）

| # | 内容 |
|---|---|
| B1 | 批量离队弹窗按钮显示「确认离队」；批量删除弹窗「确认删除」不变 |
| B2 | 密码不足 8 位提示文案全站一致「至少 8 位」 |
| B3 | 用户中心「我的角色」徽标渲染正常（徽章族零回归） |
