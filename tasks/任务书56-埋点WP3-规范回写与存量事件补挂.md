# 任务书 #56：REQ-141 WP3 埋点收官（规范回写 + 存量事件补挂）

> 执行方：Kimi Code ｜ 单 WP 一次提交 ｜ 密钥：不涉及（验证清理环节需服务器侧 service_role 当次使用，运营当次粘贴、当次使用）｜ 运营侧：commit+push + 真机验收
> 开工前先读 docs/开发规范.md 与 docs/DESIGN.md，本任务书之外不顺手加功能、不顺手重构。

> 前置状态（已实测确认的事实，直接采信）：
> - **#54 采集层已上线销号**：js/track.js 三壳装载；唯一埋点入口 = `window.WBTrack.event(name, props)`（track.js 第 122 行），payload = {event, page, vid, uid, props}，POST /api/track 恒 204 全程静默；uid 由 app.js 授权两行写入 `window.__wbUid`，埋点方无须自行取 uid。
> - **#55 WP1+WP2 已上线销号**：POST /api/analytics/summary 两段鉴权（401 登录 → 403 白名单）；sql/34 analytics_overview 六键聚合，其中 events 键按事件名 TOP 聚合**已在**；数据中心「访问统计」看板四面板可用。当前库内仅 page_view 一类事件，事件 TOP 面板虚位以待。
> - 运营白名单已补录（2026-09-23 真机验收通过）。
> - **候选挂点**（顾问按 master@4b740db3 粗摸，供定位参考，最终以 Code 定点申报为准）：
>   注册 = `CloudSync.signUp` 成功路径（app.js ~1593）；建会 = `CloudSync.createGuild` 成功后（~1719）；入会 = `CloudSync.joinGuildById` 成功后（~1265）；考勤 = activities 保存路径 cloudCrud add/update 双分支（~4547/4555）；装备分配 = `cloudCrud('loots', add/update)` 成功路径（~6316）；心愿 = `CloudSync.saveCloudData('wishlists','add')` 成功路径（~5685）；智能导入 = REQ-048 导入执行体完成路径（~3967 起，importConfirmRoster）。
> - **注意**：心愿单/智能导入写路径走 saveCloudData 直调，属在案技术债（P1 改道 cloudCrud 另案处理）。本任务**只在成功路径上挂埋点，不动写路径本身**。

---

## WP1：规范回写 + 七事件补挂（纯前端 app.js + 文档，无 SQL、无 server.js）

### 需求

1. **规范回写：docs/开发规范.md 新增「埋点纪律」章**
   - 章号顺排（现文档末章之后，预计第七章，以文档实际为准）；全文五条 + 一张事件清单表：
   - **五条纪律**：
     1. 唯一入口：一切埋点经 `WBTrack.event()` → POST /api/track，禁止直连 analytics_events 表、禁止另开通道。
     2. 事件登记制：新增事件须先在本章事件清单表登记（事件名 / 触发语义 / props 键），再施工；未登记事件不得上线。
     3. 触发时机：业务写操作埋点 = 写库确认成功后触发（与第一章 DB-first 同律）；禁止点击即埋、禁止失败也埋；批量操作一次用户动作埋一次（数量进 props），禁止循环逐条埋。
     4. props 纪律：只放数值与枚举口径字段；禁止人名、公会名、装备名、自由文本、uid（uid 顶层字段已有）；拿不到值的键直接省略，**不得为埋点改业务逻辑、不得为埋点新增取数**。
     5. 静默不阻塞：埋点调用不 await、不阻塞业务流、失败静默（恒 204 设计）；改动 track.js 或事件清单须按第五章递增版本串。
   - **事件清单表**（初始 8 行，page_view 为存量登记）：

     | 事件名 | 触发语义 | props |
     |---|---|---|
     | page_view | 页面/页签浏览（存量 #54） | {} |
     | user_register | 注册成功（signUp 成功回调） | {} |
     | guild_create | 建会成功（公会首行落库后） | {} |
     | guild_join | 入会成功（成员关系落库后） | {} |
     | attendance_save | 一次考勤保存成功（一次活动保存一次，与标记人数无关） | {marked: 本次在册人数} |
     | loot_assign | 一次装备分配保存成功（一次分配动作一次） | {count: 本次分配件数} |
     | wishlist_add | 心愿单新增一件成功 | {} |
     | smart_import | 一次智能导入执行完成 | {import_type: 导入类型枚举, rows: 导入行数} |

2. **七事件补挂（js/app.js）**
   - 按上表逐一在成功路径挂 `WBTrack.event(...)`；props 口径与表中一致，拿不到的键省略（如 import_type 分支不可判时只给 rows）。
   - **定点申报**：施工前先在送审回报中列出 7 处挂点（函数名 + 行号 + 一句语义确认）；施工中若发现某挂点与「成功路径」语义有出入（如多分支/提前 return），先申报再动手，不擅自改设计。
   - 埋点行须带一行注释钉来源：`// 任务书 #56：REQ-141 WP3 埋点（事件名见 docs/开发规范.md 埋点纪律章）`。

3. **verify 扩展：新建 scripts/verify-task56.js**（风格沿袭 verify-task55.js）
   - **A 组静态**：规范章锚点存在（「埋点纪律」章题 + 清单表 8 行事件名逐字）；**track.js / server.js / sql/ 零改动断言**（git diff 文件名清单不含）；app.js 中 `WBTrack.event` 恰好 7 处且事件名与清单表逐字一致；版本串 `.72` 三壳计数 15/6/8 + `.71` 在三壳零残留。
   - **B 组浏览器插桩**（沿袭 #54 pvcheck 的 addInitScript 捕获法）：建测试公会「埋点测试会」+ 测试账号一枚，七事件逐一真机触发，断言 payload：事件名逐字、props 键不超出登记键集、登录态事件 uid 非空（user_register 允许 uid=null）；触发过程零 JS 报错。
   - **C 组清零**：测试业务数据（测试公会及其考勤/装备/心愿/成员行）经 UI 删除并复核为零；测试事件行由 Code 在服务器侧按测试 vid + 时间窗删除，REST 复核为零；测试 auth 用户用 service_role 删除并复核。清理输出全量贴回报。

4. **版本串 + changelog + 台账**
   - 三壳版本串 `.71`→`.72` 全量递增（index×15 / decor×6 / data×8，计数不变）。
   - app.js changelog 新增条目 `v3.2.0-task56-analytics-events`：文案要点 = 补挂七类业务事件（注册/建会/入会/考勤保存/装备分配/心愿新增/智能导入），仅用于站点运营统计，不采集人名/公会名等业务内容；docs/开发规范.md 新增「埋点纪律」章。
   - docs/问题与需求清单.md REQ-141 行补记 WP3 段。
   - 本任务书存入 tasks/ 目录随 commit 入库。

### WP1 验收

- verify-task56 全绿（A 组锚点 / B 组七事件插桩 / C 组双清零），输出全量贴回报。
- sha256 纪律：**hash 必须在 diff 生成之后、以最终交付文件复算**，送审回报须附复算命令与输出（前两次误记在案，本次起硬性要求）。
- commit 物料：js/app.js、docs/开发规范.md、docs/问题与需求清单.md、scripts/verify-task56.js、index.html / decor.html / data.html（版本串）、docs/TASK-056-WP1-修改报告.md（新件）、tasks/任务书56 存档（新件）。
- 运营真机：看板四面板无报错（测试事件行已清理，事件 TOP 待真实使用累积属正常）；更新日志条目可见。

---

## 红线

- 不动 js/track.js、server.js、任何 sql/ 文件；不动 #55 看板代码（mdRenderAnalytics / anxState / .anx- 样式）。
- 不顺手修心愿单/导入写路径遗留（P1 在案，另案）；不为埋点改任何业务逻辑。
- 七类之外一律不埋（含登录——登录频次由 page_view 已覆盖）；props 不越登记键集。
- commit 规范：「任务书#56-WP1：…」，描述按【改了什么】【范围】【验证】三段式。
- 发现与本文档冲突的现实，**先报告运营定夺，不擅自改设计**。
