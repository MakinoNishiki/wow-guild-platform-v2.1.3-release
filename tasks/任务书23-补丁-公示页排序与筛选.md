# 任务书 #23-补丁：数据公示页 套装排序 + 三维筛选 + 掉落池筛选补全

> 执行方：Kimi Code ｜ 单一工作包，一个 commit：「任务书#23-补丁：公示页排序与筛选」｜ 密钥：不涉及 ｜ 朋友侧：零操作
> 来源：任务书 #23 运营验收反馈（截图在案）。**开工顺序：排在 #21-补丁合并部署之后**（都动 js/css，避免版本串与文件冲突）。

---

## 修正项①：套装一览排序定稿

现状：套装卡片顺序随机（死亡骑士→德鲁伊→唤魔师→猎人乱序混排）。
要求：**先按职业、再按专精**——同一职业的全部专精连续排完，才进入下一个职业。职业顺序与专精顺序以 game_classes / game_specs 的既有排序字段（或 id/既定常量顺序）为准，全站统一，公示页不另造顺序。

## 修正项②：套装区三维筛选

套装一览增加筛选条：**职业 / 职责 / 专精**（专精选项随职业联动；职责由专精推导，选职责=过滤出该职责全部专精的套装）。筛选条样式与交互习惯跟站内既有筛选条一致。

## 修正项③：团本/大秘境掉落池筛选补全

- 两区在现有「部位/类型 + 装备名关键词」基础上，补**主属性 / 副属性**标签筛选（数据源 = boss_loot / dungeon_loot 的 primary_stats / secondary_stats 数组字段，多标签为 AND 关系）；
- **筛选逻辑与展示结果严格一致**：每个筛选维度单独/组合使用时，结果集必须精确匹配条件（不得出现「选了力量却显示智力装备」「清空筛选后结果未还原」）；
- 大秘境「按 BOSS / 整体池」双视图下筛选均生效；筛选状态在赛季切换后重置为默认。

## 修正项④（排查项）：套装卡片长文本与「件」徽标疑似重叠

验收截图中「猎人·野兽控制」卡片的 2件/4件 徽标与长效果文本疑似重叠/挤压。排查该卡片及全部套装卡片：长文本（≥3 行）下徽标与正文排版不得重叠、不得溢出卡片；如有问题按 DESIGN.md 规范修正（徽标独立列或浮动，正文留足间距）。

## 验收

- 排序断言：套装区前两屏截图，同职业连续、职业序与 game_classes 一致；
- 筛选断言（写进 verify 或浏览器实测输出）：套装三维各选一例 + 掉落池主/副属性各选一例 + 组合筛选一例 + 清空还原一例，结果集与条件逐项核对一致；
- 双视图（按 BOSS / 整体池）筛选生效截图；赛季切换后筛选重置实测；
- 1366×768 与 1920×1080 两档截图；控制台零报错零 404；
- 改 js/css 按纪律递增 index.html 与 data.html 版本号（当前 20260805.4 → 20260805.5）；
- npm test + SEC-001 回归全绿；测试数据自清理复核为零。

### 红线

- 纯公示页（data.html / dataPublic.js / css）改动，不碰主应用、不碰 server.js、不动数据与权限；
- 筛选/排序只读不写；缺数据维度报告，不擅自造数；
- 不动密钥与 .env。
