# 任务书 #14-补丁2（8 项）

> 执行者：Kimi Code。开工铁律生效中。本补丁含多项"报告实测通过但运营环境复现失败"项——验收取证要求升级：**所有前端修复必须附真实 Chrome 截图证据（多状态），并写明运营复测前需 Ctrl+Shift+R 强刷**。

---

## 一、BUG-048 重开（P0）：纯 DPS 职业职责下拉空值

**现象（运营实测）**：导入后图标路径已回填，但法师/术士/猎人等**纯 DPS 职业**专精职责下拉全部空值（战士等混合职业正常）。库内 role 非空率 100%（verify 断言），故为**前端下拉值与库值映射失配**或部分行值异常。

**要求**：
1. dump game_specs 全量 40 行（class, spec, role, icon）贴进报告，逐行核对与英雄榜枚举一致；
2. 核查前端职责下拉 option 值与库 role 值的映射（大小写/trim/枚举词表），纯 DPS 职业与混合职业对比定位；
3. verify 断言从"战士防护=TANK"单条扩为**全量 40 行职责映射断言**（法/术/猎全系 DAMAGE 必须显式覆盖）；
4. 修复后真浏览器逐职业核验 13 职业全部专精下拉显示正确，截图取证。

## 二、BUG-049 重开（P0）：日期 yyyy 在运营 Chrome 仍透出

**现象**：::selection 透明化方案在运营浏览器无效，选中态仍蓝底 yyyy/mm/dd。

**要求**：放弃 selection 补丁式思路，改根治方案——**原生 datetime-edit 文本全状态透明（不设例外），显示职责完全交给遮罩层**；focus/blur/选中/赋值四状态在真实 Chrome 逐态截图取证。若原生选择器面板（点开的日历弹层）受影响，单独说明并保证面板可用。

## 三、REQ-061：自定义日期范围框默认隐藏

考勤记录筛选行的起/止两个日期框为既有"自定义范围"功能，但常驻显示造成困惑。改为：**时间筛选=「全部时间/本赛季」等预设时隐藏两框；仅当选「自定义范围」时显示**（无该选项则在筛选下拉中新增）。隐藏/显示切换真浏览器核验。

## 四、REQ-058 补漏：手动标记徽章同页两制

**现象（运营截图实证）**：同一列表页，"进军奎尔丹纳斯"卡片为新"手动标记 23"文字标签，"孢陨幽境"卡片仍为旧蓝色 ▭1 icon——分支渲染不统一。

**要求**：所有活动卡片徽章**统一一个渲染组件**，旧快照/无快照/回退分支全部走同一文字标签样式；真浏览器核验同页不再两制。

## 五、套装表数据与占位样式整改（对应运营 #4）

1. **dump tier_sets 全表**贴报告（S1/S2 各行实值），说清哪些是运营真实输入、哪些是测试残留；
2. **清除测试垃圾行**（如战士行"123/321"），并复查 9 张主数据表无同类残留——正式库清场纪律再次强调；
3. 运营上轮在 S2 输入的套装名（战士"翡翠督军的统御"、法师"始源魔网守卫的装束"）若因 sql/11 迁移被清：**报告说明原因并给出恢复方案**（能恢复则恢复，不能则明示由运营重录）；
4. 灰字"占位联动"样式整改：placeholder 必须与实值一眼可辨（更浅颜色+斜体+"如："前缀），不得伪装成已有数据。

## 六、REQ-059：装备特效文本游戏绿

掉落池展示与装备分配行的特效文本颜色改为游戏内特效亮绿（参考魔兽 tooltip 绿字，建议 #1eff00 或规范色板中最接近值），字体大小/行距保持现有层级。真浏览器核验明暗两种底色的可读性。

## 七、REQ-060：部位↔类型联动（映射表已给定，照此实现）

部位选定后，类型下拉**只保留合法项**（不合法已选值自动清空）；批量录入按同规则校验、坏行报错指出行号：

| 部位 | 合法类型 |
|---|---|
| 头部/肩部/胸部/手腕/手部/腰部/腿部/脚部 | 板甲/锁甲/皮甲/布甲 |
| 背部 | 披风 |
| 颈部 | 项链 |
| 手指 | 戒指 |
| 饰品 | 饰品 |
| 武器 | 单手剑/双手剑/单手斧/双手斧/单手锤/双手锤/匕首/拳套/长柄武器/法杖/弓/枪/弩/魔杖/战刃 |
| 副手 | 盾牌/副手物品 |

类型下拉同步按上表细分武器单手/双手；保留手动录入兜底（手输非法组合时保存前弹提示确认，不硬拦——兜底优先）。

## 八、REQ-057 按方案 B 执行（运营已拍板）

装备库选择列表**只保留数据中心 boss_loot 录入的装备**；103 件内置英文装备全部移出选择列表；历史引用（loot_records 3 条、心愿单 6 条英文装备）保底显示、不删、不报错。装备分配/心愿单的装备数据源正式切换为 boss_loot 主数据，报告写明切换点与回退方案。

---

## 验证与交付

- verify-master-data.js 同步扩断言（全量 40 行职责映射）；31/31 基线+新增断言全绿；verify-authz.js 34/34；
- 全部前端修复附真实 Chrome 多状态截图证据；
- 台账：BUG-048/049 重开备注 + FIXED-030/031（若改方案）、REQ-057 方案变更备注、REQ-059/060/061 登记；changelog 四维补录；
- **不提交 git**，运营验收后与 #14 全线一并提交。
