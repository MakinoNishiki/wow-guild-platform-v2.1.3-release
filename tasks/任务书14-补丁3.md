# 任务书 #14-补丁3（6 项）

> 执行者：Kimi Code。开工铁律生效中。本补丁含 P0 主链路一条（BUG-051），优先修。

---

## 一、BUG-051（P0）：装备库选择面板「确认选择」无法交互

**现象（运营实测）**：REQ-057 方案 B 切换后，装备库选择面板可选中装备（高亮正常），但点「确认选择」**无任何反应**，装备无法进入分配行。

**排查方向**：picker 数据源切换 getMasterLootItems() 后，确认流程引用的字段（id/itemId/名称映射）与 boss_loot 新数据结构不匹配；或确认 handler 存在绑定异常。

**修复+顺带三项**：
1. 确认链路修复，真浏览器实测全链路：选中装备 → 确认选择 → 装备出现在分配面板对应行；
2. 装备卡片特效显示"装备：装备："重复前缀——显示层去重（录入文本已含"装备："时不再加），历史数据同步兼容；
3. boss_loot 漏网测试垃圾（"zzz"行）清除，并**全量复查 9 张主数据表**，报告贴清场后行数。

## 二、专精表格列对齐（运营 #1 遗留）

数据中心专精区块：13 个职业的表格**列宽不统一**（法/术/猎等职责列横向偏移）。改 table-layout:fixed 或固定列宽，全职业 ID/专精/职责/图标路径四列左对齐同一位置，1366×768 与宽屏双档核验。
（说明：职责枚举 TANK/HEALER/DAMAGE 仅后台显示，消费端考勤/成员/导入已是中文标签，运营确认不改。）

## 三、REQ-062：活动「团号」字段

1. 活动数据模型加 `team_label text`（可空）；创建/编辑活动面板加「团号」输入框（占位"如 1 或 战战子"）；
2. 考勤活动卡片徽章显示规则：填纯数字 → 显示「N 团」；填文字 → 显示「团号：X」；空 → 不显示；
3. **徽章区布局重做**：文字标签不再被裁剪（此前"1 团"被裁成 ▭1 的根因），与手动标记徽章/WCL 复盘按钮排列协调；
4. **旧活动记录兼容**：无 team_label 的老记录正常渲染，不得出现乱码/错位（运营已删一条旧记录，但不得要求删数据解决）。

## 四、套装名物化 + 填写限制解除（运营 #5）

1. **物化**：tier_sets 中已录套装名的职业，把套装名**真实写入该职业全部专精行**（S1/S2 分别处理），不再依赖占位联动显示；
2. **解除绑定**：取消"套装名为空则 2 件/4 件效果不可填"的限制，效果可独立填写保存；
3. **缺失补齐**：S2 尚未录套装名的职业，等运营提供官方名单链接后按名单补齐（运营另行给到，本项可先交付前两条）。

## 五、日期 Ctrl+A 全选态 yyyy 收尾（P2，不阻塞）

统一排查全站日期输入框（新增版本/赛季的开启与结束日期、团本开放日期等）：Ctrl+A 全选时原生 yyyy 仍有透出。尝试根治；若浏览器限制确实无法覆盖，报告说明技术结论即可，**不阻塞交付**（运营已确认后台自用可接受）。

## 六、下拉「其他」项合并（运营 #6）

部位/类型下拉选项中删除「其他」，保留「其他（手动输入）」一项；历史数据已存"其他"值的显示兼容。

---

## 验证与交付

- BUG-051 全链路真浏览器取证（选中→确认→入行截图）；REQ-062 三态（数字/文字/空）+旧记录兼容截图；
- verify-master-data.js 34/34 基线全绿；verify-authz.js 34/34；node --check 全过；
- 台账：BUG-051+FIXED-032、REQ-062 登记；changelog 四维补录；
- **不提交 git**，运营验收后与 #14 全线一并提交。
