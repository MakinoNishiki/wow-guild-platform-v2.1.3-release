# 任务书 #23-补丁7：筛选条左缘对齐收口（组头左对齐 + misc 行回左线）

> 执行方：Kimi Code ｜ 单工作包、一个 commit ｜ 密钥：不涉及 ｜ 朋友侧：零操作
> 需求登记：**BUG-055（复开，修法变更）** ｜ 状态说明：补丁6 把 misc 行缩进贴 chip 列系顾问误读运营意图；v20260807.20 运营验收 C1 失败项（截图红线在案），运营定稿**左缘一条竖线**。
> 规范同步：本补丁含两份规范文档条款修订（右对齐→左对齐），与代码同一 commit 入库（§10 规范先行，同修不拖）。
>
> 前置状态（运营拍板 + 顾问实测坐标，直接采信）：
> - 组头四标签现为 56px 固定盒**右对齐**：两字标签（部位/类型）文本左缘比三字标签（主属性/副属性）内缩 12px，左缘不成线（运营：「部位/类型是同样的问题」）；
> - misc 行勾选框现贴 chip 列基线 217（补丁6 修法）；搜索框行左缘=组头盒左缘 153；
> - **定稿**：组头四标签、misc 勾选框、搜索框三者**左缘对齐于组头盒左缘，一条竖线**；chip 列（217）与行内子类组标（护甲/武器等）一律不动。

---

## WP1：左缘对齐收口（css/data-public.css + verify 断言同步 + 两份规范同修）

### 需求

1. **组头左对齐**：`.dp-chip-label` text-align right → **left**；56px 固定盒宽保持、12px 次级色、24px 行高与首行 chip 同基线等其余规格不动——四标签文本左缘同线。
2. **misc 行回左线**：`.dp-misc-row` 撤销补丁6 的 64px 缩进（padding-left → 0），勾选框左缘回组头盒左缘；开关默认关、问号逐字文案、交互一律不变。
3. **不动项**：搜索框行（已在左线）、chip 列 217 基线、行内子类组标（护甲/武器/首饰等）样式与对齐。
4. **verify 断言同步**：verify-task23-patch4.js 组头断言 `textAlign==='right'` 改 `'left'`；补丁6 新增的 misc 217 基线断言同步改为左缘同线断言；其余断言不动。三脚本（patch/patch3/patch4）全量重跑全绿。
5. **规范同修**（同一 commit）：
   - docs/魔兽管家UI设计规范v2.md §4.3：「组头 56px 右对齐」→「组头 56px 左对齐」，并补「组头/排除杂项行/搜索框行左缘同线」；
   - docs/公示页筛选系统设计规范.md §2 布局句与 §3 尺寸表同口径修订（右对齐→左对齐 + 左缘同线条款）。

### WP1 验收

- 1366×768 与 1920×1080 两档截图：组头四标签左缘一条竖线、misc 勾选框在同线、chip 列 217 不动（红线标注留证）；
- 三 verify 重跑全绿（断言同步后）；对数回归：爆击 166 / 力量 101 / 组合 45 / 清空 409（库内漂移以实测为准附注）；
- 版本串按合并时点递增（index.html 与 data.html 双头）；控制台零报错零 404；npm test + SEC-001 回归；零测试数据创建；
- commit：「任务书#23-补丁7：筛选条左缘对齐收口（组头左对齐+misc回左线+规范同修）」，三段式；不 push。

### 红线

- 只动 data-public.css（如必需可碰 dataPublic.js 渲染类名但预期零 JS 改动）、两份规范文档、verify-task23-patch4.js 断言；
- 不碰主应用、server.js、数据与权限；chip 几何规格不动；不顺手加功能；
- 不动密钥与 .env；发现与本文档冲突先报告运营定夺。
