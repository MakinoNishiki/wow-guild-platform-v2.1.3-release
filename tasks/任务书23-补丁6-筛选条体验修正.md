# 任务书 #23-补丁6：公示页体验修正（chip 光学居中 / 排除杂项行对齐 / 搜索清除钮 / 套装一览折叠）

> 执行方：Kimi Code ｜ 单工作包 ｜ 密钥：不涉及 ｜ 朋友侧：零操作
> 需求登记：**BUG-054 / BUG-055 / REQ-084 / REQ-085** ｜ 来源：v20260805.18 运营逐项验收（2026-08-07）体验反馈三项 + 运营追加套装一览折叠一项，顾问生产实测已定案，证据如下「前置状态」。
> 铁律#4：本补丁为验收发现转化，根因与修法均已经顾问 A/B 实测定稿，无需重新审计，直接施工。
>
> 前置状态（顾问 2026-08-07 生产实测，直接采信）：
> - **BUG-054（chip 文本光学不居中）**：chip 高 24px、`line-height: normal`（行盒≈17px）、flex `align-items:center`，几何上缝=下缝=3.5px——但中文无西文降部，字形在 17px 行盒内偏上，视觉偏上约 1.5px（运营截图 4 反馈在案）。A/B 对照实验证明：`.dp-chip{line-height:1}` 后中文字形光学居中（对照图 backup 留证）。
> - **BUG-055（排除杂项行出列）**：四个组头（部位/类型/主属性/副属性）实测完全同列（左 153/右 209/宽 56）；排除杂项行勾选框左缘=153（与组头列左缘齐）、文字溢出组头列右缘 209 闯入 chip 区，视觉上像「第五个标签但不在列」。主/副属性行 chip 列起点=217（=组头 56+组间 8）。
> - **REQ-084（搜索框一键清空）**：`#dpSearch` 宽 1060px、`padding-right:14px`、无清除钮（运营截图 6 诉求：长文本键入后反复 backspace 体验差）。既有先例：REQ-077 英雄榜输入框清空钮（任务书 #24-补丁），复用同款交互。
> - A1 分诊（不立项，记录在案）：验收截图 Console 的「CSP blocks eval」报错与「卸载事件侦听器弃用」告警**均源自浏览器扩展**（生产两页响应头无 CSP、全部线上 JS 零 eval/new Function；content.js 即扩展注入脚本），无痕窗口打开即无。「No label associated with a form field ×78」为我方表单无障碍既存欠账（警告级），已登记 BUG-056 排队，不在本补丁。

---

## WP1：筛选条体验三项（纯前端：css/data-public.css + js/dataPublic.js + data.html）

### 需求

1. ** BUG-054 chip 光学居中**
   - `.dp-chip` 增加 `line-height: 1`（flex 居中体系不变），中文字形光学居中；
   - 覆盖筛选条全部 chip（含「全部」chip 与子类分隔后的各组）；几何规格不动（24px 高 / 12px 圆角 / 12px 字 / padding 不变）；
   - 副检查：`data-public.css` 内若有其他 24px chip 变体同条款处理；`.tag` 徽标族（20px，补丁5 并轨件）本补丁**不动**，走查截图附注观感，若同样偏上在修改报告提出，另案处理。
2. ** BUG-055 排除杂项行对齐**
   - `.dp-misc-row` 内容（勾选框+文字+问号帮助）整体缩进：左留白 = 组头列宽 56px + 组间 8px（即内容左缘与主/副属性行 chip 列起点 217 对齐）；
   - 开关默认关、问号逐字文案、交互行为一律不变，只动对齐。
3. **REQ-084 搜索框清除钮**
   - `#dpSearch` 右侧内置清除钮（×）：有输入内容才显示、点击清空并还原全集（当前口径 409）、键盘 Esc 同效、`aria-label="清除搜索"`；
   - 复用 REQ-077 英雄榜清空钮既有样式与显隐逻辑（主应用同款），输入框 `padding-right` 让位（约 36px），不产生布局抖动；
   - 清除后焦点留在输入框，toast 不需要（无感清除）。
4. **REQ-085 套装一览区块折叠**
   - 现状实测（顾问 2026-08-07）：套装一览头为纯 `.dp-section-title`（不可折叠），40 张套装卡平铺单一 `.dp-tiers` 网格，**无职业分组容器**；其余两区块均为 `.dp-section-head.dp-section-collapser`；
   - 套装一览标题升级为与团本/大秘境区块**同构折叠头**（`.dp-section-head.dp-section-collapser`，data-collapse 取 `sec:tiers` 类唯一键）：折叠控件同规格（热区 ≥24×24px、描边 2px、常态次要色、悬浮提亮）；点击收起/展开整个 `.dp-tiers` 网格，与另两区块折叠互不干扰；默认全展开；
   - 折叠记忆纳入既有 sessionStorage 机制（补丁4 口径：切赛季往返保持）；
   - 套装三维筛选（职业/职责/专精）与折叠互不干扰：区块折叠时筛选状态保留，展开后筛选结果不变；
   - **取舍说明**：本项只做区块级。职业级折叠（13 职业分组头）需先把平铺网格按职业分组成容器，牵动 REQ-074 排序/三维筛选联动，属结构性改动——不做顺手项，需要时另立任务书。

### WP1 验收

- verify-task23-patch / patch3 / patch4 三脚本全量重跑全绿（patch4 的三级折叠断言不得因新区块折叠头回归）；
- chip 居中对对照截图（1366 档，同顾问 A/B 手法）；排除杂项行与 chip 列对齐截图（标注 217 基线）；清除钮三态截图（无内容不显示 / 有内容显示 / 点击还原 409）；
- 套装一览折叠：折叠/展开实测 + 控件热区 ≥24px 断言 + 切赛季往返记忆实测 + 三区块折叠互不影响实测，两态截图；
- 对数回归：爆击 166 / 力量 101 / 组合 45 / 清空 409（库内若有漂移以实测为准并附注）；
- 版本串按合并时点递增（index.html 与 data.html 双头）；控制台零报错零 404；npm test + SEC-001 回归；零测试数据创建；
- commit：「任务书#23-补丁6：筛选条体验修正（chip光学居中/排除杂项行对齐/搜索清除钮）」，三段式；不 push。

### 红线

- 只动 data.html / dataPublic.js / data-public.css；不碰主应用、server.js、数据与权限；
- .tag 徽标族不动；BUG-056（label 无障碍）不动；封存口径不变；
- 不动密钥与 .env；发现与本文档冲突先报告运营定夺。
