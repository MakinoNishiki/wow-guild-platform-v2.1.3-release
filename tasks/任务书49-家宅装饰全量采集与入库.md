# 任务书 #49：家宅装饰全量采集与入库（WoWButlerDecor v0.2 + decor 目录表）

> 执行方：Kimi Code ｜ 两个工作包，严格按顺序做、分开提交（WP1 真机采集验证通过后才开始 WP2）｜ 密钥：WP2 涉及（service_role 只在服务端环境变量，迁移经 SSH 按既有纪律）｜ 运营侧：真机三跑采集 + 上传 SavedVariables + 执行迁移
> **开工前先读《开发规范》（references/dev-rules.md），本任务书外不顺手加功能。**
>
> 前置状态（已实测确认的事实，直接采信）：
> - 任务书 #48 + 补丁已 commit 并 push（2026-09-16）：`addon/WoWButlerDecor/`（toc + 单 lua，v0.1.1），探针切片已完成。
> - **真机二跑判定绿（run 20260916-203500，顾问判读在案）**：国服 Housing API 存活；搜索器为 userdata、方法链全通（probedMethods 5 个）；异步容错链生效（两次 n=0 后第三次 n=2062）；`GetCatalogSearchResults` 返回 12.0.5 新形态 `{recordID, entryType, variantIdentifier}`，`toEntryID` 双形态兼容必要且正确。
> - **totalCount=2062 件，逐条取字段零失败，0.53s**；中文名/sourceText 全净无乱码；sourceText 非空 96.4%（1987/2062）；icon=fileID 2021 件、缺 41；quality/size 全非空。
> - 样本证实每件装饰带 `dataTagsByID` 中文标签（资料片/尺寸/风格/颜色/种族/专业/搜索词），是图鉴筛选的直接资产。
> - 2062 低于 WoWDB 3158 属口径差（游戏内目录可见集 vs datamining 全量含未发布/隐藏条目），运营认可「玩家可见真相优先」，不构成阻塞。
> - 当前探针只落盘 30 条 samples，**本任务书要解决的缺陷：①全量 2062 件未落盘；②数据未转换成可入库格式；③无 decor 目录表**。

---

## WP1：插件 v0.2 全量落盘（纯插件：toc + 单 lua）

### 需求

1. **`/wbd scan` 升级为全量采集**
   - 完整复用 v0.1.1 已验证链路：CreateCatalogSearcher → 类型守卫（table|userdata）→ 能力探测 → 容错枚举链（空→SetSearchText("")→C_Timer.After 重试×3）→ `toEntryID` 双形态解包 → 逐条 pcall GetCatalogEntryInfo。这段逻辑**只准调用、不准改写**。
   - 采集循环内，在现有 stats/samples 之外，把每件装饰的**目录字段集**追加进 `WoWButlerDecorDB.items`（数组，下标 1..N）：
     - 保留：`recordID`、`entryType`、`itemID`、`name`、`iconTexture`、`asset`、`uiModelSceneID`、`quality`、`size`、`placementCost`、`categoryIDs`、`subcategoryIDs`、`sourceText`（原始带格式串，不清洗）、`dataTagsByID`、`isAllowedIndoors`、`isAllowedOutdoors`、`canCustomize`、`firstAcquisitionBonus`。
     - **剔除玩家态字段**（个人拥有/摆放数据，对目录无价值）：`totalNumStored`、`totalNumPlaced`、`numPlaced`、`quantity`、`showQuantity`、`remainingRedeemable`、`destroyableInstanceCount`、`dyeSlots`、`entryID`（与 recordID+entryType 冗余）。
   - stats / samples(30) / diag / runMeta 原样保留（回归对照用）；runMeta 增加 `dataset = "full"`。
   - 完成后黄字一行：`[WBD] 全量采集 N 件已暂存，/reload 后写入 SavedVariables（文件约 X MB）`；X 用 items 序列化近似长度估算即可，不准不追究。
   - 落盘前清空上一轮 `WoWButlerDecorDB.items`（防重跑叠加）。

2. **版本号双同步**：toc `## Version: 0.2.0` + lua `ADDON_VERSION = "0.2.0"`。

3. **纯只读红线不变**：禁用任何写类 Housing API；全部调用 pcall 包裹；失败红字 + diag 不静默。

### WP1 验收

- luaparse（或等价）语法 OK，块配对复核；
- 对照 v0.1.1 diff 评审：枚举/容错/diag 逻辑零改动，新增仅 items 落盘与版本号；
- 运营真机三跑（干净态纪律：先 `/reload`，跑完 `/reload` 落盘，再复制 `WTF/Account/<账号>/SavedVariables/WoWButlerDecor.lua` 上传顾问）；
- 顾问 lupa 全真解析复核：items 件数 = totalCount = 2062±50、字段集齐、name/sourceText 中文无乱码、无玩家态字段混入。

---

## WP2：转换脚本 + decor 目录表 + 种子入库（scripts + sql 迁移 + 服务器执行）

> 开工前提：WP1 验收通过、顾问已用真机 SavedVariables 验证字段集。
> **先侦察再定案**：WP2 动手前先出半页侦察小结（写进送审回报），确认三点：①`sql/` 目录现有迁移编号最大值与文件命名风格；②现有掉落库表（boss_loot/dungeon_loot）的字段风格（数组/JSON 字段用的是 jsonb 还是数组类型、主键风格）；③repo 内是否已有 scripts/ 类工具目录。下述设计为**定案倾向**，与现状冲突的按现状风格调整并在送审回报中说明；冲突大到需要改设计的，先报告运营定夺。

### 需求

1. **转换脚本 `scripts/decor/convert_decor.py`**（Python + lupa，与数据线作业规范钦定工具一致）
   - 输入：WoWButlerDecor SavedVariables 文件路径；用 lupa `LuaRuntime` 全真解析，禁止正则/文本猜测。
   - 输出两份：
     - `decor_catalog.json`：全量 2062 件的字典数组（调试用/留档用）；
     - `decor_seed.sql`：单事务种子文件，`INSERT ... ON CONFLICT (record_id) DO UPDATE`——**匹配键 = record_id 主键，天然幂等，重跑不插重**（数据线订正前置自查的匹配键问题在此从源头规避）。
   - 字段映射规则：Lua 字段 → snake_case 列；`categoryIDs/subcategoryIDs` → jsonb 数组；`dataTagsByID` → jsonb 对象（键为 tagID 字符串，值为中文标签）；布尔/数值直映射；`sourceText` 原样存 `source_text`（**本批不做结构化解析**——raw 保留，图鉴页设计时再定解析规则，免重采）。
   - 脚本头注释写明用法与依赖（`pip install lupa`）。

2. **schema 迁移 `sql/<下一版本号>_decor_catalog.sql`**（含文件头回滚注释 `DROP TABLE decor_catalog;`）
   - 表名倾向 `decor_catalog`（侦察后定），列设计倾向：
     ```sql
     record_id        integer PRIMARY KEY,
     entry_type       smallint NOT NULL DEFAULT 1,
     item_id          integer,
     name             text NOT NULL,
     icon_file_id     bigint,
     asset_id         bigint,
     model_scene_id   integer,
     quality          smallint,
     size             smallint,
     placement_cost   smallint,
     category_ids     jsonb,
     subcategory_ids  jsonb,
     source_text      text,
     tags             jsonb,
     indoors          boolean,
     outdoors         boolean,
     can_customize    boolean,
     first_acquisition_bonus smallint,
     client_version   text,      -- 采集客户端版本，如 12.1.0
     run_id           text,      -- 采集批次，如 20260916-203500
     collected_at     timestamptz DEFAULT now()
     ```
   - 本表为**公开目录数据**（无公会维度、无用户数据），RLS 策略按现有公开表（如 boss_loot 公示页读取路径）同风格处理，侦察小结里写明现状做法再沿袭。

3. **执行迁移 + 种子**（运营侧 SSH，迁移纪律全套）：备份 → docker exec（supabase_admin）→ 单事务执行迁移 → 执行种子 → `NOTIFY pgrst, 'reload schema';` → REST 复核。

4. **明确不做**（防顺手加功能）：图标文件提取不做（icon_file_id 已存，后续专项）；sourceText 结构化解析不做；前端页面不做。

### WP2 验收

- 侦察小结在送审回报中，三点问题有明确答案；
- 迁移执行后 REST 复核（顾问终审直连复核，不凭送审口径）：
  - `select count(*)` = 2062（与采集件数一致）；
  - 抽查 3 条逐字段对齐采集原件：`record_id=28350`（暴雪嘉年华门垫，source_text 含「商城」「世界商人」）、`record_id=27973`（月溪镇旧式夜景窗，双商人双地区）、`record_id=27043`（"受枷者的狂怒"壁画，掉落来源）；
  - tags jsonb 可读（如 28350 含「至暗之夜」「小号」「蓝色」）；
  - **幂等验证**：种子 SQL 重跑一次，count 不变、无报错；
  - 零触碰清单：boss_loot/dungeon_loot 等现有表行数前后不变。

---

## 红线

- 不动任务书 #48（v0.1.1）已验证的枚举/容错/diag 逻辑；不顺手重构插件其他部分；
- schema 变更只走 `sql/` 带版本号增量 SQL，禁止直接改库；
- 不动任何密钥与 .env；不 commit 不 push（等顾问终审出物料）；
- commit 规范：「任务书#49-WP1：…」「任务书#49-WP2：…」，描述按【改了什么】【范围】【验证】三段式；
- 发现与本文档冲突的现实（如字段在真机消失、表名冲突），**先报告运营定夺，不擅自改设计**。

---

## 运营验收步骤

1. WP1 commit 后：游戏内勾好插件 → `/reload` → `/wbd scan` → 看到黄字「全量采集 N 件已暂存」→ `/reload` → 复制 SavedVariables 发顾问；
2. 顾问判绿后转 WP2；Code 交付脚本+SQL 送审 → 顾问终审 → commit 物料；
3. 运营按迁移纪律执行 SQL（备份→执行→reload schema）；
4. 顾问 REST 复核出开核结果表 → 运营验收 → 台账签发。
