# 任务书 #28：公示页改版 V2（更名「副本掉落」/过滤器重构/卡片排版/视图 tab/双壳嵌入/星标数据链）

> 执行方：Kimi Code ｜ 五个工作包 WP1→WP5 串行、分开提交 ｜ 密钥：WP1 数据回填走运营私聊通道 ｜ 朋友侧：零操作
> 需求登记：**REQ-086**（台账行随下次统一更新入库）
> 来源：《魔兽管家数据公示页改版 V2》产品需求文档（运营 2026-08-07），顾问评审 + 生产侦察定案，三项拍板在案：**双壳并存 / 视图 tab 并存 / 星标连数据管道一起做**。
> 规范状态：《公示页筛选系统设计规范》v1.0 将被 WP2 重构推翻——WP2 交付物含**筛选规范 v2.0 落稿**（层级分类+来源+搜索置顶+重置），v1.0 届时标注废止；UI 规范 v2 增补（§4.11 属性色板/金星变体/视图 tab 族/§6 fade 条款）已先行入库，施工以其为准。
> ⚠️ 硬时间点：**8.13 S2 翻牌**——WP1 转换器输出格式须于 8.12 前冻结并通知运营，S2 录入按新格式执行，不得中途再改。
>
> 前置状态（顾问 2026-08-07 生产侦察，直接采信）：
> - 星标数值链现状：插件导出（1.0.4）→ wjdc_convert.py → 库内 `primary_stats`/`secondary_stats` **仅存属性名数组，全链路无数值**（实锤：库样例与转换器源码均无数值字段）；星标比较（爆击+300 vs 急速+100）须先建数值链；
> - 删杂项口径：当前赛季全集 409 行 → 删 `slot=杂项` 68 行（装饰27/垃圾22/坐骑3/配方类12/其它4）= **新基线 341 行**；**爆击 166/力量 101/组合 45 对数不变**（杂项不含这些属性）；
> - 双壳拍板：主应用内嵌 tab + data.html 免登录薄壳保留（同一渲染模块，两壳复用），匿名公开访问不死；
> - 视图 tab 拍板：「全部/团本掉落池/大秘境掉落池/套装一览」四 tab，「全部」= 现长页（保留跨来源组合筛选），单区 tab 聚焦短页。

---

## WP1：星标数据链（插件 1.0.5 + 转换器 + sql/19 + 回填）

### 需求

1. **插件 1.0.5（addon/WoWButlerExporter）**：掉落导出增加主/副属性**数值**采集——优先 `GetItemStats(itemLink)`（返回属性数值表）；不可用则回退 tooltip 解析。输出字段：`primary_values` / `secondary_values`（属性名→数值，如 `{爆击=300, 急速=100}`）。原有字段格式不变（向后兼容）。
2. **转换器（scripts/wjdc_convert.py）**：透传数值字段进入库 JSON；旧格式文件（无数值）转换不报错、数值列留空。
3. **sql/19**：`boss_loot`、`dungeon_loot` 各加 `secondary_values jsonb null`、`primary_values jsonb null`（回滚注释 + NOTIFY pgrst，幂等）。
4. **回填**：运营游戏内 `/wjdc all` 重跑当前赛季 → 转换器重新转换 → 入库覆盖（数值列写入）。运营配合动作：上号跑宏、把导出文件给执行方。

### WP1 验收

- 插件导出文件含数值字段（样例贴修改报告）；转换器新旧两格式文件均转换通过；
- sql/19 迁移输出 + REST 复核：当前赛季装备 `secondary_values` 非空率 ≥95%（玩具/杂项类可空），抽查 5 件与游戏内数值一致（运营截图对照）；
- **转换器输出格式冻结声明**（8.13 S2 翻牌按此格式）；
- 测试数据自清理复核为零；迁移可回滚。

---

## WP2：过滤器重构 + 搜索置顶 + 重置 + 动画（纯前端：公示页三文件）

### 需求

1. **删杂项**：数据层排除 `slot=杂项` 全部行（装饰/坐骑/玩具/幻化/配方/消耗品/垃圾等），页面不再出现杂项卡片；**删除「排除杂项物品」开关与问号帮助**（功能失去存在意义）；新对数基线 341（爆击 166/力量 101/组合 45 不变）。
2. **部位+类型 合并为「分类」层级筛选**：
   - 一级 tab：部位 / 武器 / 护甲 / 主属性 / 副属性 / 来源（六个）；
   - 二级值 chips（点一级 tab 展开其值域，组内 OR、跨组 AND 语义不变）：
     - 部位：头部/肩部/胸部/腕部/手部/腰部/腿部/脚部/背部/颈部/手指；
     - 武器：单手/双手/远程/主手/副手；
     - 护甲：板甲/锁甲/皮甲/布甲/盾牌；
     - 主属性：力量/敏捷/智力；副属性：爆击/急速/精通/全能；
     - **来源（新维度）**：团本（梦境裂隙/虚影尖塔/进军奎尔丹纳斯/孢陨幽境）+ 秘境（艾杰斯亚学院/魔导师平台/迈萨拉洞窟/节点希纳斯/风行者之塔/萨隆矿坑/执政团之座/通天峰）——值域按赛季数据驱动，不得写死；
   - **数据匹配映射表**（进任务书附录与代码注释，不上界面）：远程=弓/枪械/弩（双手不含远程）；单手=单手剑/单手锤/单手斧/匕首/拳套/战刃；双手=双手剑/双手锤/双手斧/长柄武器/法杖；盾牌归护甲组。映射外新值归「其它」并打印控制台警告（不放逐、不静默）。
3. **搜索框上移至筛选条最上方**（原最下行上调，样式不变，清除钮保留）。
4. **「重置」按钮**：筛选区右上角，点击清空全部已选（分类六组 + 关键词）还原 341，样式=次级按钮。
5. **筛选动画**：筛选/搜索触发卡片重渲染时，卡片入场 fade+轻微上浮（150–250ms ease-out，只动 transform/opacity，尊重 prefers-reduced-motion）；出场不做（直接重渲染）。
6. **筛选规范 v2.0 落稿**：本 WP 同步产出 `docs/公示页筛选系统设计规范.md` 全文替换（层级分类+来源+搜索置顶+重置+删杂项口径+动画规格），文首标注「v2.0（2026-08）取代 v1.0」。

### WP2 验收

- 杂项卡片零存在断言（页面无 slot=杂项 渲染）；「排除杂项」开关与问号 DOM 不存在；
- 六组一级 tab + 值域联动逐项实测；来源按赛季驱动（切 S2 值域随之变化）；组内 OR/跨组 AND、筛选与结果严格一致（341 基线逐项对数）；
- 映射表断言：单手/双手/远程/护甲各抽代表值归组正确（含弩归远程、盾牌归护甲、模板外归其它）；
- 搜索置顶布局 + 清除钮不回归；重置按钮一键还原 341；
- 动画两态截图/录屏（fade 入场、reduced-motion 下禁用）；筛选规范 v2.0 文档入库；
- 三 verify 脚本锚点同步重写（筛选条 DOM 全变）+ 全绿；版本串递增双头；零报错零 404。

---

## WP3：装备详情展开（数据层公开 RPC 化 + 卡片详情覆盖层）

> 回写说明（2026-08-08）：本节原文案「卡片排版 + 主属性彩色 + 副属性星标」为旧版 WP 划分，已被台账 REQ-086 最新划分取代（WP3 装备详情展开 / WP4 排版留白对比度 / WP5 英雄榜筛选）；旧文案中的排版/星标内容不属本 WP，由运营另行安排。本节按六案裁定（顾问 2026-08-08 拍板）回写。

### 需求

1. **数据层公开 RPC 化（sql/21）**：boss_loot/dungeon_loot 两表读取收口为公开 RPC `get_public_loot_detail()`——两表合并 + 实例（团本/巢穴/大秘境）/BOSS 联查预组装、字段白名单输出（不含 official_item_id 等内部列）、杂项排除下沉服务端（`slot<>'杂项'`，裁定③）；security invoker 不抬权、grant anon+authenticated、幂等 + NOTIFY pgrst；anon 两表直读权限本期不动（裁定④）；执行纪律同 sql/19/20（备份→执行→NOTIFY→REST 复核，运营/执行方通道）。
2. **卡片点击展开详情覆盖层**（UI 规范 §4.4 详情卡变体）：全卡点击切换，全页单开互斥，再点/点外/Esc 收起；字段行序：实例来源 → BOSS（整体池兜底）→ 掉落难度 → 套装关联 → 主属性数值 → 副属性数值 → 特效全文；**无数据的行整体隐藏**。
3. **六案裁定口径**：①掉落难度整行隐藏（tiers 全 null，REQ-087 复活录入后自动显示，代码不改）；②套装关联有则显示无则不显，不建映射（仅套装兑换物显示「可兑换本赛季套装（详见套装一览）」）；③杂项下沉服务端确认；④anon 直读不动确认；⑤hover 特效快读路径不变，点击进详情层（特效全文在详情层完整可读），移动端原 .expanded 点击路径并入详情层；⑥F1（特效覆盖层 max-height/padding 过渡）注册规范 §6 例外（限定该组件，写明理由与日期）。
4. **规范增补随施工同批**（评审闸 §10）：§4.4 详情卡变体（F2 全卡引导形态）+ §6 详情层动画注册与 F1 例外。
5. **动画**在 §6 白名单内：详情层 200ms ease-out 只动 transform/opacity，reduced-motion 降级。

### WP3 验收

- sql/21 迁移输出 + anon REST 复核：343 行（跨赛季全集=411−68）/字段白名单净/杂项零行/S1 视图 341/S2 两件归属烈毒之渊；
- 详情层逐项真浏览器实测：六字段行、无数据行隐藏、单开互斥、再点/点外/Esc 收起、套装兑换物关联行、S2 数据；hover 特效快读零回归（补丁4 断言重跑）；
- 动画两态（含 reduced-motion 降级）；两档截图；三 verify + npm test + SEC-001 全绿；版本串递增双头；零报错零 404。

---

## WP4：视图 tab（全部/团本/秘境/套装）

### 需求

1. 掉落池顶部加视图 tab 族（复用并扩展现有「按BOSS/整体池」toggle 形态，规范 §4.4 注册）：**全部（默认）/ 团本掉落池 / 大秘境掉落池 / 套装一览**；
2. 「全部」= 现长页三区块堆叠（跨来源组合筛选可用）；单区 tab = 只渲染对应区块（短页聚焦）；
3. 过滤器与赛季切换在四个 tab 下照常生效；筛选状态跨 tab 保留；折叠记忆不回归；
4. tab 态计入 sessionStorage（刷新保持）。

### WP4 验收

- 四 tab 切换渲染正确（全部=三区块堆叠、单区=仅本区块）；「全部」tab 下跨来源组合筛选实测（团本+秘境同筛）；
- 筛选/赛季/折叠记忆跨 tab 保留；sessionStorage 刷新保持；两档截图；零报错零 404；版本串递增。

---

## WP5：双壳嵌入 + 更名「副本掉落」

### 需求

1. **模块参数化**：js/dataPublic.js 改造为可嵌入模块——暴露 `window.DPPublic.init(container?)`，双壳各自调用；现有 dp- 前缀 id 与主应用共存不冲突（若冲突点存在，改容器作用域选择器并附清单）。
2. **主应用内嵌**：index.html 新增 `#page-lootpool` 页签容器（复刻 data.html 主体骨架），侧边栏「数据公示」入口更名「**副本掉落**」、点击=**应用内切换 tab**（不再新开标签页）；登录用户与其余 tab 体验一致。
3. **免登录薄壳**：data.html 保留为独立公开入口（匿名可开），同一模块渲染；页头标题更名「副本掉落」（副标同步）；主应用页签标题更名。
4. 版本串三头口径更新（index/data/recruit 若有）；data.html 的 no-cache 头不动。

### WP5 验收

- 登录用户：侧边栏点「副本掉落」应用内切换（无新标签页），其余 tab 零回归；
- 匿名无痕窗口：data.html 独立打开正常渲染（免登录不死）；
- 两壳同版本数据一致（同库同源）；标题/入口更名全站一致无残留「数据公示」字样（更新日志等历史记录除外）；
- 主应用回归：npm test + SEC-001 全绿、登录/权限/其他页签零影响；版本串递增；零报错零 404。

---

## 总红线

- WP1 之外不动插件既有导出字段格式（向后兼容）；sql/19 只加列不改列；
- WP2-WP4 只动公示页三文件；WP5 碰主应用以第 2/3 条为限，不顺带改主应用其他逻辑；
- 零依赖红线：星 SVG 自绘、动画纯 CSS，禁任何库；
- 8.12 前转换器格式冻结；各 WP 分开 commit、三段式、不 push；
- 每 WP：npm test + SEC-001 回归全绿、测试数据自清理复核为零、版本串递增；
- 发现与本文档冲突先报告运营定夺，不擅自改设计。
