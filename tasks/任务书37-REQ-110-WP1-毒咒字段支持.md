# 任务书 #37：REQ-110 WP1 毒咒（Venomcurse）字段支持——数据模型+录入+卡片展示

> 已定案（2026-08-11 运营裁）：插队第四批最前，S2 团本掉落录入前必须就位。字段=文本标签型（存「毒咒」，可扩展未来其他咒）。范围外（本包不做）：插件采集（插件 1.0.9 批次）、毒咒筛选项（任务书 #30 增补）。§7 第四批第 1/3 个版本。前置侦察先送审（§1）。

## 一、背景规格（运营截图在案）

12.1 物品 tooltip 品质行下新增绿色「毒咒」标签行；毒咒装备带专属毒咒效果词条（触发 Venomcurse），效果文本本身走现有特效字段，本包新增的是**毒咒标签字段**。

## 二、前置侦察（先送审）

1. `boss_loot` / `dungeon_loot` 表列清单；`get_public_loot_detail` RPC 定义与字段透出方式（加列是否需改函数）。
2. 数据中心（数据管理）掉落录入/编辑表单现状与字段写入链。
3. dataPublic.js 卡片 meta 行结构与现有徽标样式族（如「可兑换本赛季套装」标记）。
4. 智能导入/converter 链路字段映射点（为插件 1.0.9 预留，本包只确认映射位置不改）。

## 三、施工规格

### WP1 数据模型（sql 迁移）
- 两表各加列：`venomcurse`（毒咒标签）——**text、可空、默认 NULL**，存「毒咒」；文本标签型（未来其他咒直接存新值，不再加列）。
- 走 sql/ 目录版本号迁移：两表 ADD COLUMN + 列注释；备份→docker exec→NOTIFY pgrst→回滚注释（迁移纪律）；迁移报告随完工报告送审。
- 存量零回填（S1 无毒咒装备，如实声明）。

### WP2 录入 UI
- 数据中心掉落录入/编辑表单加「毒咒」字段：**预设选项下拉**（无 / 毒咒），禁自由输入；列表/详情同步展示。

### WP3 RPC 透出 + 卡片展示
- `get_public_loot_detail` 透出 venomcurse 字段（按侦察①结论改函数或确认自动透出）。
- 装备卡 meta 行加**「毒咒」徽标**：仅 venomcurse 非空时渲染；样式沿用现有徽标族（参照套装可兑换标记），毒咒绿色调可与主题协调自定；毒咒效果文本走现有特效行，零新逻辑。
- 双宿主（公开壳 data.html + 登录壳 #page-lootdrop）同一渲染器自动覆盖。

## 四、验证与纪律

- verify 覆盖：迁移后两表列在、录入写读一致、RPC 透出、卡片徽标渲染（有毒咒样本显示/无样本不显示）、picker 不受影响；§4 样本=测试数据自建自清理。
- **公示页 308 基线零回归**（S1 全无毒咒→线上卡片零变化，逐值复测）。
- §1 审计单/§2 computed 断言；回归八组+task36 全绿清零。
- 版本串 20260811.47 → 递增两壳 16 处；changelog 补录 REQ-110。
- 完工报告送审，**不 commit 不 push**。

## 五、B 表（push 后运营验收，随批交付）

| # | 内容 |
|---|---|
| B1 | 数据中心录入/编辑一件掉落，「毒咒」字段可选可存 |
| B2 | 副本掉落装备卡：毒咒装备显示「毒咒」徽标，特效行正常 |
| B3 | 无毒咒装备卡片无徽标，与现状一致零变化 |
| B4 | 公示页公开壳同步显示徽标 |
