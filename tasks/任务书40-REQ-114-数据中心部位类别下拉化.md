# 任务书 #40：数据中心编辑表单部位/类别词表下拉化（REQ-114）

> 执行方：Kimi Code ｜ 单工作包、一个 commit ｜ 密钥：不涉及 ｜ 朋友侧：零操作
> 需求登记：**REQ-114**（2026-08-12 运营 REQ-110 测试时发现编辑表单字段与现有不匹配、饰品 item_type=「其它」，裁定「做吧」）
> 前置事实（REQ-097 已建成）：`js/lootTaxonomy.js` 词汇表单一真源在位（SLOT_OPTIONS 12 部位/WEAPON_GROUPS/ARMOR_TYPES/isMiscSlot 等），双壳可消费。毒咒字段同款 `type:'select'` 先例（任务书 #37，mdEditorSave 空串→NULL 统一口径，app.js:12053）。

---

## 一、WP1｜双表单 slot/item_type 下拉化

- 范围：`mdEditLootItem`（字段数组 app.js:12379-12386 + payload 12393）与 `mdEditDungeonLootItem`（12518-12525 + 12531）——**双表单复制维护铁律，两处同步改**。
- `slot` 改 `type:'select'`：选项 = LootTaxonomy.SLOT_OPTIONS（12 部位）+ 库内实测存在的其它 slot 值（副手物品/杂项/套装兑换物等，§1 盘点后列全）——**数据中心是全量管理入口，杂项/兑换物行也要能编辑，选项必须覆盖库内全部现存值**。
- `item_type` 改 `type:'select'`：选项 = 武器组全量（单手剑/单手锤/单手斧/匕首/拳套/战刃/魔杖/双手剑/双手锤/双手斧/长柄武器/法杖/弓/弩/枪械/盾牌）+ 护甲 4 项（板甲/锁甲/皮甲/布甲）+ 库内现存其它值（其它/装饰/垃圾/图样类/坐骑等，§1 盘点列全）。
- **原值兜底**：编辑存量行时若原值不在选项表，该值作为额外选项插入并选中（显示原值、不强制改写、不报错）；§4 声明此类行数。
- 空串→NULL 口径、payload 组装、校验逻辑不动。

## 二、WP2｜选项清单单一真源化

- 下拉选项从 `LootTaxonomy` 派生（SLOT_OPTIONS/WEAPON_GROUPS/ARMOR_TYPES 展开+杂项族补充常量），**禁在 app.js 内联第三份词表**；杂项族补充常量若词表没有，加到 lootTaxonomy.js（如 MISC_ITEM_TYPES）再消费。
- §1 先盘点：boss_loot 190 行 + dungeon_loot 204 行的 slot/item_type 全量 distinct 值对照表，随报告送审（不阻塞开工，完工报文附）。

## 三、规范与红线

- 只动数据中心两个编辑表单与 lootTaxonomy.js（如需补常量）；不碰 picker、不碰公示页、不碰回填语义、零 schema。
- §2 生效值断言（select 渲染项数/选中值）；§4 样本声明；§6 版本串 .51→.52（两壳 16+2，lootTaxonomy 已在引用内，无新增）。
- 零依赖；测试数据自清为零；完工**不 commit 不 push**，报告+对照表送审。

## 四、验收

- verify：双表单 select 渲染断言（选项数=词表派生长度+兜底值）、选/存/读回环断言、原值兜底行渲染断言。
- 回归：task27×3/29/31/32/bug071/34/35/36/37/38/39 + npm + SEC-001 全绿；308 基线零漂移。

### B 表（运营手工，3 项）
| # | 操作 | 预期 |
|---|---|---|
| B1 | 数据中心→团本掉落→编辑任意装备，点开「部位/类别」 | 两字段都是下拉，选项完整正确，当前值已选中 |
| B2 | 编辑一件饰品，看 item_type 下拉 | 原值「其它」在选项中且已选中，可改可不改 |
| B3 | 把一件装备部位改「肩部」保存→副本掉落页查看 | 保存成功；掉落页肩部筛选能筛到它（与 REQ-113 重拉联动正常）；改回原值 |

### commit 物料
标题：「任务书#40：数据中心部位/类别字段词表下拉化（REQ-114）」，三段式；不 push。
