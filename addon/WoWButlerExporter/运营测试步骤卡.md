# 运营测试步骤卡 · WoWButler Data Exporter（任务书 #26 WP1 验收）

> 目的：验证插件在真实游戏内可用。预计 15 分钟。每步做完在「结果」栏打 ✅/❌，❌ 请截图并反馈顾问侧。

## 准备

| # | 步骤 | 预期 | 结果 |
|---|---|---|---|
| 1 | 把 `WoWButlerExporter` 文件夹复制到 `_retail_\Interface\AddOns\` | 文件夹内含 .toc / .lua / README | ☐ |
| 2 | 登录界面「插件」里勾选本插件（必要时勾「加载过期插件」） | 列表可见「魔兽管家数据导出器」 | ☐ |
| 3 | 进入游戏输入 `/wjdc` 回车 | 聊天框显示用法提示 | ☐ |

## 主链路实测

| # | 步骤 | 预期 | 结果 |
|---|---|---|---|
| 4 | 输入 `/wjdc all`，等待进度滚完 | 聊天框逐副本提示「团本：xxx（N 个 BOSS，M 件掉落）」「大秘境：…」，最后提示「已导出，请 /reload」；团本段会先提示「四难度档采集已启用」、大秘境段提示「大秘境难度档采集已启用」（1.0.12 起），并逐 BOSS 提示「档重扫完成（N/M 件带档）」，**总时长约为单档 4 倍（15–20 分钟量级）属预期**；导出开始/结束有「导出 run …」标记+时间戳（1.0.12 起）；若出现红字「N 件物品解析失败记 failed」，/reload 后重跑一次再判定；若出现红字「档采集通道不可用」，属自动回退单档，文件照常可用但请截图反馈 | ☐ |
| 5 | 输入 `/reload` | 界面重载，无报错弹窗 | ☐ |
| 6 | 打开 `WTF\Account\<账号名>\SavedVariables\WoWButlerExporter.lua` | 文件存在，开头为 `WJDCDump = {`，含 `meta`、`raids`、`dungeons` 段（1.0.4 起不再含 `tier` 段；1.0.6 起 loot 行含 `primary_values`/`secondary_values` 数值字段，如 `["secondary_values"] = { ["爆击"] = 300, ["急速"] = 100 }`；1.0.7 起**团本** loot 行另含 `primary_tiers`/`secondary_tiers` 档字段，如 `["secondary_tiers"] = { ["lfr"] = { ["爆击"] = 280 }, ["normal"] = { ["爆击"] = 300 }, … }`，只记存在的档；1.0.12 起**大秘境** loot 行同样产 tiers（普通/英雄/史诗/时空漫游，因副本而异只记存在的档）；`meta` 含 `run_id` 与 `tier_channel`/`dungeon_tier_channel` 通道状态） | ☐ |
| 7 | **抽查 BOSS ①**：游戏内打开副本手册（冒险指南）→ 任选一个当前团本 BOSS → 对掉文件里同名 BOSS 的掉落列表 | 件数一致（正常量级每 BOSS 6–12 件），抽查 3 件装备名称与手册一致 | ☐ |
| 8 | **抽查 BOSS ②**：换一个大秘境副本 BOSS 同上核对 | 件数一致，名称一致 | ☐ |
| 9 | **数据质量抽查**：文件中任选 3 件装备，核对 `ilvl` 与游戏内手册显示装等一致（应为当前赛季真实装等量级，不是 44 之类小数字）；文件内不应出现 `type = ""` 的空类型行 | ilvl 一致、无空 type 行；若 BOSS 条目带 `failed` 列表，聊天框应有对应红字报告（两者必须同时出现，缺一即 ❌） | ☐ |
| 10 | 输入 `/wjdc me` → `/reload` → 重新打开导出文件 | 文件只含 `meta` 和 `me`，角色名/服务器/职业/专精/等级/装等/公会名与本人角色面板一致（装等允许 ±1 的四舍五入差） | ☐ |
| 11 | 把第 6 步（/wjdc all）与第 10 步（/wjdc me）两次文件分别发给顾问侧 | 注意：`/wjdc me` 会覆盖文件，**请先备份 all 产物再跑 me**，或跑完 me 再跑一次 all | ☐ |

## 判定

- 全部 ✅ → WP1 游戏内验收通过，管道进入 WP2 实文件端到端（WP3 彩排）。
- 任何 ❌ → 截图 + 导出文件（若已生成）发顾问侧，注明步骤编号。

## 注意事项

- 测试期间**不要**用记事本编辑导出文件。
- 大秘境内、战斗中跑命令不影响结果，但建议在主城空闲时跑，避免卡顿。
- 插件不联网、不读账号数据，只读游戏自带副本手册，可放心常驻。

## 附：报障取证 + 1.0.12 验收跑（V1-V9 签字判据）

> 用途：某件装备游戏内 tooltip 有「毒咒」绿字标签行/「装备：…」特效行，但导出文件里 `effect`/`venomcurse` 为空时，跑本取证把 tooltip 原始行完整倒出，供顾问侧对照定位漏读形态。
> 1.0.11 定论：平面双通道（SetItemByID / 裸链接）对 EJ 预览态装备天生缺特效/毒咒行，导出已改挂难度上下文通道（史诗档链接）；**悬停直读（probe hover）为同类取证标准件**。
> 1.0.12：两跑实测定损修复包——**首次真机跑即验收跑，聊天框现场全留（完整截图）**，按 V1-V9 逐条判定。
> 1.0.13：断链零留存——每实例收尾即落表，中断跑出的文件 `meta` 带 `partial = true`（中断产物仍可发顾问侧，已扫副本数据不丢）。
> 1.0.14：链死（C stack overflow）与跨难度错位根治——连跑稳定性与档值带档率为本轮重点观察项；任何中断后需 /reload 再跑。
> 1.0.16：真机核毕三修（E1 过滤器污染/E2 毒咒残差/E3 单件主属性）——跑后**先截图聊天全文再 /reload**；「毒咒疑似行」黄字出现请一并截图。
> 1.0.17：1.0.16 翻车打回修 v2（F1-F5）——过滤器清零改官方重置接口（1.0.16 自动清空实际未生效且误把槽位钉成「头部」，本版根治）、枚举等待列表重建完成再扫、0 件掉落不再静默跳过（重试 3 次仍 0 会红字）、同 BOSS 重复掉落去重；任一副本 0 件时本跑不覆写上次计数基线。
> 1.0.18：1.0.17 双跑打回三修（BUG-094/095/096）——钉难度前校验副本是否支持该档（首选档无效自动改钉，快照行出现「改钉」注记属正常）、完整性质量门（缺副本或任一 0 件红字逐一点名+不覆写计数基线，判空跳过副本记入导出文件 `skipped_instances`）、BOSS 选中读回校验（未生效重试 3 次，仍失败记 `selectFail` 空占位）、单 BOSS 超 40 件判异常红字、毒咒支持「史诗↵毒咒」两行形态；**每段结束新增黄字「重建钩子统计（G1 查证）」请一并截图**。
> 1.0.19：1.0.18 冒烟红打回修（BUG-096 转码）——BOSS 掉落枚举改显式指定 BOSS 序号（不再依赖选中态；1.0.18 选中读回校验真机三连翻车，读回链整链拆除、`selectFail` 标记退役）、单 BOSS 超 40 件守卫保留、冒烟红字文案按实态修正；**冒烟门槛与全量判据不变**。
> 1.0.20：1.0.19 冒烟半红打回修（096 根治确认绿；094 域回归——合法性闸以当前选中副本为判定对象而副本选中从不复位，鲁阿夏尔 lfr/17 被误判致 0/8 带档）——钉档/判合法前先确认副本选中成立（名字比对+自动重选，不符红字具名绝不错判）、闸判否黄字明示「IsValid（档）=false @ 当前实例=X」、「切档读回失败」附当前实例+IsValid+读回三要素；**冒烟门槛加严：鲁阿夏尔 8/8 全带档 + 拉维 7/7 全带档**。
> 1.0.21：BUG-094 二次翻转终案——运营截图实证至暗之夜=资料片名借用的世界首领整合类目、无难度维（难度下拉不存在），唯三真实例有下拉（毒牙祭坛=普通/英雄/史诗、潮缚石窟=世界/普通/英雄/史诗、烈毒之渊=随机团队/普通/英雄/史诗；「世界」档=暴雪 FrameXML "World"->PrimaryRaidLFR，行为同 LFR 显示为世界）——档扫改**按实例自适应有效档探测**：只扫有效档、无效档快照注记「跳过档=key」（正常态）、零有效档=无难度维单趟采集记 normal 单档（快照注记「无难度维（世界首领类）」，与 1.0.15 数据形态连续）、整 BOSS 0 带档降格黄字；**冒烟团本目标改烈毒之渊第 1 BOSS 盘魂者内克扎莉（四档全有效，lfr 路径进冒烟网），至暗之夜留全量验**。
> 1.0.22：BUG-097（P0）+BUG-098（P1）打回修+BUG-095 重开随修——1.0.21 全量双跑每个多 BOSS 实例仅 1 号 BOSS 出件、2 号起全 0（A/B 确定性全等）：主根=1.0.19 枚举双参化误读第二参语义（暴雪 FrameXML 实证第二参=多归属 BOSS 第 N 归属序、非 BOSS 序号选择器，单归属件 N≥2 恒 nil）——枚举回滚选中态单参（1.0.17 健康路径），串 BOSS 防护改逐件比对掉落条目自带归属 ID（整表皆串/空表=重选 BOSS+等列表重建重试 3 次，仍败红字记空占位）；档扫陈旧/空表重试同样补重选（旧重试原地空转）；**完整性质量门加件数量级闸**（每本低于下限即红字拒写计数基线：至暗 6/潮缚 11/烈毒 110/大米各本按 1.0.9 基线−10）+空掉落占位 BOSS 具名入违规；**冒烟堵门洞：团本目标改烈毒之渊全 8 BOSS（判据 114±4）、大米目标毒牙祭坛前 2 BOSS**——2 号位起枚举路径自此进冒烟网。
> 1.0.23：BUG-099 毒咒形态补丁（1.0.22 冒烟 097 判绿但毒咒跨行匹配 8/8 未中，run 20260815-005810 八件 mythic 疑似 dump 单元均呈「史诗↵毒咒」合一格）——毒咒匹配加第三级**单串内嵌换行形态**（剥码后 `^史诗%s*\n%s*毒咒%s*$`，免疫锚不变：名行以字续不命中、flavor 以「。」收尾不命中）；**「毒咒疑似行」黄字 dump 改转义可见化**（`\n`、`|` 逐字节显形，下一跑字节形态一锤定音）；**毒咒八件全中列入冒烟硬项**（validate-export.js 同步硬闸）。
> 1.0.24：BUG-099 快补——1.0.23 冒烟主体绿、毒咒 0/8 仍红；转义 dump 铁证=单串「史诗\n毒咒」纯 \n 零杂码（夹码假设证伪），而显式 \n 模式经顾问 LuaJIT 实证理论必中、真机未中（匹配输入串≠dump 串，悖论留档）——按顾问定向：单串形态级模式改 `^史诗%s+毒咒%s*$`（不指名换行符，%s+ 通吃 \n/空格/混合形态；免疫锚不变：兑换物名行/flavor 句回归实证不误伤）；疑似 dump 豁免条件同步；冒烟门槛不变。
> 1.0.25：BUG-099 终修+取证一体版——1.0.24 冒烟主体三连绿、毒咒 0/8 仍红，悖论闭合：非 %s 不可见字节实锤（U+00A0/U+200B/U+FEFF/U+3000，%s+ 对其失明）——①单串形态级改**字节无关版** `^史诗.-毒咒%s*$`（`.-` 吃掉任意夹杂字节，不管夹的是什么；免疫锚不变，fengari 断言覆盖合成不可见字节串必中+四类免疫不误伤）；②hex dump 兜底随版带上（仅八件 ID 疑似行）——连字节无关版都不中=「史诗」「毒咒」字面本身非预期字码，hex 截图一跑定死（最后一种可能）。冒烟门槛不变。
> 1.0.26：BUG-099 终修——hex 一跑定案，根因=**色码 hex 段内嵌空格畸形码**（raw=`|cFF 0FF 0史诗|r\n|cFF 0BF D毒咒|r`，挤掉空格=6 位色值 FF0FF0/FF0BFD）：严格剥码 `|c%x+` 遇空格断裂剥不掉→匹配串以「|」开头→所有 `^史诗` 锚模式首字节即败（四版全灭同一根因；聊天显示干净=渲染器对畸形色码宽容）。修法=venomcurse 判定路径专用加固剥码（`|c[%x ]+` 兼容内嵌空格畸形码，先 |c 后 |r 再 trim，局部不触全局剥码）+模式回归严格版 `^史诗%s*毒咒%s*$`（.- 版退役）；命中后疑似 dump/hex 行自动消失（=修复生效现场证据）。
> 1.0.27：BUG-100 终修 + REQ-116 副轨——①伪实例「史诗钥石地下城」的 2 个空 BOSS 占位不再计入 095 质量门违规源（1.0.26 定版真机现形误拒写基线；红字取证提示照旧保留）——**闭环标志=修复后首个干净全绿跑 LastCounts 基线重建成功：聊天框无「拒写/不覆写」红字、导出文件无 `meta.abnormal`**；②每档装等采集 `ilvl_tiers`（只加新键，既有字段零改动），**validator 预判清单新增「ilvl_tiers 非空率」报告行**（报告型不升硬闸；旧版导出提示「未采集（旧版导出）」不判负）。冒烟门槛与全量判据不变。
> 冒烟模式（1.0.17 起）：`/wjdc smoke` 扫烈毒之渊全 8 BOSS + 毒牙祭坛前 2 BOSS（1.0.22 起，时长数倍于单 BOSS 版属预期），黄字全要素与全量同链——**全量 A/B 前先跑冒烟：冒烟绿再连跑 A/B，冒烟红直接截图打回，不烧全量**。

| # | 步骤 | 预期 | 结果 |
|---|---|---|---|
| Q1 | 确认插件为 1.0.27（导出启动语显示「插件 v1.0.27」；旧版先整目录替换再 /reload）。**注意：任何一次导出中断（红字报错/客户端异常）后，需 /reload 再跑下一次**（并发锁不自动释放，1.0.13 起中断跑已扫数据不丢；1.0.15 起超 5 分钟无进展会有哨兵红字主动提醒）。**红字取证要求：出现任何红字报错，取整屏截图或 BugSack 错误栈全文一并反馈**（SecureTypes 案已销案观察关闭，新栈照旧回传） | 版本号正确 | ☐ |
| Q2 | 鼠标**悬停**报障装备（背包/角色面板/副本手册掉落均可）保持不动，聊天框输入 `/wjdc probe hover` | 聊天框滚出悬停直读诊断：实物链接 → H 通道全行原样 dump → 特效行/毒咒行判定 → 解析结果 effect/venomcurse 值 | ☐ |
| Q3 | 聊天框输入 `/wjdc probe 271876`（觉醒恐牙胸甲，报障样本；查别的装备就把数字换成对应物品 ID） | 物品级诊断：API 通道（C_Item.GetItemStats）→ A/B 平面通道 dump 对照 → 解析结果 | ☐ |
| Q4 | **先跑冒烟**：`/wjdc smoke`——**冒烟门槛（1.0.23 口径）：快照 slot=15 + 烈毒之渊全 8 BOSS 合计 114±4 件且每 BOSS 均出件（2 号位起枚举路径在网）+ 拉维 7/7 全带档 + 扭缠盘蛇>0 件 + 毒咒八件全中（venomcurse=毒咒 逐件核对，BUG-099 硬项）**；冒烟绿再继续，冒烟红直接截图打回不烧全量。**注意：冒烟时长数倍于旧单 BOSS 版属预期**；094 的病是「团本阶段先跑过」的会话态，只有全量 A/B 能终判；**验收跑**：同一游戏会话内 `/wjdc all` **连跑两次（跑 A → 中间不 /reload → 跑 B）**；**看到「已导出」提示后先完整截图聊天全文，再 /reload 写盘**（本轮曾因先 reload 丢失聊天记录，V8/V12 顺延教训）；两跑聊天框截图 + 导出文件发顾问侧转换复核（SavedVariables 直读+validate-export.js 预判链路已就位，REQ-121）。**可选跑 C**：另起会话强制中断一次导出（跑到一半直接 /reload），验 D7 断链零留存 | 逐条对 V1-V17（见下） | ☐ |

**V1-V17 验收判据（顾问终审签字版 V1-V9 + 1.0.14 追加观察项 V10-V12 + 1.0.18 三修判据 V14-V17 + 1.0.19 修订 V16 + 1.0.20 门槛加严 + 1.0.21 终案口径 + 1.0.22 口径（097 零件 BOSS 硬闸/095 量级闸/冒烟全 8 BOSS/098 首跑即绿）+ 1.0.23 口径（V3 毒咒八件冒烟硬项/dump 转义显形）+ 1.0.24 口径（单串 %s+/hex dump 并入）+ 1.0.25 口径（字节无关版，已被 1.0.26 取代）+ 1.0.26 口径（V3 终修：畸形色码加固剥码+严格模式回归，BUG-099）+ 1.0.27 口径（BUG-100 伪实例空占位出 gating——闭环标志=修复后首个干净全绿跑基线重建成功无拒写行；ilvl_tiers 非空率报告进 validator 预判清单），逐条 ✅/❌）：**

| # | 判据 | 结果 |
|---|---|---|
| V1 | 同会话连跑两次（中间不 reload）：两跑各实例计数一致，团本合计 135±、大米合计 225± | ☐ |
| V2 | 大米 28 件缺席 + 4 件新增（ID 清单见 `docs/插件1.0.12-修法送审件.md` 附录）逐一销账：错档/去重/真缺数据，顾问侧给去向表；**E1 修后三基数复核：28 件缺席销账 + 大米饰品 24 件 + 兑换物 20 件** | ☐ |
| V3 | 271876（觉醒恐牙胸甲）：`effect=「装备：你的法术和技能有几率使你的爆击提高377，但会使你的其他次要属性降低62，持续12秒。」`、`venomcurse=毒咒`；**1.0.26 口径（BUG-099 终修，根因=色码 hex 段内嵌空格畸形码）：毒咒八件全中=冒烟唯一硬项（validate-export.js 文件侧硬闸）+ 兑换物五件与 270165 flavor 句不误判；venom 路径加固剥码（`|c[%x ]+` 兼容畸形码）+模式回归严格版 `^史诗%s*毒咒%s*$`；中则疑似 dump/hex 行自动消失（豁免生效=修复生效证据），仍不中则 dump/hex 照旧出、整屏截图发顾问** | ☐ |
| V4 | 后二 BOSS（盘卷祭坛/乌拉特克）必扫到档（掉落行带 `primary_tiers`/`secondary_tiers`），无任何跨 BOSS 截断（聊天框无「后续 BOSS 不再切档」类提示——该机制已删） | ☐ |
| V5 | 此前 31 件冷缓存缺损件主/副属性补齐；3 件毒锻兑换物 `type` 归位（不再错标武器） | ☐ |
| V6 | 大秘境饰品 `effect` 非空通过率（顾问侧统计，目标 24/24；**终审复验口径：M+ 饰品 effect 非空 24/24**） | ☐ |
| V7 | 导出文件不出现伪实例「史诗钥石地下城」 | ☐ |
| V8 | 三条观测日志在聊天框可见：①每实例「难度档快照=…」②与上次计数不一致时黄字「计数对比：…」③开始/结束「导出 run …」标记+时间戳 | ☐ |
| V9 | converter 转换零回归：转换行数 = 导出行数 | ☐ |
| V10 | 暖缓存连跑无爆栈：跑 B（暖缓存）全程无「C stack overflow」红字（D1/BUG-089，1.0.14） | ☐ |
| V11 | 跨跑带档率稳定：跑 A/跑 B 同 BOSS 带档件数一致（重点复测鲁阿夏尔/索姆贝兰，D4/BUG-090，1.0.14） | ☐ |
| V12 | 档扫黄字「缺席=/陈旧跳档=」计数在聊天框可见并截图回传（供评估 absent 是否真·该档不掉，1.0.14） | ☐ |
| V13（可选跑 C） | D7 断链零留存：强制中断后 /reload，导出文件含中断前已扫实例 + `meta.partial = true`；完整跑文件无 partial 键（BUG-088，1.0.13） | ☐ |
| V14 | **BUG-094 判据（1.0.21 终案口径）**：大米 8 本全出件、合计 225±；钉档失败红字绝迹（自适应探测生效）；无效档快照注记「跳过档=key」可见且属正常（如毒牙祭坛跳过 timewalking）；旧本落非 23 档须有注记 | ☐ |
| V15 | **BUG-094/095/097 判据（1.0.22 口径）**：三团本实例全在场——至暗之夜 **4 首领全出件 + 快照「无难度维（世界首领类）」注记 + tiers 仅 normal 单档**（世界首领整合类目终案形态；件数判据 32±4=DB 已录 4 首领各 8 件口径，**待运营裁定**，旧 8±2 为 12.0 单 encounter 时代口径）、潮缚石窟 13±、烈毒之渊 114±4；**任何 0 件 BOSS=097 特征直接判红**；`skipped_instances` 只在真空时出现且红字具名；BUG-095 量级闸生效=件数低于下限（至暗 6/潮缚 11/烈毒 110/大米各本 1.0.9 基线−10）或存在空占位 BOSS 即红字点名+拒写 `WJDCLastCounts`+`meta.abnormal` 落键——本次 1.0.21 双跑基线已被污染（烈毒 16 等坏值），**修复后首个全绿跑即重建基线** | ☐ |
| V16 | **BUG-096/097/098 判据（1.0.23 口径）**：冒烟门槛=快照 slot=15 + **烈毒之渊全 8 BOSS 合计 114±4 件且每 BOSS 均出件 + 拉维 7/7 全带档 + 扭缠盘蛇>0 件 + 毒咒八件全中（BUG-099 硬项，八件均在烈毒之渊冒烟范围内）**且**物品名确属该 BOSS**（件数不对/归属错位/带档缺一/毒咒缺一即现形打回）；A/B 连跑同实例件数全等（V1 升级版）；单 BOSS>40 守卫零触发（触发即异常具名，不许静默）；枚举串表防护黄字「剔除串表件 N 件次」出现即截图回传（不应出现）；读回/selectFail 机制 1.0.19 已整撤，相关红字不再出现属预期；**098 判据=冒烟后首跑全量即全绿**（A 跑不再有 staleSkip 全档/空表 absent 形态）；每段结束黄字「重建钩子统计（G1 查证）」截图回传——超时放行 ≈ 总数即坐实 G1 恒 true | ☐ |
| V17 | 毒咒形态（1.0.26 三级匹配：单行/单串（加固剥码+严格版 `%s*`）/跨两行，判据不变）：271876（觉醒恐牙胸甲）`venomcurse=毒咒` 持续命中；三形态均判有 | ☐ |

- probe 是纯只读诊断，不写导出文件、不改游戏状态，可随时跑。
- 判断口径：H 通道（悬停实物链接）有特效/毒咒行而 A/B 平面通道没有 = 难度上下文差异实锤（1.0.11 导出已改走难度档链接覆盖）；H 通道也没有 = 该形态需再取证，靠 dump 原行对照定位。
