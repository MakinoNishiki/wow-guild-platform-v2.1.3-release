# 魔兽管家数据导出器（WoWButler Data Exporter）

> 游戏内插件，一键把「当前资料片」的副本手册数据导出成文件，发给顾问侧转换录入。
> 零外部依赖、不联网、不碰账号数据，只读游戏自带的副本手册和角色信息。

## 一、安装（一次即可）

1. 关闭游戏（或退回角色选择界面）。
2. 把 **`WoWButlerExporter` 整个文件夹** 复制到：
   `World of Warcraft\_retail_\Interface\AddOns\` 下面
   复制完路径应该是 `...\_retail_\Interface\AddOns\WoWButlerExporter\WoWButlerExporter.toc`
3. 启动游戏 → 登录界面左下角「插件」按钮 → 勾选「魔兽管家数据导出器」→ 若提示过期，勾选「加载过期插件」（12.x 正式服接口号已按 120000 填好，正常不会提示）。
4. 进入游戏，聊天框输入 `/wjdc` 能看到用法提示即安装成功。

## 二、使用（每条命令导完都要 /reload）

| 命令 | 导出内容 |
|---|---|
| `/wjdc all` | 全量：团本掉落 + 大秘境掉落 |
| `/wjdc raid` | 仅团本掉落 |
| `/wjdc mplus` | 仅大秘境掉落 |
| `/wjdc me` | 仅本人角色档案（角色名/服务器/区域/阵营/种族/职业/专精/等级/装等/公会名） |
| `/wjdc probe [序号\|物品ID\|hover]` | 诊断（不出文件）：团本序号=打印副本手册接口原始返回；物品ID=打印数值采集链核验（1.0.6 起）；`hover`=悬停链接直读（1.0.11 起，鼠标悬停装备不动再输入，取实物链接全行 dump，取证标准件），仅在顾问侧要求时使用 |

> **1.0.11 说明（S2 录库前修复包）**：
> ①**冷缓存自动等待**：物品数据未加载完不再直接解析（旧版曾致部分装备主属性缺损/类型错位），
> 插件自动排队等数据就绪，无需 /reload 重跑补齐；仍有个别记 `failed` 的会红字单列，照原流程反馈。
> ②**特效/毒咒改走难度档链接**：团本装备特效文本按四难度档链接读取、导出存**史诗档**文本；
> 毒咒标签按史诗档判定（平面 tooltip 通道对预览态装备天生缺这些行，已实锤）。
> ③属性数值 API 通道换用 `C_Item.GetItemStats`（12.1 起旧全局函数已移除）。
> 导出字段格式零改动，转换器照常可用。

> **1.0.12 说明（两跑实测定损修复包）**：
> ①**同一会话可连跑多次**：上一版在同一会话第二次导出可能出现团本 0 件/大米件数漂移
> （难度档状态未复位所致），已根治，连跑两次结果应一致；
> ②**逐件隔离**：单件采集失败只跳过单件，不再出现「整 BOSS 回退」「后续 BOSS 不再切档」的连坐；
> ③**大秘境也按难度档采集**：普通/英雄/史诗/时空漫游逐档探测（因副本而异，没有的档自动跳过），
> 饰品特效按史诗档读取，导出大秘境掉落新增 `primary_tiers`/`secondary_tiers` 字段；
> ④**伪实例过滤**：「史诗钥石地下城」不再混入导出；
> ⑤**导出日志更可查**：开始/结束带 run 标记+时间戳，每实例打印难度档快照，
> 与上次导出件数不一致时黄字对比提示。
> 导出字段格式零改动（仅大米段新增 tiers 字段），转换器照常可用。

> **1.0.13 说明（断链零留存）**：
> 旧版「全部跑完才一次性写结果」，中途崩溃/链死会让整跑数据全丢（/reload 后文件还是上一次旧数据）。
> 现改为**每跑完一个副本立即落表**：即使中断，已扫完的副本数据都在，`/reload` 写盘不丢；
> 中断跑出的文件 `meta` 里带 `partial = true` 标记，发给顾问侧时请注明是中断产物。

> **1.0.14 说明（稳定性根治）**：
> ①修复暖缓存连跑时可能报「C stack overflow」中断的隐患（导出链改为逐帧推进，现场表现为更稳、略慢属预期）；
> ②修复跨难度掉落列表错位导致的档值漏采（切档后等一帧再扫+按物品 ID 对齐，带档率跨跑波动消除）；
> ③回调报错文案改实话：报「导出链中断」即为真中断，按提示 /reload 后重跑即可，已扫数据不丢。

> **1.0.15 说明（断链哨兵）**：
> 导出超过 5 分钟毫无进展时，聊天框红字主动提醒「导出链已超过 300 秒无进展，判定链中断」——
> 看到它按提示 /reload 后重跑即可，已扫数据不丢；正常导出不会触发。

> **1.0.16 说明（真机核毕三修）**：
> ①过滤器自动清空在本版实际未生效且误把槽位过滤器钉成「头部」（已随 1.0.17 根治，请直接使用 1.0.17）；
> ②毒咒标签采集加固（全难度档扫描+疑似行自动取证，聊天框出现「毒咒疑似行」黄字请截图反馈）；
> ③修复个别装备主属性漏采（行首隐藏码所致）。

> **1.0.17 说明（翻车打回修 v2）**：
> ①**过滤器处理根治**：改用官方重置接口清空手册职业/专精/槽位过滤器，导完自动还原——
> 1.0.16 的自动清空实际未生效（且误把槽位钉成「头部」致大量掉落漏采），本版起真正无需手动操作；
> ②枚举等待掉落列表重建完成再扫（修复同会话连跑时第二次导出大量漏件）；
> ③某 BOSS 掉落枚举为 0 时不再静默跳过——自动重试 3 次，仍为 0 会红字提示（看到请截图反馈）；
> ④同一 BOSS 重复掉落自动去重；任一副本本次 0 件时，本跑不覆写上次计数基线（红字提示）；
> ⑤**新增冒烟模式 `/wjdc smoke`**：只扫至暗之夜第 1 BOSS + 毒牙祭坛第 1 BOSS，约 3 分钟出数——
> 全量连跑 A/B 前先跑冒烟：冒烟绿（无红字、掉落/档值/特效齐全）再烧全量，冒烟红直接截图打回不烧全量。

> **1.0.18 说明（双跑打回三修）**：
> ①**难度档钉设加固**：钉难度前校验该副本是否支持此档（与游戏自带手册同一道闸），
> 首选档不支持时自动改钉可用档——单难度副本（世界 BOSS 型）不再被钉成空档而整本失踪，
> 聊天框快照出现「改钉」注记属正常；
> ②**完整性质量门**：本次导出若缺副本（团本不足 3 个/大米不足 8 个）或任一副本 0 件，
> 红字逐一点名且不覆写上次计数基线——残缺跑不会污染后续对比；被判空跳过的副本会
> 记入导出文件 `skipped_instances` 字段，发文件给顾问侧时无需另附说明；
> ③**BOSS 选中读回校验**：选中 BOSS 未生效时自动重试 3 次，仍失败则该 BOSS 记空占位
> （文件里带 `selectFail` 标记）并红字提示——宁可空缺也不错配到别的 BOSS；
> 单 BOSS 掉落件数异常（>40 件）会红字判异常、本跑不覆写计数基线（数据照常导出）；
> ④毒咒标签支持两行形态（「史诗」下一行「毒咒」）识别，判定口径不变；
> ⑤每段结束新增一行黄字「重建钩子统计」（G1 查证），看到请连同其他日志一起截图。

> **1.0.19 说明（冒烟打回修）**：
> ①**BOSS 掉落枚举改显式指定**：枚举时直接指明「第几个 BOSS」取掉落，不再依赖
> 「当前选中 BOSS」状态——1.0.18 的选中读回校验真机三连翻车（鲁阿夏尔/拉维 `selectFail`），
> 读回链已整体拆除、`selectFail` 标记随之退役，掉落归错 BOSS 的病类连根拔除；
> 单 BOSS>40 件异常守卫保留（纯保险）；
> ②冒烟红字文案按实态修正：目标副本在册但采集 0 件时如实说明，不再误称「未枚举到」；
> ③冒烟门槛与全量验收判据不变。

> **1.0.20 说明（094 域回归修）**：
> ①**难度档误判修复**：1.0.18 引入的「钉难度前校验」会以当前选中副本为判定对象，而副本
> 选中状态从不自动复位——1.0.19 冒烟鲁阿夏尔被判「不支持随机团队档」致 8 件掉落全丢档值
> （件数不受影响）。本版起校验前先确认副本选中成立（选中名字比对，不符自动重选一次），
> 选中不成立会红字具名、绝不在错误对象上硬判；
> ②校验不通过时黄字明示「IsValid（档）=false @ 当前实例=X」（看到请连同日志截图）；
> ③「切档读回失败」红字附三要素：当前实例名+校验值+读回值，定位一屏到位；
> ④冒烟门槛同上加严：**鲁阿夏尔 8/8 全带档 + 拉维 7/7 全带档**。

> **1.0.21 说明（BUG-094 二次翻转终案）**：
> ①**档扫改「按实例自适应有效档探测」**（实证依据：至暗之夜实为资料片名借用的世界首领
> 整合类目，手册里没有难度下拉；只有毒牙祭坛/潮缚石窟/烈毒之渊三个真实例有难度维）——
> 每个副本先逐个难度候选问游戏「这个本有没有这一档」，只扫回答「有」的档；
> 回答「没有」的档跳过并在难度快照行注记「跳过档=xxx」（属正常，看到不用报障）；
> ②**至暗之夜这类无难度维副本**：不再切档，单趟采集，导出文件里 tiers 只记 normal 一档
> （与 1.0.15 旧文件数据形态一致，转换器无需改动），快照行注记「无难度维（世界首领类）」；
> ③「整 BOSS 0 件带档」由红字降为黄字（档已确认有效还扫不出值，多为数据侧原因）；
> 真正的通道报错（中断/切档失败）维持红字不变；
> ④**冒烟目标调整**：团本侧改扫烈毒之渊第 1 BOSS 盘魂者内克扎莉（四档齐全，能真正验到
> 随机团队档链路），判据=**盘魂者内克扎莉 16±2 件全带档 + 拉维 7/7 全带档 + 快照 slot=15**；
> 至暗之夜改在全量跑里验（判据=4 个首领、8±2 件、快照有「无难度维」注记、tiers 仅 normal 档）。

> **1.0.22 说明（BUG-097/098 打回修+BUG-095 重开随修）**：
> ①**1.0.21 全量双跑曝确定性缺陷（BUG-097）**：每个多 BOSS 副本只有 1 号 BOSS 出件、
> 2 号起全 0——根因是 1.0.19 把掉落枚举改成「显式指定 BOSS 序号」时误读了游戏接口第二参数的
> 语义（该参数实为「一件装备有多个掉落 BOSS 时取第几个归属」的展示用途，不是选 BOSS 用的，
> 绝大多数装备只有一个归属 BOSS，序号≥2 必然取空）。本版枚举改回 1.0.17 验证过的选中态路径，
> 防串 BOSS 改为逐件核对掉落条目自带的归属 ID：整表都对不上=选中没生效，自动重选重试 3 次，
> 仍失败才红字记空占位（宁空不错）；个别混入的他 BOSS 条目自动剔除并黄字计数；
> ②**重试不再原地空转（BUG-098）**：枚举 0 件重试、档扫遇到陈旧/空列表重试，都会先重新选中
> BOSS 再等列表重建——旧版只等不重选，列表永远归不了位（A 跑「列表滞留 1 号形态」和
> B 跑「列表直接空」两种现象同根）；
> ③**完整性质量门加严（BUG-095 重开）**：每个副本件数低于下限（至暗 6/潮缚 11/烈毒 110/
> 大米各本按 1.0.9 基线−10）或存在空掉落占位 BOSS，即红字逐一点名、本跑不覆写上次计数基线、
> 导出文件落 `meta.abnormal`——1.0.21 双跑 30 个空占位红字却照常覆写基线的洞就此堵上；
> ④**冒烟堵门洞**：旧冒烟只测第 1 BOSS，正是 097 三版穿网的门洞——团本目标改烈毒之渊
> **全 8 BOSS**（判据=合计 114±4 件且每 BOSS 均出件），大米目标改毒牙祭坛**前 2 BOSS**
> （拉维 7/7 全带档+扭缠盘蛇>0 件）；冒烟时长数倍于旧版属预期；
> ⑤至暗之夜件数判据拟从 8±2 改 32±4（数据库已录 4 首领各 8 件），**待运营裁定**。

> **1.0.23 说明（BUG-099 毒咒形态补丁）**：
> ①**毒咒匹配加第三级形态**：1.0.22 冒烟 097 修复判绿（烈毒全 8 BOSS 112 件全出件），
> 但毒咒跨行匹配 8/8 未中——真机疑似 dump 显示八件史诗档的「史诗↵毒咒」合在同一格里
> （12.1 疑似以单串内嵌换行形态交付，1.0.18 的「上一行+本行」拼接够不到）。本版匹配升三级：
> 纯「毒咒」行 → 单串内嵌换行（剥码后「史诗+换行+毒咒」整格判定）→ 跨两行拼接；
> 免疫锚不变（装备名行以字续不命中、flavor 句以「。」收尾不命中），兑换物五件与 270165
> 干扰件不误标的既有行为不受影响；
> ②**「毒咒疑似行」黄字 dump 转义可见化**：dump 内容里的换行、竖线逐字节显形
> （`\n` 显示为字面两字符、`|` 显示为 `||`）——即使第三级匹配仍未命中，下一跑的
> dump 截图可直接读出真实字节形态，零猜测定向下一版；
> ③**毒咒八件全中列入冒烟硬项**：八件（268215/268202/268207/271874/271875/268265/
> 271876/271878）均在冒烟范围烈毒之渊内，缺一件冒烟即判红打回；
> 注意：第三级匹配按字节形态推断落地，**若真机仍不中属预期内分支**——此时以转义 dump
> 截图为下一版唯一定向依据，勿凭肉眼猜形态。

> **1.0.24 说明（BUG-099 快补）**：
> 1.0.23 冒烟主体绿（097 修复确认）、毒咒 0/8 仍红——但转义 dump 一锤定音：
> 八件史诗档的毒咒单元就是**干干净净的「史诗+换行+毒咒」一个串，零杂码**（夹码假设证伪）。
> 怪就怪在：1.0.23 的显式换行模式经顾问侧 Lua 实证对这种串理论必中，真机却不中——
> 说明真正吃进匹配的串和 dump 出来的串存在 dump 三符（换行/回车/竖线）之外的字节差异
> （比如 Tab、不换行空格、零宽字符这类肉眼和转义都看不见的东西）。
> 本版按顾问定向快补：单串形态级模式**不再指名换行符**，改 `^史诗%s+毒咒%s*$`——
> `%s+` 通吃换行/空格/Tab/混合空白形态；免疫锚不变（必须以「史诗」起、以「毒咒」加空白收尾，
> 装备名行、flavor 句、兑换物名均不误伤，离线回归实证在案）。
> **预授权并入：字节级 hex dump**——若 `%s+` 仍不中，八件 ID 的疑似行当轮自动多打一行
> 「毒咒疑似行 hex」（逐字节十六进制，不换行空格/零宽字符这类肉眼与转义都看不见的字节
> 全部显形），与疑似 dump 一并整屏截图回传即可，一跑定字节。
> 冒烟门槛不变：绿→全量 A/B；若仍 0/8，疑似 dump 截图照旧回传（取证兜底仍在）。

> **1.0.25 说明（BUG-099 终修+取证一体版，已被 1.0.26 取代）**：
> 1.0.24 冒烟主体三连绿、毒咒 0/8 仍红——悖论闭合：隔开「史诗」和「毒咒」的是一种
> **看不见的、不属于空白类的字节**（不换行空格/零宽字符这类，空白匹配对它同样失明）。
> ①本版模式改**字节无关版** `^史诗.-毒咒%s*$`——中间的 `.` 通吃任何字节，
> 不管夹的是什么都判有；免疫锚不变（必须以「史诗」起、以「毒咒」加空白收尾，
> 名行/flavor 句/兑换物名离线回归实证不误伤）；
> ②hex 字节 dump 兜底随版带上（仅八件 ID 疑似行）——万一连字节无关版都不中，
> 说明「史诗」「毒咒」这几个字本身的字码就不是我们以为的字码（最后一种可能），
> hex 截图发顾问一跑定死。
> 冒烟门槛不变：绿→销案连跑全量 A/B；红→hex 截图发顾问。

> **1.0.26 说明（BUG-099 终修，已被 1.0.27 取代）**：
> hex dump 一跑定案——根因找到了：毒咒行原文是**带畸形色码的** `|cFF 0FF 0史诗|r↵|cFF 0BF D毒咒|r`，
> 色值的十六进制段里嵌了空格（挤掉空格=正常 6 位色值 FF0FF0/FF0BFD）。旧剥码认
> 「连续十六进制」，遇空格断开剥不掉，剩下的串以「|」开头，所有以「史诗」开头的
> 匹配模式第一个字就败了——四个版本全灭都是这一个根因；聊天里看着干净，是游戏
> 渲染器对畸形色码宽容。
> 修法两条：①毒咒判定路径**专用加固剥码**——`|c[%x ]+` 连空格一起吃，兼容正常色码
> 和这种畸形码（中文字节天然止跑，不会误吃正文；只用于毒咒判定，不动全局剥码，
> 影响面=本功能）；②匹配模式回归严格版 `^史诗%s*毒咒%s*$`（剥码修好后中间只剩换行，
> 不需要 1.0.25 的通吃宽版了，误伤面最小）。
> **冒烟唯一硬项=毒咒八件全中**：中了，聊天里的疑似 dump/hex 黄字行当轮自动消失
> （消失=修复生效的现场证据）；绿→直接连跑全量 A/B（A 跑即 098 复验场）。

> **1.0.27 说明（BUG-100 终修 + 每档装等采集，当前版本）**：
> ①**伪实例空占位不再误伤计数基线（BUG-100）**：1.0.26 全量真机现形——伪实例
> 「史诗钥石地下城」的 2 个空 BOSS 走了「空掉落占位」路径，被完整性质量门误判为残缺跑，
> 导致拒写上次计数基线（LastCounts）。本版起伪实例的空占位不再计入质量门违规源
> （红字取证提示照旧保留，不静默）；修复后首个干净全绿跑即重建基线（聊天框无「拒写」
> 红字、无 abnormal 标记=闭环标志）；
> ②**新增每档装等采集（REQ-116 副轨）**：难度档重扫时顺带记下每个难度档 tooltip 上的
> 「物品等级 N」，导出文件 loot 行新增 `ilvl_tiers` 字段，形如
> `["ilvl_tiers"] = { ["normal"] = 311, ["mythic"] = 344 }`（只记存在的档；
> 只加新键、既有字段格式零改动，转换器照常可用；旧版导出文件无此键属正常，
> validator 会提示「未采集（旧版导出）」不判负）。用途=顾问侧交叉验证静态表装等；
> ③冒烟门槛与全量判据不变：绿→直接连跑全量 A/B。

> **套装效果说明（1.0.4 起）**：`/wjdc tier` 已移除——游戏内套装效果提取在 12.x 实证不可靠，
> 套装数据改走顾问侧文章/OCR 管道录入，本插件不再导出。

> **属性数值说明（1.0.6 起）**：掉落导出新增主/副属性**数值**字段 `primary_values` / `secondary_values`
> （如 `["secondary_values"] = { ["爆击"] = 300, ["急速"] = 100 }`，任务书 #28 WP1 星标数据链）。
> 采集优先走游戏接口 GetItemStats，不可用时自动回退 tooltip 解析，无需任何额外操作；
> 原有字段（名称/部位/类型/装等/属性名/特效）格式不变，旧版导出文件转换器照常可用。
> （1.0.5 为数值字段首版，因一处 Lua 多返回值隐患在真机必炸，请直接使用 1.0.6。）

> **四难度档说明（1.0.7 起，任务书 #29 WP1）**：**团本**掉落按 随机团队/普通/英雄/史诗 四档
> 逐件切档重扫，新增 `primary_tiers` / `secondary_tiers` 字段，形如
> `["secondary_tiers"] = { ["lfr"] = { ["爆击"] = 280 }, ["normal"] = { ["爆击"] = 300 }, ["heroic"] = { ["爆击"] = 320 }, ["mythic"] = { ["爆击"] = 340 } }`
> （只记存在的档——某装备无随机档则无 `lfr` 键；无静态属性的品类天然空档）。
> **采集时长约为旧版 4 倍（全量 15–20 分钟量级）属预期**，聊天框逐 BOSS 提示「四档重扫完成」。
> 大秘境无四难度（钥石层数缩放），不产 tiers，`primary_values`/`secondary_values` 仍是其唯一数值口径。
> 若切档通道不可用（聊天框红字提示「四档采集通道不可用」），插件自动回退旧版单档采集，
> 导出文件照常可用（tiers 不出现），请把红字截图反馈顾问侧。

操作流程：

1. 聊天框输入命令，例如 `/wjdc all`。
2. 聊天框会滚动显示进度（每个副本一行）。看到红色「N 件物品未缓存记 failed」提示时**不要慌**，先 `/reload` 再重跑一次同一命令；仍失败请截图整段聊天记录反馈顾问侧（失败的物品已记录在文件 `failed` 字段里，不会静默跳过）。
3. 看到「已导出，请 /reload 或退出游戏写入文件」后，输入 `/reload`（或直接退出游戏）。
   **不做这一步文件不会写盘。**

## 三、导出文件在哪

```
World of Warcraft\_retail_\WTF\Account\<你的账号名>\SavedVariables\WoWButlerExporter.lua
```

- 账号名不确定时，直接到 `WTF\Account\` 下逐个文件夹找 `SavedVariables\WoWButlerExporter.lua`。
- 每次导出会**整体覆盖**该文件，发文件前确认刚跑的是想要的命令。
- 把这个 `.lua` 文件原样发给顾问侧即可（微信/飞书/邮件都行），**不要用记事本改它**。

## 四、常见问题

| 现象 | 原因与处理 |
|---|---|
| 红色提示「不支持副本手册接口（EJ）」 | 装错客户端了——本插件只支持 12.x 正式服（`_retail_`），不能装怀旧服。确认安装路径含 `_retail_`。 |
| 聊天框红字「N 件物品解析失败记 failed」 | 物品数据重试后仍未就绪或解析报错。`/reload` 后重跑同一命令即可补齐；仍失败截图反馈顾问侧。 |
| 掉落里个别装备缺属性/特效 | 同上，缓存问题。`/reload` 后重跑同一命令即可补齐。 |
| 找不到导出文件 | 确认已执行 `/reload` 或退出过游戏；确认找的是 `<账号名>` 下的 `SavedVariables`（不是角色名下的）。 |
| `/wjdc` 无反应 | 插件未启用：登录界面「插件」里勾选并确认已加载。 |

## 五、出问题找谁

联系顾问侧（Kimi Code 支持通道），反馈时请附上：
1. 聊天框红字提示的截图；
2. 导出文件（如果已生成）；
3. 你执行的命令和游戏版本（登录界面左下角版本号）。
